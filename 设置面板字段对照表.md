# X-AnyLabeling 个人扩展版（beta.4 fork）— 设置面板字段对照表

> ## ⚠ 状态声明（2026-08-12 核对）
>
> **基准 commit**：`77cf2832`。本表的真相源 `settings/schema.py` 和 `configs/xanylabeling_config.yaml` **均不在未提交改动清单中**（`git diff HEAD` 确认零改动），故全部 51 静态字段 + 83 快捷键 + 12 隐藏配置键的判定**与 HEAD 一致，可靠**。
>
> ---

> 本表是 [`菜单功能对照表.md`](菜单功能对照表.md) 的姊妹篇，覆盖**不挂菜单、由配置驱动**的所有可配置项——这是 `菜单功能对照表.md` 抓不到的部分（也是之前 `digit_shortcut_mode` 被遗漏的根因）。
>
> - **设置面板**：上游 beta.4 已有完整的 `settings/` 设置对话框系统（`schema.py` 全文为上游基线 `b99efef6` 引入），fork 在其上**新增字段**和**新增配置块**。
> - **来源**：`anylabeling/views/labeling/settings/schema.py`（`SettingField` 定义，51 静态字段 + 动态快捷键字段）、`anylabeling/configs/xanylabeling_config.yaml`（默认值真相源）。
> - **判定方式**：每个字段经 `git show b99efef6:schema.py | grep` 判定基线时是否已存在；yaml 顶层键经 `git show b99efef6:xanylabeling_config.yaml` 比对。
> - **状态**：`✅新增` = fork 新增字段/配置键；`📦上游` = 上游 beta.4 原版字段。
> - **面板位置**：分 `主分类(General/Shape/Canvas/Shortcuts)` → `次分类` → `组` 三级，由 `SettingField(primary=, secondary=, group=)` 决定。
>
> 全文主表统一六列：**字段 key · 显示文本 · 面板位置 · 类型/取值 · 状态 · 说明**。

---

## 一、General · Behavior（行为）

| 字段 key | 显示文本 | 面板位置 | 类型/取值 | 状态 | 说明 |
|---|---|---|---|:---:|---|
| `display_label_popup` | Display Label Popup | General/Behavior/Basic | bool | 📦上游 | 显示标签弹出 |
| `auto_highlight_shape` | Auto Highlight Shape | General/Behavior/Basic | bool | 📦上游 | 编辑模式自动高亮选中 shape 顶点 |
| `auto_switch_to_edit_mode` | Auto Switch To Edit Mode | General/Behavior/Basic | bool | 📦上游 | 自动切换选中对象到编辑模式 |
| `auto_person_instance` | Auto Create Person Instance | General/Behavior/Manual Annotation | bool | ✅新增 | 画 person 自动分配 `max(gid)+1`，免手动输 ID（提交 `440759de`） |
| `digit_shortcut_mode` | Digit Shortcut Mode | General/Behavior/Manual Annotation | enum: `rename` / `bind_draw` | ✅新增 | **数字键 0~9 行为总开关**：rename 批量改名 / bind_draw 同实例新建（提交 `440759de`） |
| `exif_scan_enabled` | Enable EXIF Scan | General/Behavior/Basic | bool | 📦上游 | 加载目录时扫描 EXIF 元数据 |
| `system_clipboard` | Use System Clipboard | General/Behavior/Basic | bool | 📦上游 | 复制粘贴使用系统剪贴板 |
| `model_hub` | Model Hub | General/Behavior/Runtime | enum: `github` / `modelscope` | 📦上游 | 模型下载源 |
| `logger_level` | Logger Level | General/Behavior/Runtime | enum: debug/info/warning/error/fatal | 📦上游 | 日志级别 |

---

## 二、General · File List / Label Dialog（文件列表与标签对话框）

| 字段 key | 显示文本 | 面板位置 | 类型/取值 | 状态 | 说明 |
|---|---|---|---|:---:|---|
| `file_list_checkbox_editable` | File List Checkbox Editable | General/File List/Navigation | bool | 📦上游 | 文件列表直接勾选 checked 状态 |
| `sort_labels` | Sort Labels | General/Label Dialog/Behavior | bool | 📦上游 | 标签排序 |
| `validate_label` | Validate Label | General/Label Dialog/Validation | enum: None / `exact` | 📦上游 | 标签校验 |
| `show_label_text_field` | Show Label Text Field | General/Label Dialog/Layout | bool | 📦上游 | 显示标签文本输入框 |
| `label_completion` | Label Completion | General/Label Dialog/Behavior | enum: `startswith` / `contains` | 📦上游 | 标签自动补全 |
| `move_mode` | Move Mode | General/Label Dialog/Behavior | enum: `auto` / `center` | 📦上游 | 移动模式 |
| `fit_to_content.column` | Fit To Content Column | General/Label Dialog/Layout | bool | 📦上游 | 列宽自适应 |
| `fit_to_content.row` | Fit To Content Row | General/Label Dialog/Layout | bool | 📦上游 | 行高自适应 |

---

## 三、Shape（图形样式）

| 字段 key | 显示文本 | 面板位置 | 类型/取值 | 状态 | 说明 |
|---|---|---|---|:---:|---|
| `shape_color` | Shape Color Strategy | Shape/Color Strategy/Mode | enum: None / `auto` / `manual` | 📦上游 | 图形配色策略 |
| `default_shape_color` | Default Shape Color | Shape/Color Strategy/Mode | color(3) | 📦上游 | 默认图形颜色 |
| `shift_auto_shape_color` | Auto Color Shift | Shape/Color Strategy/Mode | int[-1000,1000] | 📦上游 | 自动配色时的颜色索引偏移 |
| `shape.line_color` | Line Color | Shape/Drawing Style/Color | color(4) | 📦上游 | 默认描边色 |
| `shape.fill_color` | Fill Color | Shape/Drawing Style/Color | color(4) | 📦上游 | 默认填充色 |
| `shape.vertex_fill_color` | Vertex Fill Color | Shape/Drawing Style/Color | color(4) | 📦上游 | 顶点填充色 |
| `shape.select_line_color` | Select Line Color | Shape/Selection Style/Color | color(4) | 📦上游 | 选中描边色 |
| `shape.select_fill_color` | Select Fill Color | Shape/Selection Style/Color | color(4) | 📦上游 | 选中填充色 |
| `shape.hvertex_fill_color` | Hover Vertex Fill Color | Shape/Selection Style/Color | color(4) | 📦上游 | 悬停顶点高亮色 |
| `shape.point_size` | Point Size | Shape/Geometry/Basic | int[1,50] | 📦上游 | 顶点显示大小 |
| `shape.line_width` | Line Width | Shape/Geometry/Basic | int[1,20] | 📦上游 | 默认线宽 |

---

## 四、Canvas · Interaction / Wheel / Refinement / Crosshair / Cuboid / Mask（画布交互）

### 基础交互 / 撤销 / 双击

| 字段 key | 显示文本 | 面板位置 | 类型/取值 | 状态 | 说明 |
|---|---|---|---|:---:|---|
| `canvas.epsilon` | Selection Epsilon | Canvas/Interaction/Basic Interaction | float[0.1,100.0] | 📦上游 | 顶点/边选取像素阈值 |
| `canvas.double_click` | Double Click | Canvas/Interaction/Basic Interaction | enum: None / `close` | 📦上游 | 双击行为（close 完成当前 shape） |
| `canvas.double_click_edit_label` | Double Click Edit Label | Canvas/Interaction/Basic Interaction | bool | 📦上游 | 编辑模式双击 shape 打开标签编辑 |
| `canvas.num_backups` | Undo Backups | Canvas/Interaction/Undo/Backup | int[0,200] | 📦上游 | 撤销历史快照数 |

### 滚轮编辑矩形（上游）

| 字段 key | 显示文本 | 面板位置 | 类型/取值 | 状态 | 说明 |
|---|---|---|---|:---:|---|
| `canvas.wheel_rectangle_editing.enable` | Enable Wheel Rectangle Editing | Canvas/Wheel Editing/Basic | bool | 📦上游 | 编辑时滚轮调整矩形几何 |
| `canvas.wheel_rectangle_editing.adjust_step` | Adjust Step | Canvas/Wheel Editing/Step | float[0.1,20.0] | 📦上游 | 滚轮调整步长 |
| `canvas.wheel_rectangle_editing.scale_step` | Scale Step | Canvas/Wheel Editing/Step | float[0.01,1.0] | 📦上游 | 滚轮缩放比例 |

### 精修拖拽（fork 新增，重点）

| 字段 key | 显示文本 | 面板位置 | 类型/取值 | 状态 | 说明 |
|---|---|---|---|:---:|---|
| `canvas_precision_mode` | Precision Drag Mode | Canvas/Interaction/Refinement | enum: `fixed` / `zoom` | ✅新增 | 精修拖拽降速模式（提交 `440759de`） |
| `canvas_precision_max_factor` | Precision Drag Max Factor | Canvas/Interaction/Refinement | float[1.0,20.0] | ✅新增 | zoom 模式降速上限，避免早期过僵（提交 `440759de`） |
| `canvas_precision_factor` | Precision Drag Factor | Canvas/Interaction/Refinement | int[1,20] | ✅新增 | fixed 模式下降速倍数（提交 `440759de`） |

> 配套菜单项：`toggle_precision_mode_lock`（精修模式锁定，View 菜单）。三个字段共同支撑模块 #09（部分退役）。

### 十字线（上游）

| 字段 key | 显示文本 | 面板位置 | 类型/取值 | 状态 | 说明 |
|---|---|---|---|:---:|---|
| `canvas.crosshair.show` | Show Crosshair | Canvas/Interaction/Crosshair | bool | 📦上游 | 显示十字线辅助线 |
| `canvas.crosshair.width` | Crosshair Width | Canvas/Interaction/Crosshair | float[1.0,10.0] | 📦上游 | 十字线线宽 |
| `canvas.crosshair.color` | Crosshair Color | Canvas/Interaction/Crosshair | str | 📦上游 | 十字线颜色 |
| `canvas.crosshair.opacity` | Crosshair Opacity | Canvas/Interaction/Crosshair | float[0.0,1.0] | 📦上游 | 十字线不透明度 |

> 这 4 个字段在 schema.py 中是上游原版；但 fork 在提交 `966e1456` 删了独立的 `crosshair_settings_dialog.py`，把十字线设置**统一并入了**这个设置面板（菜单项 `set_cross_line` 现打开此面板）——这是 UX 集成层的 🔧增强，字段本身未改。

### 属性叠加 / 旋转 / 画笔 / 立方体 / 掩膜（上游）

| 字段 key | 显示文本 | 面板位置 | 类型/取值 | 状态 | 说明 |
|---|---|---|---|:---:|---|
| `canvas.attributes.background_color` | Background Color | Canvas/Attributes Overlay/Color | color(4) | 📦上游 | 属性叠加背景色 |
| `canvas.attributes.border_color` | Border Color | Canvas/Attributes Overlay/Color | color(4) | 📦上游 | 属性叠加边框色 |
| `canvas.attributes.text_color` | Text Color | Canvas/Attributes Overlay/Color | color(4) | 📦上游 | 属性叠加文本色 |
| `canvas.rotation.large_increment` | Large Increment | Canvas/Rotation/Increment | float[0.01,45.0] | 📦上游 | 旋转大步长 |
| `canvas.rotation.small_increment` | Small Increment | Canvas/Rotation/Increment | float[0.01,10.0] | 📦上游 | 旋转小步长 |
| `canvas.brush.point_distance` | Brush Point Distance | Canvas/Interaction/Brush | float[1.0,200.0] | 📦上游 | 画笔采样点间距 |
| `canvas.cuboid.default_depth_vector` | Default Depth Vector | Canvas/Cuboid/Geometry | vector2 | 📦上游 | 立方体默认深度方向 |
| `canvas.cuboid.min_depth` | Min Depth | Canvas/Cuboid/Geometry | float[1.0,200.0] | 📦上游 | 立方体最小深度 |
| `canvas.mask.opacity` | Mask Opacity | Canvas/Mask/Rendering | int[0,255] | 📦上游 | 掩膜渲染不透明度 |

---

## 五、Shortcuts（快捷键，动态生成）

由 `_shortcut_fields()` 从 `xanylabeling_config.yaml` 的 `shortcuts:` 块动态生成，按 5 个分类（AI / Dialog / File / Shape / View）展示。共 83 个快捷键字段。

### fork 新增的 8 个快捷键（重点）

| 字段 key | 显示文本 | 分类 | 默认值 | 状态 | 说明 |
|---|---|---|---|:---:|---|
| `shortcuts.edit_digit_relabel` | Edit Digit Relabel | Dialog | `Alt+R` | ✅新增 | 打开数字键改名管理器（提交 `0d3d5f9e`） |
| `shortcuts.switch_digit_page` | Switch Digit Page | Dialog | `F1` | ✅新增 | 数字键分页切换（提交 `eaa6f402`） |
| `shortcuts.enter_keypoint_fill_mode` | Enter Keypoint Fill Mode | Dialog | `K` | ✅新增 | 进入关键点填充模式（提交 `4f3bafcf`） |
| `shortcuts.toggle_keypoint_tool_window` | Toggle Keypoint Tool Window | Dialog | `Ctrl+K` | ✅新增 | 切换关键点工具窗口（提交 `4f3bafcf`） |
| `shortcuts.switch_to_prev_person` | Switch To Prev Person | Dialog | `Ctrl+Shift+[` | ✅新增 | 关键点工具切换上一人（提交 `6cdf1069`） |
| `shortcuts.switch_to_next_person` | Switch To Next Person | Dialog | `Ctrl+Shift+]` | ✅新增 | 关键点工具切换下一人（提交 `6cdf1069`） |
| `shortcuts.toggle_rect_edge_align` | Toggle Rect Edge Align | View | （配置） | ✅新增 | 矩形单边调整开关（提交 `30d522c3`） |
| `shortcuts.toggle_precision_mode_lock` | Toggle Precision Mode Lock | View | （配置） | ✅新增 | 精修模式锁定开关（提交 `440759de`） |

### 上游 75 个快捷键字段（一览，不逐条展开）

| 分类 | 数量 | 代表 key | 状态 |
|---|:---:|---|:---:|
| AI | 7 | `auto_run` / `auto_label` / `auto_labeling_run` / `auto_labeling_clear` / `auto_labeling_finish_object` / `auto_labeling_add_point` / `auto_labeling_remove_point` | 📦上游 |
| Dialog | 5 | `open_settings` / `open_chatbot` / `open_vqa` / `show_overview` / `show_navigator` / `edit_digit_shortcut` / `edit_group_id` / `edit_labels` / `edit_shapes` | 📦上游（除新增 6 项外） |
| File | 15 | `close` / `open` / `open_dir` / `open_video` / `save` / `save_as` / `save_to` / `delete_file` / `delete_image_file` / `open_next` / `open_prev` / `open_next_unchecked` / `open_prev_unchecked` / `toggle_annotation_checked` / `quit` | 📦上游 |
| Shape | 28 | `create_polygon` / `create_rectangle` / `create_rotation` / `create_cuboid` / `create_quadrilateral` / `create_circle` / `create_line` / `create_point` / `create_linestrip` / `create_brush_polygon` / `edit_polygon` / `delete_polygon` / `duplicate_polygon` / `copy_polygon` / `paste_polygon` / `undo` / `undo_last_point` / `add_point_to_edge` / `edit_label` / `group_selected_shapes` / `ungroup_selected_shapes` / `loop_thru_labels` / `loop_select_labels` / `hide_selected_polygons` / `show_hidden_polygons` / `remove_selected_point` / `union_selected_shapes` | 📦上游 |
| View | 18 | `fit_width` / `fit_window` / `zoom_in` / `zoom_out` / `zoom_to_original` / `show_attributes` / `show_labels` / `label_display_mode` / `show_linking` / `show_masks` / `show_texts` / `toggle_auto_use_last_gid` / `toggle_auto_use_last_label` / `toggle_compare_view` / `toggle_keep_prev_mode` / `toggle_visibility_shapes` | 📦上游（除新增 2 项外） |

---

## 六、隐藏配置项（仅 yaml 生效，未暴露到设置面板 UI）

这些是 fork 新增的配置键，存在于 `xanylabeling_config.yaml` 但**没有对应 `SettingField`**（不进设置面板），只能改 yaml 或通过菜单 action 间接控制。这是最易遗漏的一类——`digit_shortcut_mode` 之所以被忽视，正因为它"夹在 UI 和纯配置之间"。下表把这类项系统列出，彻底补齐。

| 配置 key | 默认值 | 控制的菜单 action | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `keep_prev_viewport` | false | `keep_prev_viewport` | #15 | ✅新增 | 切图保持缩放+视图中心（视口持久化） |
| `auto_activate_keypoint_fill` | false | （自动钩子） | — | ✅新增 | 进入 person 自动启用关键点填充 |
| `show_rectangle_pixels` | false | `show_rectangle_pixels` | — | ✅新增 | 显示矩形像素尺寸（小目标 person 精修配套） |
| `show_rectangle_size_violations` | false | `show_rectangle_size_violations` | — | ✅新增 | 显示矩形尺寸规则告警开关 |
| `rectangle_size_rules` | `[person, min 36×36]` | `configure_rectangle_size_rules` | — | ✅新增 | 矩形尺寸规则列表（label/min_width_px/min_height_px/trigger_mode/enabled） |
| `label_display_mode` | label | （View 快捷键 `label_display_mode`） | — | ✅新增 | 标签显示模式 |
| `label_on_selection` | false | `label_on_selection` | — | ✅新增 | 选中对象上显示标签 |
| `label_zoom_threshold` | （数值） | （自动） | — | ✅新增 | 标签显示的缩放阈值 |
| `rename_shortcuts` | `{}` | `digit_relabel_manager` | #14 | ✅新增 | 数字键改名绑定映射（独立于 draw 的 `digit_shortcuts`） |
| `digit_shortcut_pages` | 1 | `switch_digit_page` | #13 | ✅新增 | 数字键分页页数（默认 1 = 单页兼容模式） |
| `pose_view` | （21 子键，见下） | `pose_view` | #05 | ✅新增 | Pose View 渲染配置块（见下表） |
| `rect_refine` | （3 角色） | `toggle_rect_refine_mode` | — | ✅新增 | 三框聚焦的标签角色映射（body/head/face） |

### `pose_view` 块子键（模块 #05，全部 ✅新增）

| 子 key | 默认值 | 说明 |
|---|---|---|
| `enabled` | false | Pose View 开关 |
| `layout_mode` | `anti` | 布局：`direct`/`anti`/`column`/`category` |
| `color_mode` | `bodypart` | 配色：`bodypart`（按部位）/ `person`（按目标） |
| `font_size` | 11 | 标签字号 |
| `opacity` | 0.85 | 不透明度 |
| `leader_length` | 40 | 引线长度 |
| `border_width` | 1.5 | 边框宽度 |
| `column_gap` | 8 | 分栏间距 |
| `pose_click_to_focus` | true | 点击 person 聚焦其 group（filter-driven） |
| `show_skeleton` | true | 显示骨架 |
| `show_midline` | false | 显示中线 |
| `show_bbox` | true | 显示 person 框 |
| `occlusion_highlight` | true | 遮挡高亮 |
| `show_leader` | true | 显示引线 |
| `font_shadow` | true | 字体阴影 |
| `font_color_mode` | `white` | 字色模式：`white`/`black`/`auto`/`yellow`/`custom` |
| `custom_font_color` | `#00ff88` | 自定义字色 |
| `body_colors` | head/la/ra/ll/rl 5 色 | 身体部位分色 |
| `person_colors` | 3 色列表 | 按目标分色 |
| `color_preset` | `default` | 预设：`default`/`pastel`/`neon`/`earth` |

### `rect_refine` 块（✅新增）

```yaml
rect_refine:
  label_roles:
    body: [person, halfperson]
    head: [head]
    face: [face]
```

定义"三框聚焦模式"中 body/head/face 三类角色对应的标签集合。

---

## 七、统计汇总

### 按来源统计

| 类别 | ✅新增 | 📦上游 | 合计 |
|---|:---:|:---:|:---:|
| 静态 SettingField（schema.py） | 5 | 46 | 51 |
| 动态快捷键字段（shortcuts 块） | 8 | 75 | 83 |
| 隐藏配置项（仅 yaml） | 12 | 0 | 12 |
| **合计** | **25** | **121** | **146** |

### 个人扩展改动密度

| 区域 | 改动密度 | 说明 |
|---|---:|---|
| 隐藏配置项 | 12/12 = 100% | fork 新增的配置键全部为个人扩展 |
| 动态快捷键 | 8/83 ≈ 10% | 关键点工具（6 项）+ 矩形边/精修（2 项） |
| 静态字段 | 5/51 ≈ 10% | person 实例化、数字键模式、精修拖拽（3 项） |

### fork 新增配置项全清单（25 项，去重汇总）

**设置面板 UI 暴露的（13 项）**：
- `auto_person_instance`、`digit_shortcut_mode`、`canvas_precision_mode`、`canvas_precision_max_factor`、`canvas_precision_factor`
- `shortcuts.edit_digit_relabel`、`shortcuts.switch_digit_page`、`shortcuts.enter_keypoint_fill_mode`、`shortcuts.toggle_keypoint_tool_window`、`shortcuts.switch_to_prev_person`、`shortcuts.switch_to_next_person`、`shortcuts.toggle_rect_edge_align`、`shortcuts.toggle_precision_mode_lock`

**仅 yaml 生效的（12 项）**：
- `keep_prev_viewport`、`auto_activate_keypoint_fill`、`show_rectangle_pixels`、`show_rectangle_size_violations`、`rectangle_size_rules`、`label_display_mode`、`label_on_selection`、`label_zoom_threshold`、`rename_shortcuts`、`digit_shortcut_pages`、`pose_view`（21 子键）、`rect_refine`

---

## 附：如何使用本表

1. **找配置**：知道功能名 → 在"显示文本"或"说明"列搜索 → 看"字段 key"和"面板位置"。
2. **定位代码**：
   - 设置面板 UI：`schema.py` 搜 `"<key>"` 找 `SettingField` 定义；`runtime_applier.py` 找运行时应用逻辑。
   - 配置默认值：`xanylabeling_config.yaml` 搜 `<key>:`。
   - 配置读取点：在 `label_widget.py` / `canvas.py` 搜 `self._config.get("<key>")` 或 `self._config["<key>"]`。
3. **改配置**：
   - 设置面板暴露的 → 在 Tools/设置对话框（`open_settings` 快捷键）里改。
   - 仅 yaml 的 → 直接编辑 `xanylabeling_config.yaml`，重启生效。
4. **验证来源**：
   - 静态字段：`git show b99efef6:anylabeling/views/labeling/settings/schema.py | grep "<key>"` 有输出 = 上游，无 = fork 新增。
   - 配置键：`git show b99efef6:anylabeling/configs/xanylabeling_config.yaml | grep "<key>:"` 同理。
5. **查菜单关联**：本表的"控制的菜单 action"列指向 [`菜单功能对照表.md`](菜单功能对照表.md) 对应项；个人扩展完整说明见 [`个人扩展功能提炼.md`](个人扩展功能提炼.md)。
