# X-AnyLabeling 3.3.8 Canvas 拆分总结

本文总结 `D:\xinlujing-AnyLabeling-3.3.8` 中 Canvas 拆分的核心思想，用作 `xinjiegou-X-AnyLabeling-4.0.0-beta.4` 后续拆分的技术依据。

## 资料来源

- `D:\xinlujing-AnyLabeling-3.3.8\docs\migration\05_reports_5`
  - 自定义功能迁移报告，包含 QGraphicsView 目标架构、迁移策略、功能分层。
- `D:\xinlujing-AnyLabeling-3.3.8\docs\migration\05_reports_4`
  - Canvas 功能补齐报告，包含新旧架构差异、属性映射、未实现功能。
- `D:\xinlujing-AnyLabeling-3.3.8\docs\migration\05_reports_3`
  - Canvas 功能迁移检测报告，包含鼠标、键盘、绘制、选择等验证项。
- `D:\xinlujing-AnyLabeling-3.3.8\anylabeling\views\labeling\canvas_adapter.py`
  - 旧 Canvas API 到新 QGraphicsView 架构的兼容适配层。
- `D:\xinlujing-AnyLabeling-3.3.8\anylabeling\views\labeling\graphics\`
  - 实际拆分后的 QGraphicsView、Scene、Item、Manager、Handler、State 模块。

## 一句话结论

3.3.8 的 Canvas 拆分不是简单把 `canvas.py` 切成多个文件，而是把旧 QWidget Canvas 中混合在一起的绘制、事件、状态、命中检测、选择编辑、显示控制拆成 QGraphicsView 架构，同时保留 `canvas_adapter.py` 作为旧 API 兼容层，降低对 `label_widget.py` 和外部调用方的冲击。

## 拆分前的问题

旧 Canvas 是典型的巨型 QWidget：

- `paintEvent()` 负责背景图、shape、标签、顶点、十字线、比较视图、加载状态等多层绘制。
- `mousePressEvent()`、`mouseMoveEvent()`、`mouseReleaseEvent()` 同时处理创建、选择、拖拽、复制、右键菜单、悬停高亮。
- `keyPressEvent()` 混合处理撤销点、方向键移动、旋转、Esc 取消、Alt 吸附等逻辑。
- 状态变量直接散落在 Canvas 上，例如 `shapes`、`selected_shapes`、`h_hape`、`h_vertex`、`pixmap`、`scale`、`current`、`line`。
- 新功能往往只能继续往 `canvas.py` 里塞，导致回归风险高、测试困难、局部修改影响全局。

## 核心拆分思想

### 1. 先保外部接口，再换内部实现

3.3.8 没有要求 `label_widget.py` 立即理解新架构，而是新增 `canvas_adapter.py`：

- 对外仍暴露旧 `Canvas` 类名。
- 保留旧 Canvas 的属性和信号。
- 内部持有 `CanvasGraphicsView`、`CanvasScene`、各种 Manager。
- 外部调用 `self.canvas.shapes`、`self.canvas.selected_shapes`、`self.canvas.set_editing()` 等旧接口时，由适配层转发到新架构。

这使迁移可以分阶段进行，不需要一次性重写 `label_widget.py`、菜单、快捷键和外部业务逻辑。

### 2. 用 QGraphicsView/Scene/Item 替代单一 paintEvent

旧架构：

```text
Canvas(QWidget)
  paintEvent() 统一画所有内容
  mouse/key event 统一处理所有交互
```

新架构：

```text
Canvas Adapter(QWidget)
  CanvasGraphicsView(QGraphicsView)
    CanvasScene(QGraphicsScene)
      BackgroundItem
      ShapeItem / PolygonItem / RectangleItem / RotationItem / PointItem
      ControlPointItem
      LabelItem
      CrossLineItem
      CompareItem
      LoadingItem
      SplitLineItem
      KieLinkItem
      AttributeItem
```

拆分后的收益：

- 每个显示层独立 `paint()`，不再堆在一个 `paintEvent()`。
- Z 序由 `QGraphicsItem.setZValue()` 和 constants 管理。
- 命中检测从手写遍历变为 `scene.itemAt()`、`items()`、Item 的 `shape()`。
- 缩放从手写 `self.scale + adjustSize()` 变为 `QGraphicsView` transform。
- 坐标转换从 `transform_pos(event.pos())` 变为 `mapToScene(event.pos())`。

### 3. 按职责拆成 View、Scene、Item、Manager、Handler、State

3.3.8 的实际模块边界如下。

| 层级 | 代表文件 | 主要职责 |
| --- | --- | --- |
| 兼容层 | `canvas_adapter.py` | 保持旧 Canvas API、信号、属性；对接 `label_widget.py` |
| 视图层 | `graphics/canvas_graphics_view.py` | 鼠标、键盘、滚轮、右键菜单、缩放、坐标转换 |
| 场景层 | `graphics/canvas_scene.py` | 管理背景图、场景范围、公共 overlay、Item 容器 |
| 配置层 | `graphics/canvas_config.py` | 管理 Canvas 配置，替代散落的 kwargs 和属性 |
| 状态层 | `graphics/state/canvas_state.py` | `CanvasMode`、`CreateMode`、编辑/绘制状态 |
| 绘制层 | `graphics/items/*.py` | 每种 shape 或 overlay 独立绘制和命中区域 |
| 管理层 | `graphics/managers/*.py` | shape 同步、可见性、光标、高亮、编辑控制 |
| 输入处理层 | `graphics/handlers/input_handler.py` | 创建 shape、绘制流程、部分键盘流程 |
| 自动标注交互层 | `graphics/handlers/auto_labeling_handler.py` | 自动标注相关点选、预览、结果交互 |

### 4. 数据模型仍以 Shape 为核心

拆分后并没有推翻原有 `Shape` 数据模型，而是让 `QGraphicsItem` 成为 `Shape` 的显示和交互外壳。

推荐理解方式：

```text
Shape 数据对象
  是保存、导出、业务处理的真实数据

ShapeItem / RectangleItem / PolygonItem
  是屏幕上的渲染和交互代理

ShapeManager
  负责 Shape 与 Item 之间的同步
```

这个选择很重要：如果后续 beta.4 拆分时直接把数据塞进 Item，会导致保存、撤销、label list、自动标注结果合并都变复杂。应继续让 `Shape` 保持主数据地位。

## 3.3.8 模块职责细化

### canvas_adapter.py

定位：旧接口保护层。

主要职责：

- 继承 `QWidget`，对外仍叫 `Canvas`。
- 内部创建 `CanvasGraphicsView`。
- 保留旧信号，例如 `zoom_request`、`scroll_request`、`new_shape`、`selection_changed`、`shape_moved`。
- 保留旧属性，例如 `shapes`、`selected_shapes`、`h_hape`、`h_vertex`、`h_edge`、`pixmap`、`scale`。
- 封装旧方法，例如 `load_pixmap()`、`load_shapes()`、`set_editing()`、`delete_selected()`、`select_shape_point()`、`bounded_move_shapes()`、`bounded_rotate_shapes()`。
- 在必要处用 `__setattr__` 或 property 做状态同步。

对 beta.4 的启示：第一阶段应优先做 PyQt6 版 adapter，而不是直接修改 `label_widget.py` 的所有调用点。

### canvas_graphics_view.py

定位：替代旧 Canvas 的事件入口和 viewport 控制。

主要职责：

- 接收鼠标、键盘、滚轮、双击事件。
- 使用 `mapToScene()` 做坐标转换。
- 将创建流程交给 `InputHandler`。
- 将 shape 查找、选择、移动交给 `ShapeManager` / `ShapeEditController`。
- 处理右键菜单、拖拽复制、方向键移动、旋转快捷键、滚轮缩放信号。
- 维护 QGraphicsView 的抗锯齿、背景、viewport update mode、transform anchor。

注意点：

- 旧 Canvas 的 `scale` 不是简单字段，新架构里实际来源是 view transform。
- 旧的 `adjustSize()` 语义需要适配，否则 `label_widget.paint_canvas()` 之类逻辑容易失效。
- 右键菜单仍要兼容旧 `menus[0]`、`menus[1]` 约定。

### canvas_scene.py

定位：场景容器和公共显示层管理。

主要职责：

- 管理背景图 `BackgroundItem`。
- 设置 scene rect，使图片坐标与 scene 坐标保持一致。
- 管理十字线、比较图、加载状态、分割线等非 shape overlay。
- 提供 `set_pixmap()`、`get_pixmap()`、`get_image_rect()` 等基础接口。

对 beta.4 的启示：背景图、overlay 和 shape 不应继续混在一个绘制函数里，Scene 是天然的分层容器。

### graphics/items/

定位：从旧 `paintEvent()` 中拆出来的可视元素。

典型职责：

- `ShapeItem`：通用 shape 绘制、边框、填充、顶点、标签、悬停和选择状态。
- `PolygonItem`、`RectangleItem`、`RotationItem`、`CircleItem`、`LineItem`、`PointItem`：各 shape 类型的路径、边界、命中检测。
- `ControlPointItem`：顶点控制点，支持拖拽和固定屏幕大小显示。
- `LabelItem`、`DescriptionItem`、`AttributeItem`：文字显示层。
- `KieLinkItem`：KIE 关联线。
- `CrossLineItem`：鼠标十字线。
- `CompareItem`、`SplitLineItem`：比较视图。
- `LoadingItem`：加载动画。

关键原则：

- 每个 Item 只负责自己的绘制和局部交互。
- Item 的 `boundingRect()`、`shape()` 要准确，否则命中检测和刷新区域会出问题。
- 需要屏幕固定尺寸的元素应使用 `ItemIgnoresTransformations` 或按 view scale 反向计算。

### graphics/managers/

定位：把原来 Canvas 内的操作逻辑拆成可测试、可替换的控制器。

主要模块：

- `ShapeManager`
  - Shape 与 ShapeItem 的创建、删除、刷新、查找、同步。
  - 提供 `get_shape_at()`、`get_all_shapes_at()` 等命中检测接口。
- `VisibilityController`
  - 控制 label、group、selected-only、hidden shape 等可见性。
- `HighlightManager`
  - 管理悬停形状、悬停顶点、悬停边。
- `CursorManager`
  - 管理绘制、拖拽、选择、顶点编辑等光标状态。
- `ShapeEditController`
  - 管理移动、顶点拖拽、边编辑等编辑流程。
- `FocusModeController`
  - 管理聚焦显示、背景隐藏等状态。

对 beta.4 的启示：不要把“选择/显示/高亮/编辑/光标”继续留在一个类里。它们共享 Shape，但职责不同。

### graphics/handlers/

定位：输入流程编排。

`InputHandler` 主要承担：

- 根据当前创建模式处理鼠标按下、移动、释放。
- 创建 polygon、rectangle、rotation、circle、line、point、linestrip。
- 维护正在绘制的临时 shape 和 rubber band。
- 处理 Esc、Backspace、Return、Alt 等绘制相关快捷键。

对 beta.4 的启示：事件入口可以仍在 View，但复杂流程要下沉到 handler，避免 `mousePressEvent()` 再次膨胀。

## 新旧架构关键差异

| 方面 | 旧 QWidget Canvas | 3.3.8 QGraphicsView 架构 |
| --- | --- | --- |
| 绘制 | 单个 `paintEvent()` 绘制全部层 | 多个 `QGraphicsItem.paint()` |
| 图层 | 代码顺序控制 | Item ZValue 控制 |
| 坐标 | `transform_pos(event.pos())` | `mapToScene(event.pos())` |
| 缩放 | `self.scale` + `adjustSize()` | `QGraphicsView` transform |
| 命中检测 | 遍历 `self.shapes` 手写判断 | `scene.itemAt()` + Item `shape()` |
| 选择状态 | Canvas 直接维护 | Adapter/Manager/Item 同步 |
| 顶点控制 | Canvas 绘制和判断 | `ControlPointItem` |
| 标签显示 | Canvas 内绘制 | `ShapeItem` / `LabelItem` |
| 扩展功能 | 继续改 `canvas.py` | 新增 Item、Manager、Handler |

## 关键属性映射

| 旧 Canvas 属性 | 新架构位置 | 说明 |
| --- | --- | --- |
| `self.shapes` | `canvas_adapter.shapes` + `ShapeManager` | Shape 数据列表仍是主数据 |
| `self.selected_shapes` | `canvas_adapter.selected_shapes` + Item selection | 需要双向同步 |
| `self.h_hape` | `canvas_adapter.h_hape` + `HighlightManager` | 旧变量名保留兼容 |
| `self.h_vertex` | `HighlightManager` / `ShapeItem` | 悬停顶点 |
| `self.h_edge` | `HighlightManager` / `ShapeItem` | 悬停边 |
| `self.pixmap` | `CanvasScene.BackgroundItem` | 背景图 |
| `self.scale` | `CanvasGraphicsView.transform().m11()` | 适配层应提供旧字段语义 |
| `self.current` | `InputHandler` | 正在绘制的 shape |
| `self.line` | `RubberBandItem` / `InputHandler` | 临时线段或橡皮筋 |
| `self.menus` | `canvas_adapter.menus`，View 调用 | 右键菜单兼容 |

## 功能迁移分类

3.3.8 文档中将功能分为三类，这个分类对 beta.4 拆分很有价值。

### 直接迁移类

这类主要在 `label_widget.py` 或独立 manager 中，和 Canvas 架构耦合较低。

例如：

- 数字快捷键分页。
- 数字快捷键改名。
- 多标签循环输入。
- 反选。
- FilterController。
- 关键点补全的非 Canvas 部分。
- 数据管理中心。
- 分组标签列表视图。

处理方式：保留现有 PyQt6 实现，后续只调整与 Canvas 的少量接口。

### 需要适配类

这类依赖坐标、缩放、视口或信号，需要从 QWidget 坐标系适配到 QGraphicsView 坐标系。

例如：

- Ctrl+双击定点放大。
- 视窗状态保持。
- 滚轮缩放。
- 鼠标居中缩放。
- 右键菜单。

处理方式：统一通过 View/Adapter 提供视口 API，不让业务层直接读写内部滚动条和 scale。

### 需要重写类

这类直接修改了旧 `canvas.py` 的绘制或事件流程，需要放到新架构对应模块中重写。

例如：

- 循环选择重叠 shapes。
- 点拟合矩形。
- 跨页粘贴预览。
- 智能标签显示。
- show selected label only。
- shape/selected_shapes 状态同步。
- 属性显示。
- KIE linking。
- 鼠标坐标显示。

处理方式：

- 绘制类功能进 Item。
- 选择/命中类功能进 ShapeManager 或 ShapeEditController。
- 显示过滤类功能进 VisibilityController。
- 输入流程类功能进 InputHandler 或 CanvasGraphicsView。

## 对 beta.4 的推荐迁移路线

beta.4 当前仍是 PyQt6 QWidget Canvas，核心文件为：

`D:\xinjiegou-X-AnyLabeling-4.0.0-beta.4\anylabeling\views\labeling\widgets\canvas.py`

该文件仍包含 `mouseMoveEvent()`、`mousePressEvent()`、`mouseReleaseEvent()`、`paintEvent()`、`keyPressEvent()` 等核心逻辑，说明 beta.4 尚未完成 3.3.8 这种 QGraphicsView 拆分。

推荐路线如下。

### 阶段 0：冻结外部接口

目标：确定 `label_widget.py`、菜单、自动标注、快捷键、导航器到底依赖 Canvas 哪些属性和方法。

产物：

- Canvas 外部 API 清单。
- 信号清单。
- 属性读写清单。
- 高风险调用点清单。

原则：不要一开始就改 `label_widget.py` 的大量调用点。

### 阶段 1：建立 PyQt6 版兼容适配层

目标：新增 `canvas_adapter.py`，让外部继续使用 `Canvas`。

建议路径：

```text
anylabeling/views/labeling/widgets/canvas.py          # 旧 QWidget Canvas 暂保留
anylabeling/views/labeling/canvas_adapter.py          # 新增，兼容旧 API
anylabeling/views/labeling/graphics/                  # 新增 QGraphicsView 架构
```

可以先让 adapter 只覆盖最小功能：

- 加载图片。
- 加载 shapes。
- 显示 shapes。
- 基础选择。
- 缩放和滚动信号。
- 基础编辑模式切换。

### 阶段 2：迁移基础渲染

目标：把 `paintEvent()` 的主要图层拆为 Item。

优先顺序：

1. 背景图 `BackgroundItem`。
2. 基础 `ShapeItem`。
3. `PolygonItem`、`RectangleItem`、`RotationItem`、`PointItem`。
4. 顶点和边高亮。
5. 标签显示。
6. 十字线。

这个阶段应重点保证视觉结果接近旧 Canvas，不急着迁移高级功能。

### 阶段 3：迁移基础交互

目标：把创建、选择、移动、顶点编辑迁入 View/Handler/Manager。

优先顺序：

1. 坐标转换和缩放。
2. shape 命中检测。
3. 单选、多选、框选或已有选择逻辑。
4. polygon/rectangle/rotation/point 创建。
5. 顶点移动。
6. shape 移动和边界约束。
7. 撤销最后一点、Esc 取消、Return 完成。

### 阶段 4：迁移高级功能

目标：迁移和 Canvas 强耦合的自定义功能。

建议顺序：

1. selected_shapes 状态同步。
2. show selected label only / 可见性控制。
3. 循环选择重叠 shapes。
4. 跨页粘贴预览。
5. 点拟合矩形。
6. 智能标签显示。
7. KIE linking。
8. 属性显示。
9. 比较视图。
10. 加载动画。

### 阶段 5：切换入口并保留回退

目标：让 `label_widget.py` 使用新 adapter，同时保留旧 Canvas 一段时间作为回退。

建议增加配置开关：

```yaml
canvas:
  use_graphics_view: false
```

在迁移稳定前，不建议直接删除旧 `widgets/canvas.py`。

## PyQt5 到 PyQt6 注意点

3.3.8 是 PyQt5，beta.4 是 PyQt6。迁移时不能机械复制。

常见差异：

| PyQt5 | PyQt6 |
| --- | --- |
| `from PyQt5 import QtCore, QtGui, QtWidgets` | `from PyQt6 import QtCore, QtGui, QtWidgets` |
| `Qt.LeftButton` | `Qt.MouseButton.LeftButton` |
| `Qt.RightButton` | `Qt.MouseButton.RightButton` |
| `Qt.ControlModifier` | `Qt.KeyboardModifier.ControlModifier` |
| `Qt.Horizontal` | `Qt.Orientation.Horizontal` |
| `Qt.NoButton` | `Qt.MouseButton.NoButton` |
| `QWheelEvent.pos()` | 多数场景改用 `position().toPoint()` |
| `event.pos()` | PyQt6 中部分事件需要确认返回类型 |
| `sip` import | PyQt6 下通常为 `from PyQt6 import sip` 或避免直接依赖 |

迁移建议：

- 先复制结构，不要直接批量复制全部代码。
- 每个文件先完成 import 和 enum 迁移。
- 建议抽一个 `qt_compat` 工具并不一定必要，因为 beta.4 已经是纯 PyQt6；更直接的做法是完全按 PyQt6 写。

## 主要风险

### 1. selected_shapes 与 Item selection 不一致

旧业务大量依赖 `canvas.selected_shapes`。新架构中 QGraphicsItem 也有 selection 状态，两边必须同步。

建议：

- 由 `ShapeManager` 或 adapter 统一管理。
- 禁止各处随意直接改 Item selection 后不更新 `selected_shapes`。
- 删除 shape、加载新图、切换过滤器时清理失效选中项。

### 2. scale 语义不一致

旧 Canvas 的 `scale` 是普通字段，新架构的 scale 来自 view transform。

建议：

- adapter 暴露 `scale` property。
- setter 内部调用 view transform 或统一缩放方法。
- 避免业务层直接操作 `QGraphicsView.scale()` 后不通知外部。

### 3. 坐标系混淆

旧 Canvas 坐标、widget 坐标、图片坐标经常混在一起。QGraphicsView 后至少有 viewport 坐标和 scene 坐标。

建议：

- 内部 shape 数据统一使用图片/scene 坐标。
- 事件入口立即 `mapToScene()`。
- Viewport 相关逻辑单独命名，例如 `viewport_pos`、`scene_pos`。

### 4. 绘制顺序回归

旧 `paintEvent()` 的层级顺序隐含很多视觉规则。

建议：

- 用 constants 明确 ZValue。
- 基础顺序可按：背景图、比较图、shape 填充、shape 边框、关联线、标签、顶点、hover/selection、十字线、加载层。

### 5. 高级功能遗漏

3.3.8 文档列出过一些新架构未完全迁移或需验证的功能：

- 编辑模式鼠标悬停高亮。
- 形状移动边界约束。
- 鼠标离开时保存 moving shape。
- Esc 逐点撤销。
- 跨页粘贴。
- Point-Fit-Rect。
- Click-to-Refine。
- 矩形裁剪到图片边界。
- 智能标签显示。
- 属性显示。
- KIE linking。
- 鼠标坐标显示。

这些功能在 beta.4 迁移时应提前列入验收清单。

## 验证清单

### 基础显示

- 打开图片后背景显示正确。
- 缩放、拖动、适应窗口正常。
- polygon、rectangle、rotation、point、line、circle 显示正确。
- 标签、group id、description、attribute 显示符合旧逻辑。
- 隐藏背景、隐藏标签、隐藏 shapes 等显示开关正常。

### 基础交互

- 创建 polygon。
- 创建 rectangle。
- 创建 rotation。
- 创建 point。
- 创建 line / circle / linestrip。
- 编辑模式选中 shape。
- 多选 shape。
- 拖拽移动 shape。
- 拖拽顶点。
- 边上加点。
- 删除点。
- 删除 shape。
- 方向键移动。
- Z/X/C/V 旋转。
- Esc、Backspace、Return 行为与旧版一致。

### 视口相关

- Ctrl+滚轮缩放。
- 鼠标中心缩放。
- 打开下一张图片后视窗保持。
- 缩略图导航和主画布位置同步。
- `paint_canvas()` 旧调用不失效。

### 状态同步

- `canvas.shapes` 与 label list 一致。
- `canvas.selected_shapes` 与屏幕选中一致。
- 删除、复制、粘贴后无悬空选中。
- 切换图片后旧 shape/item 不残留。
- 过滤器生效时选择状态合理。

### 高级功能

- 循环选择重叠 shape。
- 只显示选中标签。
- 跨页复制粘贴及预览。
- 点拟合矩形。
- 智能标签避让。
- KIE linking。
- 属性显示。
- 比较视图。
- 加载动画。
- 自动标注点选与预览。

## 推荐落地原则

1. 适配器优先，先保护旧 API。
2. Shape 仍是主数据，Item 只是显示和交互代理。
3. View 只做事件入口和视口控制，复杂流程交给 Handler。
4. Scene 只做场景和图层容器，业务规则交给 Manager。
5. 绘制功能进 Item，选择和命中进 ShapeManager，可见性进 VisibilityController。
6. 每迁移一个功能就补一个最小验证清单，不等全部完成后再统一排查。
7. PyQt5 代码只借鉴结构和逻辑，落地时按 PyQt6 API 重写。
8. 旧 `canvas.py` 在迁移稳定前保留为回退路径。

## 对 beta.4 的最终建议

beta.4 当前已经有大量 PyQt6 功能和自定义增强，直接用 3.3.8 文件覆盖不可取。更稳妥的方式是：

1. 以 3.3.8 的目录结构作为蓝本。
2. 用 PyQt6 重写 adapter、view、scene、items、managers。
3. 保持 beta.4 当前 `Shape`、`label_widget.py`、配置、快捷键、自动标注接口不大改。
4. 先让新 Canvas 跑通基础显示和基础交互。
5. 再逐个迁移高级功能。
6. 最后通过配置开关切换新旧 Canvas，稳定后再考虑删除旧 QWidget Canvas。

这条路线的本质是：不要把 beta.4 的巨型 Canvas 一刀切掉，而是先在旁边搭出新架构，用 adapter 把旧世界接进来，再逐步把绘制和交互迁到 QGraphicsView。
