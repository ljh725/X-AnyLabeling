# 扩展代码冲突检查报告

> 生成：2026-08-28 ｜ 方法：《扩展代码冲突检查_双域四闭环方法.md》
> **v2（独立复核修订版）**：本报告已按"冲突报告六项问题独立复核"全部证据重写；原 v1 结论中被推翻/降级的条目在 §12 变更记录中完整保留，未静默删除。
> 结论口径：**在记录的基准与场景范围内**发现/未发现以下冲突；不写"代码不存在冲突"。本报告只做检查，不包含任何修复性代码改动。

---

## 1. 执行摘要

| 项 | 值 |
|---|---|
| TARGET | fork `feature/selection-optimization` @ `c6b3a806`（工作区含未提交修改，全程保留并单独标记） |
| UPSTREAM_SOURCE | `D:\X-AnyLabeling-4.0.0-beta.4` 纯源码快照（非 git 仓库） |
| UPSTREAM_BASE | 无 git 提交等于快照（fork `main` 已含早期扩展）→ 原生边界=快照树 vs fork 树哈希对比 |
| 演进参照 | 本地 `upstream/main` = `1b6750dd`（unrelated histories，仅文件级 blob 对照） |
| 边界规模 | 原生未改 1134 / 原生被改 36 / fork 新增 797 / 原生删除 5；hunk 台账 349 条（**无 feature 归因字段——已知债务**，见 §3-R1A） |
| v2 结论 | **39 findings**（v1=37 + 新增 CF-038/CF-039，CF-006 被证伪转 compatible 留档） |

**v2 结论分布**（由 reclassification.json 重算）：

| status | v1 | v2 | | severity | v1 | v2 |
|---|---|---|---|---|---|---|
| confirmed_conflict | 3 | **2** | | P1 | 2 | **1** |
| latent_conflict | 11 | **8** | | P2 | 9 | **8** |
| maintenance_risk | 5 | **11** | | P3 | 26 | **20** |
| intentional_override | 5 | 5 | | none(compatible等) | — | **10** |
| upgrade_conflict | 4 | **3** | | | | |
| compatible | 9 | **10** | | | | |

**最高风险（v2）**：
1. **CF-002（P1，两次动态复现）**：Inspector 扫描线程无上下文校验，扫描中切目录 → 旧目录报告装入面板索引（`inspector_panel.py:1031-1047`）。v2 复核以干净子进程再确认（dirA×3 进索引、dirB 缺席）。
2. **CF-001（P2，QTest e2e 复现）**：Ctrl 多选手势跨切图残留。v2 升级为真实鼠标事件复现（press→RUBBER_BAND→切图后仍 RUBBER_BAND→release 恢复）；用户可见影响部分为静态推导。
3. **CF-004（P2，latent，confidence=low）**：v1 的 P1/『高强度静态证明』与『InspectorPanel 无 parent』证据**被复核推翻**——运行时 parent 链存在（InspectorPanel→QFrame→QMainWindow），关闭竞态探针×3 未复现且最小容器未发生真实 C++ 析构（sip.isdeleted 全 False）。剩余真实缺口：子 widget closeEvent 在顶层关闭不可达 + panel cancel 无 wait。真实退出路径未测（U7）。
4. **CF-003（maintenance_risk，P3）**：v1 误标 confirmed_conflict。HEAD 独立 git-archive 树同样 5 failed（157≠134/85≠83）→ **纯测试债**；description 缺失为系统性现状（116/157 含上游原生字段）拆出 CF-039。
5. **CF-006（rejected false positive）**：undo 后 gid 复用**不可达冲突**——应用无 redo、`restore_shape` 双 pop 销毁被撤销态，探针穷举可达状态 0 冲突。原 finding 作废留档，拆出 CF-038（info 面板 gid 编辑不进 dirty/undo 会计 → 切图静默丢失，P2 latent）。
6. **CF-007（maintenance_risk，P3）**：v1 误解 R2——R2 键为 `(file, group_id, label)` 且 per-file；person/head/face 同 gid 是功能本意（真实引擎场景 A/D 零 issue）。

**四闭环 v2 状态**（closure_recheck.json）：

| 闭环 | v1 | v2 | 依据 |
|---|---|---|---|
| 边界闭环 | ✅ | ✅ **通过** | unclassified=0（机器验证，复核未推翻） |
| 候选闭环 | ✅ | ⚠️ **部分通过** | v1 候选为人工硬编码；v2 重建八类资源索引+40 机器候选（24 映射已有 finding/14 归因膨胀误报/2 待察），但 hunk 级 feature 归因债务仍在（349/349 无归因字段） |
| 判定闭环 | ✅ | ⚠️ **部分通过** | v1 缺独立 violated_invariant 字段（37/37）+6 条 E×N 缺契约标注；v2 已修复（39/39 有字段、契约 missing=0），且 6 项 verdict 被复核改变 |
| 场景闭环 | ✅ | ⚠️ **部分通过** | CF-001 e2e 升级、CF-002 再确认；但 CF-004 动态未复现（最小容器无法忠实模拟退出）、完整应用启动/关闭路径未测（U4/U7） |

任一子闭环未全绿 → 总体结论为"部分通过"，不得使用全绿勾号。

---

## 2. P0/P1 确认冲突

### CF-002 Inspector 扫描跨目录污染（P1，confirmed，C6/C3，E×E）
- **位置**：`anylabeling/views/labeling/widgets/inspector/inspector_panel.py:1031-1047`（完成回调无校验）、`:1137-1141`（set_file_list 只清 UI 不取消）、`label_widget.py:10911`（切目录不停线程）。
- **触发链**：run_scan(dirA) → set_file_list(dirB) → 线程完成 → 无条件替换 `_flat_index/_last_report`。
- **违反不变量**：面板索引内容必须与当前文件列表(目录)一致。
- **影响**：数据检查/表格展示 dirA 的问题，用户据此导出/修复即跨目录污染。
- **证据**：probe1 + v2 干净子进程 `probe_e2_child`（`index_by_file_keys`=dirA×3、dirB 缺席）。
- **方向**：目录 token 校验（仿 DatasetIndexController）或切换时 cancel+wait。
- **回归**：scan(dirA) 中途切 dirB，断言完成后面板无 dirA 记录。

（v1 的 P1×2 中，CF-004 经复核降为 latent P2/low，见 §3。）

---

## 3. 分类清单（v2，39 条；全文见 conflict_findings_v2.jsonl）

### 3.1 production conflict（confirmed）
| ID | Sev | 不变量 | 位置 | 复核注记 |
|---|---|---|---|---|
| CF-002 | P1 | 索引与当前目录一致 | inspector_panel.py:1031-1047 | 两次动态复现 |
| CF-001 | P2 | press/release 配对完整；手势寿命≤单次周期 | canvas.py:6751/2314/2232 | QTest e2e 复现泄漏；可见影响=静态推导 |

### 3.2 latent risk（latent_conflict）
| ID | Sev | conf | 不变量 | 摘要 |
|---|---|---|---|---|
| CF-004 | P2 | low | 线程寿命≤宿主；退出前 cancel+wait | closeEvent 仅投递顶层窗口→panel cancel 钩子退出不可达；动态未复现（U7）；v1 证据修正 |
| CF-005 | P2 | high | 发布物属于当前会话世代 | close_queue 置 None 但线程继续；晚到 draft 发布进已关闭会话 |
| CF-010 | P2 | high | worker 不重复并行；回调目标存活 | 行为遥测导出 worker 三缺口 |
| CF-038 | P2 | high | 用户编辑必须进 dirty/undo 会计 | **新增**：info 面板 gid 直写（7381-7397）无 store_shapes/set_dirty → 切图静默丢失+撤销粒度跳跃 |
| CF-008 | P3 | med | 全局标签开关覆盖全部绘制路径 | pose overlay 硬编码 show_labels=True |
| CF-011 | P3 | high | 同数据集扫描避免重复全量 IO | SQLite 索引×复核队列无互斥 |
| CF-012 | P3 | high | 模式切换无旧意图残留 | 数字键切换不回收进行中绘制 |
| CF-033 | P3 | med | 对话框线程寿命≤对话框寿命 | 模态标签对话框线程在应用退出时随析构（NC-LABELDIALOG-MODAL） |

### 3.3 upgrade risk（upgrade_conflict，contract_type=upgrade_boundary）
| ID | Sev | 摘要 |
|---|---|---|
| CF-015 | P2 | label_widget 142 hunks×75 特性、6 处 canvas 私有访问；30/36 被改原生文件上游已演进 |
| CF-037 | P2 | canvas 97 hunks×51 特性交互契约织入 |
| CF-030 | P3 | widgets 懒导出改变导入时机/失败点延后 |

### 3.4 maintenance / test debt（maintenance_risk）
| ID | Sev | 摘要 |
|---|---|---|
| CF-013 | P2 | 双原子写路径（fsync 差异；锁不覆盖 Ctrl+S，指纹复验兜底） |
| CF-003 | P3 | **v1 confirmed 降级**：HEAD 即 5 failed 的测试债（157≠134/85≠83/顺序/description 断言），非生产冲突 |
| CF-039 | P3 | **新增**：117/158 schema 字段缺 description（系统性，上游继承为主） |
| CF-007 | P3 | **v1 latent 降级**：gid 重复不违规（R2 键=file+gid+label）；批量统一 gid 是功能本意；质检负责发现同 gid+同 label；附带 R7 跨文件聚合作用域怪癖 |
| CF-016 | P3 | 翻译重生成覆盖风险（降级：仅 .ts 合并失误才会丢，无三方证据） |
| CF-009 | P3 | label_zoom_threshold 死配置 |
| CF-022 | P3 | 三复核系统导航无仲裁（调查线索，v1 latent 降级） |
| CF-023 | P3 | Inspector Tab 固定 index |
| CF-025 | P3 | 视口恢复多器+A7 navigator 侧未完成（清单自认，v1 latent 降级） |
| CF-028 | P3 | show_labels 同名键双层级 |
| CF-036 | P3 | zoom 变换链多写者（调查线索，v1 latent 降级） |

### 3.5 intentional_override（5）
CF-014 快捷键改绑、CF-026 外观×隔离复位、CF-029 原子保存、CF-031 设置分区插入、CF-032 crosshair 深链迁移。

### 3.6 rejected false positive 与 compatible（10）
- **CF-006（rejected，P none）**：undo 后 gid 复用不可达冲突——无 redo + 双 pop 销毁被撤销态；探针穷举可达状态全部 gid 唯一（cf006_probe_results.json）；原建议会致 gid 无界增长，撤回。
- compatible：CF-017 Shape deepcopy 修复、CF-018 质检只读、CF-019 配置链保存、CF-020 paint 谓词一致、CF-021 拾取序、CF-024 identity 链、CF-027 keypoint×pose、CF-034 单字母键、CF-035 遥测追加订阅。
- 已核销误报：objectName 重复 9 处、文本级重复 connect 63 处、read_but_not_in_yaml 10 项（模型 YAML 键）。

---

## 4. E×E 交互矩阵（18 候选，v2 verdict）

| 候选 | verdict（finding） |
|---|---|
| EE-01 标签可见性 | latent（CF-008）+ maintenance（CF-009）；主干 4 开关 AND 合成一致 |
| EE-02 canvas 模式互斥 | **confirmed（CF-001）**；rect_edge/isolation/brush 复位链完整 |
| EE-03 paint 管线 | compatible（CF-020） |
| EE-04 拾取×多选 | compatible（CF-021） |
| EE-05 zoom 变换链 | maintenance（CF-036，v2 降级） |
| EE-06 双遥测 | latent（CF-010）；订阅设计本身 compatible（CF-035） |
| EE-07 复核导航 | maintenance（CF-022，v2 降级） |
| EE-08 Inspector Tab | maintenance（CF-023） |
| EE-09 数字键模式 | latent（CF-012） |
| EE-10 group_id 多写者 | latent（CF-038 拆出）；CF-006/CF-007 分别被证伪/降级 |
| EE-11 shape identity | compatible（CF-024） |
| EE-12 筛选/索引 worker | **confirmed（CF-002）**+ latent（CF-011） |
| EE-13 保存×批量 | maintenance（CF-013） |
| EE-14 视口持久化 | maintenance（CF-025，v2 降级） |
| EE-15 外观×隔离 | intentional_override（CF-026） |
| EE-16 worker×切图 | latent（CF-004/CF-005）；DatasetIndexWorker 为正面样板 |
| EE-17 keypoint×pose | compatible（CF-027） |
| EE-18 同名键 | maintenance（CF-028） |

## 5. E×N 契约矩阵（15 条 finding，契约 missing=0）

native_contract：CF-014/017/018/019/029/030/031/032/033/034/035（NC-SHORTCUT-DEFAULTS、NC-SHAPE-COPY-SEMANTICS、NC-QUALITY-READONLY-DATA、NC-CONFIG-SAVE-ROUNDTRIP、NC-LABELFILE-SAVE-LOAD、NC-WIDGETS-PACKAGE-EXPORTS、NC-SETTINGS-SECTION-ORDER、NC-CROSSHAIR-ENTRY、NC-MODEL-MANAGER-SIGNALS、NC-LABELDIALOG-MODAL）。
upgrade_boundary（升级统计类，无运行契约）：CF-015、CF-037、CF-016。
tests_only：CF-003。
契约级 violate：**0**。契约全文见 v2 复核目录 contract_coverage.json 与原 native_contracts.json（13 条）。

## 6. intentional_override 与 compatible（反证留存）

intentional_override（5）与 compatible（10）逐条反证依据见 conflict_findings_v2.jsonl 各 finding 的 false_positive_checks 与 violated_invariant 字段（v2 起为独立结构化字段）。

## 7. 上游升级热点

36 个被改原生文件中 **30 个上游也已演进**（blob 对照）。Top：label_widget.py（142 hunks×75 特性、6 处 `self.canvas._*` 私有访问）、canvas.py（97×51）、settings 三件套、yaml 快捷键、resources.py 翻译链、shape.py/label_file.py 序列化契约、已删除 crosshair_settings_dialog（上游回补引用即断）。复制实现扫描：13 个同名大函数全为 Qt 惯用名（`__init__`/`run`/`closeEvent` 等），**无实质性复制上游实现**。

## 8. 动态场景与测试（test_evidence.json + v2 复核目录）

**v2 新增动态证据**（全部 PowerShell 实跑，见 powershell_reproduction.json）：
| 命令（.ps1 块） | 结果 | 期望 |
|---|---|---|
| A `pytest tests/test_settings/test_schema.py -q --tb=line`（当前树） | rc=1，5 failed | 预期失败=复现成立 |
| B HEAD 独立树同命令 | rc=1，5 failed（157≠134/85≠83） | 预期失败=复现成立 |
| C probe2.py（QTest e2e 手势 + round-trip 机制） | rc=0；泄漏复现；机制=原生 other_data | 一致 |
| D probe_e2_child.py | rc=0；dirA 污染复现 | 一致 |
| E cf006_probe.py | rc=0；duplicate_reachable=false | 一致 |
| F cf007_scenarios.py | rc=0；A/B/D holds | 一致 |
| G rebuild_index_and_candidates.py | rc=0；八类非空+40 机器候选 | 一致 |

**v1 存量测试**（保留）：G1 42 passed；G2 67 passed+5 failed（=CF-003 债）；G3 71 passed；G4 173 passed；dialog_layout 9 passed；offscreen 一次硬崩（RC=127）单跑不可复现（U5）。

**场景矩阵** S1~S7 覆盖与 gaps（U4 完整启动、U7 真实退出路径）见 scenario_matrix.json 与 v2 closure_recheck。

## 9. unknown、扫描限制与证据缺口

| ID | 内容 | 影响/下一步 |
|---|---|---|
| U1 | git status 21 个临时目录 Permission denied | 仅临时目录；有权限会话可重扫 |
| U2 | 上游候选目录非 git 仓库 | N 基准用文件树（已满足）；无上游历史 |
| U3 | upstream/main unrelated histories | 阶段 5 仅 blob 对照，无行级三方预览 |
| U4 | 完整应用启动场景未跑 | S1 启动段为集成测试+静态覆盖 |
| U5 | offscreen 一次硬崩不可复现 | 未影响结论 |
| **U7** | **真实应用退出路径未测**（最小容器 deleteLater 因 PyQt 所有权未真正析构 C++ 对象，sip.isdeleted 全 False） | CF-004 动态缺口的根因；需真实 MainWindow 关闭探针 |
| **U8** | BehaviorAnalyticsExportWorker 未动态覆盖 | 需真实导出流程；静态矩阵见 cf004_worker_matrix.json |

## 10. 修复方向（按收益/风险）与必须新增的回归测试

1. CF-002（P1）：目录 token 校验/切换取消 → probe_e2 场景单测。
2. CF-038（P2）：info 面板 gid 回调补 store_shapes+set_dirty → 面板改 gid→切图断言 dirty。
3. CF-001（P2）：reset_state 补 `_selection_gesture.reset()` + mouseMove 手势分支 buttons 校验 → probe_a2 断言。
4. CF-005/CF-010（P2）：draft 会话 token；导出 worker busy 守卫+存活检查。
5. CF-003/CF-039（P3，低成本）：更新 test_schema 期望 + 分批补 description。
6. 长期：canvas/label_widget 扩展抽 adapter 层；hunk 台账补 feature 归因（R1A 债务）。

## 11. 产物索引与复现命令

**v1 审计**：`.tmp-conflict-audit/audit-20260828-122248/`（36 项：台账 JSON/JSONL 15、phase 脚本 9、日志 5、patch 4、子目录 2、验收记录等——数量对账见 v2 `artifact_reconciliation.json`；v1 报告中"14 个台账/脚本文件"表述不准确，以此为准；独立 boundary_map.json 当时长缺失，现于 v2 目录补齐）。

**v2 复核**：`.tmp-conflict-audit/review-six-issues-20260828-144656/`——review_meta.json、artifact_reconciliation.json、rebuilt_shared_resource_index.json、generated_candidates.jsonl（40）、manual_supplements.jsonl（32）、generation_coverage.json、closure_recheck.json、contract_coverage.json、cf003_*.log/json、cf004_ownership_graph/worker_matrix/probe_results/recheck.json、cf006_probe_results.json、cf006_recheck.json、cf007_rule_semantics（含于 scenarios）/scenarios/recheck.json、probe2_results.json、probe_e2_sub_result.json、consistency_followups.json、reclassification.json、conflict_findings_v2.jsonl（39）、boundary_map.json、powershell_commands.ps1、powershell_reproduction.json、head_tree/（只读复核用导出）。

**复现（PowerShell，在仓库根目录执行；全部命令已实际运行）**：
```powershell
$ErrorActionPreference = 'Stop'
$env:QT_QPA_PLATFORM = 'offscreen'
$env:PYTHONDONTWRITEBYTECODE = '1'
$pytestRoot = "$env:TEMP\xany-qa-rerun"
New-Item -ItemType Directory -Force -Path $pytestRoot | Out-Null
$env:PYTEST_DEBUG_TEMPROOT = $pytestRoot
& 'C:\Users\20441\.conda\envs\x-anylabeling-cu12\python.exe' -m pytest tests/test_settings/test_schema.py -q --tb=line
# 预期 5 failed，退出码 1（非零退出码即复现成功，不代表命令无效）
& 'C:\Users\20441\.conda\envs\x-anylabeling-cu12\python.exe' '.tmp-conflict-audit\review-six-issues-20260828-144656\cf006_probe.py'
# 预期 verdict=rejected_undo_reachability
& 'C:\Users\20441\.conda\envs\x-anylabeling-cu12\python.exe' '.tmp-conflict-audit\review-six-issues-20260828-144656\probe_e2_child.py'
# 预期 verdict=confirmed_conflict
```
（v1 报告中的 Git Bash 复现块作废；PowerShell 化过程及 PS5.1 stderr 陷阱说明见 powershell_command_review.json。）

## 12. v1→v2 变更记录（被推翻结论完整保留）

| 项 | v1 | v2 | 依据 |
|---|---|---|---|
| 四闭环 | 全部 ✅ | 边界✅；候选/判定/场景⚠️部分通过 | R1：hunk 349/349 无归因、states/data/registry 空、候选人工硬编码、6 条 E×N 缺契约、37/37 缺 violated_invariant |
| CF-006 | latent P2 | **rejected→compatible 留档** | R2 探针证伪（无 redo+双 pop）；拆出 CF-038 |
| CF-007 | latent P2 | maintenance_risk P3 | R3：R2 键=(file,gid,label)，真实引擎 4 场景 |
| CF-004 | latent P1(high)『无 parent』 | latent P2(low) | R4：parent 证据作废；探针×3 未复现且未真析构（U7） |
| CF-003 | confirmed P3 | maintenance_risk P3 | R5：HEAD 独立树同样失败=测试债；description 系统性缺口拆出 CF-039 |
| CF-016 | upgrade P2 | maintenance P3 | 附带：无三方上游证据 |
| CF-022/025/036 | latent P3 | maintenance P3 | 附带：无已证实序列/自认未完成 |
| CF-001 | confirmed P2 | confirmed P2（证据升级） | probe_a2 QTest e2e |
| CF-002 | confirmed P1 | confirmed P1（再确认） | 干净子进程复现 |
| probe_b note | "未知键被丢弃" | 原生 other_data 完整保留 | probe_b2 机制断言（矛盾修正） |
| 复现命令 | Git Bash（违规） | PowerShell（已实跑） | R6 |

**最终验收（v2）**：复核目录全部产物已生成且命令实跑；原审计目录与生产代码未修改；结束时 git status 仅新增 v2 复核目录与本报告更新（见 six_issue_review_report.md 验收节）。
