# 上游对比与清单更新——三层四闭环审计法

> 文档定位：用于全量识别 fork 相对可信上游的变化，并可重复、可审计地更新
> [`功能总表_上游与个人扩展.md`](功能总表_上游与个人扩展.md) 与
> [`个人修改与扩展清单.md`](个人修改与扩展清单.md)。
>
> 核心原则：机器负责穷举，人负责语义判断；任何未扫描、未归因、未验证或未对账的项目都必须显式暴露，不能被“总数大致相符”掩盖。

---

## 一、真正要证明的四件事

“更新清单且不遗漏”不是单一问题，而是四个彼此独立的闭环：

| 闭环 | 要证明什么 | 硬性收口条件 |
|---|---|---|
| 集合闭环 | 所有应参与比较的文件都被点名 | 未扫描文件、权限错误、路径冲突均为 0 |
| 变更闭环 | 每个文件变化和 diff hunk 都有归因 | 未归因 hunk 为 0；二进制、重命名、生成物均有处理结论 |
| 功能闭环 | 代码变化已归纳成真实、可达的功能变化 | 每项功能都有入口、实现、输出/持久化和验证证据 |
| 文档闭环 | 两份清单完整、一致、可追溯 | 两表个人功能 ID 集合一致，删除/退役/进行中状态同步 |

执行上仍分三层：第一层解决集合闭环，第二层解决变更与功能闭环，第三层解决证据和文档闭环。

---

## 二、先固定四个基准，禁止混用

每轮审计必须在 `audit_meta.json` 中明确记录：

| 名称 | 含义 | 用途 |
|---|---|---|
| `UPSTREAM_SOURCE` | 可信上游仓库、官方 tag 或本地原版快照 | 说明参照物来自哪里 |
| `UPSTREAM_BASE` | fork 来源判定使用的上游版本/树快照 | 判断某项内容是上游原有还是 fork 独有 |
| `DOC_BASE` | 两份清单上一次确认覆盖到的 fork commit | 只用于本轮增量更新 |
| `TARGET` | 本轮审计目标，一般为当前 `HEAD` | 已提交代码的审计终点 |
| `WORKTREE_SNAPSHOT` | 暂存区、未暂存和未跟踪文件清单及摘要 | 单独记录“进行中”，不得混入已发布功能 |

如还要比较“当前最新版上游”，再增加 `UPSTREAM_TARGET`，执行三方比较：

1. `UPSTREAM_BASE → UPSTREAM_TARGET`：上游后来新增或修改的内容；
2. `UPSTREAM_BASE → TARGET`：fork 累计变化；
3. `UPSTREAM_TARGET ↔ TARGET`：当前真实差异及可能的并行修改。

不得用 `DOC_BASE → TARGET` 的增量 diff 直接判定来源；也不得用
`UPSTREAM_BASE → TARGET` 的累计行数代表“本轮新增规模”。

### 基准最低记录项

- 仓库绝对路径、remote URL、branch、完整 commit SHA、提交时间；
- 上游目录是否为 Git 工作树、是否 dirty；
- fork 的 staged、unstaged、untracked 状态；
- 对比脚本 SHA-256、排除规则 SHA-256、执行时间和操作系统；
- 若上游只是目录快照，记录全树清单摘要而不是虚构 commit。

---

## 三、第一层：建立对称的文件全集

### 3.1 先定义“参与比较的文件”

双方必须使用同一种口径：

- 若双方都是 Git 工作树，优先枚举跟踪文件；未跟踪文件作为独立集合完整枚举；
- 若一方是发布压缩包或普通目录，双方都按相同目录规则扫描；
- ignored、临时文件和生成物可以排除，但必须进入 `exclusions.json`，记录规则、命中数量和样例；
- 不允许用“人工认为有意义”作为是否被枚举的前置条件。人工只能在机器完整点名后分类；
- `Permission denied`、坏链接、无法读取、大小写路径冲突必须进入
  `scan_errors.json`，存在任何未处理错误时不得宣称全量完成。

路径枚举使用 NUL 分隔输出，避免空格、中文、引号、换行和转义字符造成遗漏：

```powershell
$ErrorActionPreference = 'Stop'
$forkRoot = 'D:\xinjiegou-X-AnyLabeling-4.0.0-beta.4'
& git -C $forkRoot -c core.quotepath=false ls-files -z
& git -C $forkRoot -c core.quotepath=false ls-files --others --exclude-standard -z
& git -C $forkRoot -c core.quotepath=false status --porcelain=v1 -z -uall
```

扫描器应使用 `os.walk(..., onerror=...)` 或等价机制，把访问错误当成审计结果，而不是静默跳过。

### 3.2 主四桶和辅助状态

可比较的普通文件按规范化相对路径落入主四桶：

| 桶 | 含义 |
|---|---|
| `identical` | 路径相同且内容相同 |
| `modified` | 路径相同但内容不同，或文件类型/模式不同 |
| `fork_only` | 只在 fork 存在 |
| `upstream_only` | 只在上游存在 |

另建辅助表，不应强塞进四桶后丢失语义：

- `renamed_or_moved`：内容相同或高度相似但路径变化；
- `copied`：上游文件被复制后继续扩展；
- `binary_or_large`：不能按文本 hunk 分析；
- `generated`：翻译资源、编译产物等；
- `symlink_or_mode_changed`：链接目标或执行位变化；
- `case_collision`：Windows 大小写不敏感路径冲突；
- `unreadable`：无法读取或计算摘要。

内容摘要使用 SHA-256；必要时再做逐字节确认。闭环公式为：

```text
compared_union = identical + modified + fork_only + upstream_only
unscanned = unreadable + permission_error + path_collision + unexpected_error
```

只有 `unscanned = 0` 且集合交集、并集检查均通过，第一层才完成。四桶数字相加正确本身不等于扫描完整。

### 3.3 第一层固定产物

建议写入 `.tmp-doc-update/audit-<时间>/`：

```text
audit_meta.json
upstream_manifest.json
fork_manifest.json
exclusions.json
scan_errors.json
buckets.json
rename_copy_candidates.json
```

每个 manifest 至少包含：相对路径、文件种类、大小、SHA-256、来源集合、读取状态。

---

## 四、第二层：从 diff hunk 到功能语义

### 4.1 两条 diff 分开生成

1. **累计来源 diff**：`UPSTREAM_BASE → TARGET`，回答 fork 相对原版做了什么；
2. **本轮增量 diff**：`DOC_BASE → TARGET` 加 `WORKTREE_SNAPSHOT`，回答文档本轮要补什么。

生成 diff 时开启 rename/copy 检测并保留二进制状态：

```powershell
$ErrorActionPreference = 'Stop'
$forkRoot = 'D:\xinjiegou-X-AnyLabeling-4.0.0-beta.4'
$upstreamBase = '<UPSTREAM_BASE>'
$target = '<TARGET>'
& git -C $forkRoot diff --find-renames=40% --find-copies=40% --full-index --binary "$upstreamBase..$target"
& git -C $forkRoot diff --find-renames=40% --find-copies=40% --numstat "$upstreamBase..$target"
```

### 4.2 hunk 精确记账

为每个文本 hunk 生成稳定记录：

```text
hunk_id = relative_path + old_range + new_range + content_hash
```

每个 hunk 必须映射到以下之一：

- 一个或多个稳定 `feature_id`；
- `refactor`、`format_only`、`version_only`、`generated`、`docs_only`、
  `tests_only` 等明确非功能原因；
- `unresolved`，并记录阻塞原因和下一步。

行数只用于规模统计，不能替代 hunk 覆盖率。收口指标是：

```text
total_hunks = attributed_hunks + nonfunctional_hunks + unresolved_hunks
unresolved_hunks = 0
```

二进制、整文件新增和整文件删除没有普通 hunk，也必须生成等价 ledger 项。

### 4.3 功能变化分类

每个功能变化使用稳定 ID，并至少标记一种类型：

- `added`：全新能力；
- `enhanced`：扩大上游已有能力；
- `behavior_changed`：默认值、交互或输出语义变化；
- `fixed`：缺陷修复；
- `renamed_or_moved`：用户能力不变但入口或模块迁移；
- `deprecated_or_removed`：退役、删除或清理；
- `performance`、`security`、`compatibility`；
- `engineering_only`：测试、构建、CI、文档等无直接用户入口变化。

一个 hunk 可以服务多个功能，一个功能也可以跨多个文件；因此不能假设“文件行”和“功能行”是一对一关系。

### 4.4 必须机械枚举的功能面

| 功能面 | 至少检查的内容 |
|---|---|
| UI 入口 | `QAction`、action helper、`QShortcut`、菜单、工具栏、右键菜单、按钮、动态 action、信号连接、快捷键配置 |
| 配置 | YAML 默认值、schema、类型与范围、运行时应用器、持久化、旧配置迁移、删除/改名键 |
| 注册表 | 模型列表、插件/组件注册、导入导出器、规则 registry、CLI 子命令 |
| 数据契约 | JSON 字段、序列化、兼容读取、缓存、索引、迁移 |
| 工程面 | 依赖、打包、资源、i18n、构建脚本、CI、平台兼容 |
| 质量面 | 测试、校验规则、阈值、报告格式、错误处理 |

只搜索 `= action(` 或新增 YAML 行不构成完整枚举；还必须检查删除、改名、默认值和可见/启用条件变化。

### 4.5 功能可达链

每个面向用户或外部调用者的功能，至少给出以下证据链：

```text
入口/触发方式
  → 注册、导入或信号连接
  → 核心处理逻辑
  → 状态持久化、文件输出或可见结果
  → 测试、实际运行或样例输出
```

如果只有实现文件但没有入口/注册，应标为 `implemented_but_unreachable`；如果只有 UI 入口没有完成处理，应标为 `stub_or_incomplete`，不得计入“已完成能力”。

### 4.6 第二层固定产物

```text
cumulative_diff_summary.json
incremental_diff_summary.json
hunk_ledger.jsonl
surface_inventory.json
feature_ledger.json
unresolved_items.json
```

`feature_ledger.json` 是两份清单的唯一中间事实源，至少包含：

```text
feature_id、名称、功能域、变化类型、来源结论、状态、首次引入 commit、
相关文件、hunk_ids、入口、输出/持久化、验证证据、置信度、备注
```

---

## 五、第三层：证据交叉验证与文档双向对账

### 5.1 三类证据

| 证据类 | 示例 | 能证明什么 |
|---|---|---|
| 来源证据 | 官方 tag/commit、可信上游快照、发布归档 | 内容是否上游原有 |
| 结构证据 | 文件分桶、git diff、pickaxe、知识图谱、调用关系 | 变化在哪里、如何连接 |
| 行为证据 | 单测、集成测试、UI/CLI 实跑、输入输出样例 | 功能是否真实可用 |

来源结论至少需要一条可信来源证据；“已实现功能”至少需要结构证据和行为证据。git diff、pickaxe 和由同一代码生成的知识图谱不能简单算成三条完全独立证据。

### 5.2 置信度与未知项

每项功能标记：

- `confirmed`：来源、结构、行为证据完整；
- `high`：来源和结构明确，行为证据有限；
- `medium`：存在合理旁证但历史不完整；
- `unknown`：无法可靠判定。

允许保留未知项，但必须列出证据缺口；不得为了让数字好看强行归类。最终报告必须分别给出
`confirmed/high/medium/unknown` 数量。

### 5.3 两份文档的职责与双向约束

**功能总表**：描述当前产品全貌，包含上游能力、fork 增强、个人新增、退役状态和进行中能力。

**个人修改与扩展清单**：只描述 fork 相对指定上游基准的变化，包含修改文件、功能变化、删除/退役、工程变化和进行中工作区。

硬性对账：

```text
表一中状态为“个人新增/个人增强/个人修改”的 feature_id 集合
    ==
表二中状态为“已实现/进行中”的对应 feature_id 集合
```

若两边因展示边界不同不能一对一成行，也必须通过 feature ID 建立映射。另行检查：

- 表二删除/退役项是否已从表一当前能力中移除或明确标记；
- 表二进行中项是否没有被表一误写成稳定发布能力；
- 文件数量、功能数量、action 数、配置键数使用同一 ledger 统计；
- 文档中 commit、行数、文件数和当前工作区状态没有过期。

### 5.4 第三层固定产物

```text
evidence_matrix.json
document_reconciliation.json
audit_report.md
```

---

## 六、完整执行 SOP

### 步骤 0：保护现场

- 阅读仓库 `AGENTS.md`；
- 记录当前 branch、HEAD 和完整 dirty 状态；
- 不执行 reset、checkout、clean、删除、自动格式化或提交；
- 审计期间若工作区继续变化，重新生成快照或明确宣布本轮结果失效。

### 步骤 1：确定基准

- 验证本地上游目录或官方 tag 的可信性；
- 从两份旧文档读取 `DOC_BASE`，不凭记忆填写；
- 分别记录 `UPSTREAM_BASE`、`DOC_BASE`、`TARGET`、`WORKTREE_SNAPSHOT`；
- 若基准存在歧义，先列候选和证据，不能静默选择。

### 步骤 2：运行第一层

- 对称枚举双方文件；
- 生成 manifest、排除台账和扫描错误台账；
- 计算四桶与 rename/copy/binary/generated 辅助状态；
- 确认 `unscanned = 0`。

### 步骤 3：运行第二层

- 分别生成累计来源 diff 与本轮增量 diff；
- 对所有 hunk、二进制、新增和删除文件逐项归因；
- 机械枚举 UI、配置、注册表、数据契约和工程面；
- 构建功能可达链和 `feature_ledger.json`；
- 确认 `unresolved_hunks = 0`，或将确实无法解决的问题显式列入最终报告。

### 步骤 4：运行第三层

- 每个来源判定连接可信上游证据；
- 每个已完成功能连接结构证据和行为证据；
- 对知识图谱先核对快照 commit；
- 生成证据矩阵和置信度统计。

### 步骤 5：更新两份文档

- 先从 `feature_ledger.json` 更新个人清单，再同步功能总表；
- 已提交与工作区进行中严格分区；
- 同步新增、增强、行为变化、修复、删除、退役和工程项；
- 更新表头基准、验证方式和统计口径；
- 不把生成产物行数等同于手写功能代码行数。

### 步骤 6：最终验收

- 重新解析两份文档中的 feature ID，做集合差异；
- 重跑 `git status`，确认审计期间没有漏掉新变化；
- 抽查高风险功能的入口到输出链；
- 输出完整结果、未知项、限制和下一轮建议。

---

## 七、最终收口清单

- [ ] `UPSTREAM_SOURCE / UPSTREAM_BASE / DOC_BASE / TARGET / WORKTREE_SNAPSHOT` 已记录
- [ ] 双方文件枚举口径对称，排除规则有台账
- [ ] `unreadable / permission_error / path_collision / unexpected_error` 均为 0
- [ ] 四桶并集与 manifest 一致，rename/copy/binary/generated 已单独处理
- [ ] 累计来源 diff 与本轮增量 diff 未混用
- [ ] 每个 hunk、整文件新增、删除和二进制变化都有归因
- [ ] UI、配置、注册表、数据契约、工程面均已机械枚举
- [ ] 每个已完成功能都有入口、实现、输出/持久化和验证证据
- [ ] 每个 commit 已归入功能、工程、文档、生成物或明确的非功能项
- [ ] 表一个人功能 ID 与表二对应功能 ID 集合一致
- [ ] 删除、退役、改名、默认行为变化和进行中状态已同步
- [ ] 未知项、证据缺口和置信度已公开列出
- [ ] 审计产物可由记录的基准、脚本和规则重复生成

任何一项未通过时，应表述为“已完成当前口径下的审计，尚有以下未决项”，而不是“保证无遗漏”。

---

## 八、维护节奏

- 小功能落地：执行 `DOC_BASE → TARGET` 增量审计；
- 每个功能域完成：更新两份清单并刷新知识图谱；
- 上游版本切换、重大合并、每 5～10 次增量更新：执行一次
  `UPSTREAM_BASE → TARGET` 全量复核；
- 比较脚本、排除规则或文档结构变化：立即做一次全量复核；
- 临时目录中的台账可以重建，但方法脚本和 schema 应迁入 `scripts/` 或专用审计目录长期版本化。

