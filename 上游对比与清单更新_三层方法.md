# 上游对比与清单更新 — 三层方法手册

> **文档定位**:记录"如何全量找出 fork 相对上游的功能变化并无遗漏地更新清单文档"的完整方法论。
> 服务对象:[`功能总表_上游与个人扩展.md`](功能总表_上游与个人扩展.md)(合并表一)与 [`个人修改与扩展清单.md`](个人修改与扩展清单.md)(合并表二)。
> 首次完整实战:2026-08-27(基准 `77cf2832` → HEAD `c6b3a806`,15 commits)。

---

## 一、需求与问题定义

### 1.1 维护目标

两份姊妹篇文档需要随代码演进而增量更新:

| 文档 | 内容边界 |
|---|---|
| 功能总表(合并表一) | 上游原版功能 + 个人扩展,全维度 |
| 个人修改与扩展清单(合并表二) | 只含 fork 相对上游的修改与扩展 |

### 1.2 用户提出的本质问题(三次递进追问)

1. **来源判定靠什么?** ——"这个功能是我加的还是原版自带的"怎么判?
2. **git 没记录还能查吗?** ——溯源信息丢失后是否无从下手?
3. **两边代码量都很大,怎么保证不遗漏?** ——对比的完整性靠什么保证?

### 1.3 两个关键认知(方法的根基)

**认知一:来源判定不靠 git,靠"参照物"。** git 只是存放参照物的地方之一。任何一份可信的"原版快照"都能完成判定:git 基线提交、本地原版目录、GitHub 官方仓库,三重保险任选其一,互相可替代。全部失效时才退化为"考古"(旁证推断,只猜不证)。

**认知二:代码量大不是遗漏的原因,"靠人看"才是。** 机器枚举是全覆盖点名,不存在看漏;遗漏只发生在特定环节——所以方法不是"更仔细",而是把每个环节换成"机器点名 + 强制对账",人只在有限清单上逐项打勾。

---

## 二、方法总览:三层防线

| 层 | 职责 | 靠什么保证不漏 | 遗漏风险点 | 堵法 |
|---|---|---|---|---|
| 第一层 | 文件级分桶(点名) | 机器 MD5 逐文件对比,非抽查 | 噪声排除规则"排过头" | 排除规则白名单化并记录 |
| 第二层 | 文件内归因(记账) | 每个 hunk 强制归属 | 人工归纳功能行时看漏 | action 机械枚举 + 行数对账 |
| 第三层 | 交叉验证(印证) | 独立路径结论一致 | 单一路径盲区 | ≥2 条独立路径互查 |

### 2.1 第一层:文件级机器点名分桶

把 fork 工作区( git 跟踪文件 + 有意义的未跟踪文件)与上游目录逐文件 MD5 对比,每个文件**必然**落进四桶之一:

| 桶 | 含义 | 定性 |
|---|---|---|
| identical | 两边逐字节一致 | 📦上游原样 |
| modified | 两边都有但内容不同 | 🔧增强(或 ✅ 内含新增) |
| fork_only | 只在 fork 有 | ✅新增 |
| upstream_only | 只在上游有 | ⛔删除/清理 |

**闭环校验**:`identical + modified + fork_only + upstream_only = 参与对比文件总数`。数字对不上即有点名遗漏。

**噪声排除的两条纪律**:
- 排除项必须少而明确(`.git`/`__pycache__`/`.specstory`/pytest 临时目录/日志),逐项记录在文档"对比口径"里;
- fork 侧用 `git ls-files` 取跟踪文件(自动滤掉临时目录),再人工补"有意义的未跟踪文件"(新功能源码、openspec 提案)——实战中曾漏配 `.codex-tmp*` 规则导致 114 个临时文件混入,靠分桶明细复核剔除。

**工具**:`.tmp-doc-update/layer1_bucket.py`(可复用;输出 `buckets.json` 台账)。行数统计口径为"当前工作区实测行数",与 git numstat 口径并存、各自注明。

### 2.2 第二层:文件内功能归因 + 对账

第一层只回答"哪些文件变了";一个文件数千行变更里藏着数十个功能,**这里才是真正会漏的环节**。规则:

1. **记账**:每个 diff hunk 必须有归宿——归到某个功能行,或明确标"琐碎"(格式化/版本号/再生成产物)。不允许存在"没解释的变更"。
2. **对账**:文档中每个文件的"变更行"与 `git diff b99efef6 --numstat` 的 +/− 数字对得上;归因覆盖的行数加总与 diff 规模同量级,差得远就是还有没归纳的东西。
3. **UI 功能机械枚举**:菜单/快捷键不是用眼睛扫界面,是把 action 定义逐个 grep 出来形成有限清单(`grep -E '= action\('`、`addMenu/addAction`、yaml `shortcuts:` diff),逐项贴来源标签,打到最后一勾为止。
4. **配置面枚举**:设置 schema 字段、yaml 配置块的 diff 逐键清点(实战一次就抓出 `behavior_analytics` 约 30 个隐藏键)。
5. **commit 逐个归因**:文档基准 → HEAD 的每个 commit 过一遍,各自落到功能域;docs 类 commit 标记为非功能。

### 2.3 第三层:交叉验证

至少两条**互相独立**的路径得出一致结论才收口:

| 路径 | 用法 | 盲区 |
|---|---|---|
| git pickaxe(`git log -S` / `--diff-filter=A`) | 判定 action/文件引入于基线前还是后 | 历史被重写时失效 |
| 目录级 MD5 分桶 | 与 git 结论互查(上游目录在本地时) | 上游目录被删时失效 |
| 知识图谱(`.understand-anything`) | 17 分层模块清单查漏;`/understand-chat` 加速定位"功能在哪" | **图谱是快照,会滞后**;不含来源判定信息 |
| 上游官方 README/发布说明 | 上游功能清单反向核对 | 上游文档不全时失效 |

**知识图谱的正确定位**(实战教训):它是"结构地图 + 查漏网 + 定位加速器",**不是**溯源工具。且图谱基准落后于代码时反而会误导——2026-08-27 实战中,图谱(基准 `4fa58b04`)只覆盖了 15 个差距 commit 中的 2 个功能域(视口状态机 9 节点、多选框选 10 节点),行为分析、虚拟复核等均无节点。用前必须先核对 `meta.json` 的 `gitCommitHash`。

---

## 三、执行 SOP(下次更新照此跑)

```bash
# 0. 确认基准
git log -1 --format='%h %ci %s' HEAD            # 新 HEAD
git rev-list --count b99efef6..HEAD             # 距上游基线 commit 数
git log --oneline <旧文档基准>..HEAD             # 差距 commit 清单

# 1. 第一层:全量点名分桶(脚本可复用)
"C:\Users\20441\.conda\envs\x-anylabeling-cu12\python.exe" .tmp-doc-update/layer1_bucket.py
#    校验四桶数字闭环;复核 upstream_only / fork_only 明细里的意外项

# 2. 第二层:差距归因
git log --diff-filter=A --name-only <旧>..HEAD -- anylabeling/ scripts/   # 新文件逐 commit
git diff <旧>..HEAD -- anylabeling/views/labeling/label_widget.py | grep -E '^\+.*= action\('  # 新 action
git diff <旧>..HEAD -- anylabeling/configs/xanylabeling_config.yaml .../schema.py | grep -E '^\+[^+]'  # 新配置键
git diff b99efef6 --numstat -- <modified 文件>    # 行数对账
git status --porcelain | grep -vE '<噪声>'        # 当前未提交工作(写入"进行中"节)

# 3. 第三层:交叉验证
#    - 知识图谱:先看 .understand-anything/meta.json 的 commit 与文件数,过期先 /understand 重跑
#    - 图谱 layers 清单 vs 文档覆盖模块,双向查漏
#    - git pickaxe 抽查 action 定性(基线前=上游 / 基线后=个人)

# 4. 更新两份文档
#    - 表头基准块:HEAD / commit 数 / 未提交说明 / 本轮验证方式
#    - 新功能行入对应分区;修正表追加"旧表述 vs 实况"
#    - 统计汇总对账:桶数、行数、action 数
```

**收口检查清单**:
- [ ] 四桶数字闭环(总数 = 四桶之和)
- [ ] modified 每个文件在表二第一节有行、有定性、有 +/− 行数
- [ ] 差距内每个 commit 都已归因(功能域 / docs / 琐碎)
- [ ] 新 action、新配置键清单打勾到最后一项
- [ ] "工作区未提交"一节与 `git status` 当前实况一致
- [ ] 至少两条独立路径对分歧项结论一致
- [ ] 知识图谱 commit 已核对,滞后已注明或已重跑

---

## 四、实战记录(2026-08-27,首次完整执行)

**输入**:文档基准 `77cf2832`(153 commits)→ HEAD `c6b3a806`(168 commits),15 commits + 未提交工作区。

**第一层产出**:上游 1173 文件 vs fork 1898 文件 → 一致 1118 / 修改 34 / fork 独有 746 / 上游独有 21(闭环 ✓)。

**第二层产出**:7 个新功能域 9 个新 action 3 个新配置块——视口状态机(已提交)、Ctrl 多选+框选、标注外观系统、矩形复核精修、虚拟复核队列(F2/Shift+F2)、本地行为分析(30 文件 10,447 行)、跨图对象操作+shape identity;新增应用代码 44,156 → 66,830 行。

**第三层产出**:知识图谱仅覆盖 2/7 新域(滞后 13 commits),已覆盖域与 git 结论一致;图谱分层清单反向核对无整模块遗漏。

**揪出的旧文档错误**:
1. "仅上游有 1 个文件"实为 21 个(漏记 `.github/` 7 + visualgd 编译产物 9);
2. `filter_label_widget.py` 被误记为"新增文件",实为改上游文件;
3. "工作区未提交"内容过时(视口状态机、跨图重标注均已提交;当前未提交为密集矩形可见性修复 + 重复矩形质检)。

---

## 五、维护建议

1. **上游目录 `D:\X-AnyLabeling-4.0.0-beta.4` 不要删**——最省事的照妖镜,git 之外的第二参照物;
2. 每轮更新后**同步重跑 `/understand`** 刷新知识图谱,否则第三层少一条验证路径;
3. `layer1_bucket.py` + `buckets.json` 建议长期保留(可考虑迁入 `scripts/` 固化),它是每轮更新的点名台账;
4. 文档更新频率建议跟随功能域落地节奏(而非逐 commit),每轮更新在表头记录"本轮验证方式",保证方法可追溯。
