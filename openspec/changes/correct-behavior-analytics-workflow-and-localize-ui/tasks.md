## 1. 事件契约与兼容基础

- [x] 1.1 在 `versions.py` 和 `schema.py` 中定义 v4 事件 envelope、修正后的分析/导出版本及 v1-v4 支持范围，保留旧版本只读解析。
- [x] 1.2 为 v4 增加显式对象 episode 结束事实、选择来源、选择批次和结构化未归属原因的枚举与隐私白名单校验。
- [x] 1.3 明确 v4 中 `monotonic_ms` 仅用于排序、动作区间必须使用 `started_monotonic_ms`/`ended_monotonic_ms`，并拒绝负时长或缺失终态的已完成动作。
- [x] 1.4 为 v1/v2/v3/v4 单独及混合输入增加 schema、序列顺序、未知字段、隐私过滤和降级诊断测试。

## 2. 消除程序化事件风暴

- [x] 2.1 在 `label_list_widget.py` 与 `label_widget.py` 的标签列表重建、复选状态同步和可见性刷新路径中加入最小作用域程序化更新 guard 或信号阻断。
- [x] 2.2 将真实用户可见性/属性修改迁移到可验证的用户交互入口，并记录 shape_id、当前 episode、属性类别、变化摘要和关联上下文。
- [x] 2.3 审计所有 `attribute_edit` 发射点，保证一次逻辑编辑只产生一个语义动作，程序化加载、pan、zoom 和筛选刷新产生零个动作。
- [x] 2.4 增加 PyQt offscreen 回归测试：图片加载和程序刷新不记录 `attribute_edit`，用户点击仅记录一次且身份字段完整。

## 3. 选择来源与对象周期状态机

- [x] 3.1 扩展对象选择入口，区分画布/列表用户单选、用户多选、程序化同步和恢复状态，并为一次批量同步生成稳定批次标识。
- [x] 3.2 调整 `SessionTracker`，使 episode 开始和结束幂等，并在下一对象、翻页、删除、清除选择和关闭项目时保存真实边界时刻与原因。
- [x] 3.3 在 telemetry 中发射显式 `object_episode_ended` 事实，并保证选择 A→B 时 A 的结束与 B 的开始共享边界但不重叠。
- [x] 3.4 处理同毫秒多个选择事件：使用 sequence_no 和 selection_batch_id 确定顺序，禁止一个动作归入多个 episode。
- [x] 3.5 增加纯 Python 状态机测试，覆盖 A→B→A、翻页、删除、清除、项目关闭、重复关闭、未闭合范围和同毫秒批量选择。

## 4. 动作归属与焦点空闲接线

- [x] 4.1 修正 `ActionSpan` 到事件 envelope 的映射，确保开始、结束、排序时刻和 duration 在几何成功、no_change、取消和生命周期中断场景中一致。
- [x] 4.2 将 geometry/rectangle/save/zoom/pan/create/delete/navigation 动作在运行期关联到当前 episode；无当前对象时写入结构化未归属原因。
- [x] 4.3 将现有 `ActivityTracker` 接入窗口激活/失焦事件和低频 idle 观察定时器，确保失焦、空闲、恢复和关闭时边界完整发射。
- [x] 4.4 增加动作归属与活动边界测试，覆盖保存后翻页、未选择时缩放、失焦恢复、超时空闲、空闲后继续和关闭时未完成 burst。

## 5. 对象周期与时间分解统计

- [x] 5.1 重写 episode 指标构建逻辑，v4 优先使用显式关闭事实，结束时间取真实下一选择/翻页等边界，不再取 episode 内最后一条已归属事件。
- [x] 5.2 为 v1-v3 保留确定性回放兜底，但将缺失结束、焦点或空闲边界分别标记为低可信且不得伪造精确 active 时间。
- [x] 5.3 修正 `statistics_v3.py` 动作区间起点，使用 started/ended 半开区间、并集与交集计算动作、等待、转换和中性剩余量。
- [x] 5.4 增加时间守恒断言和回归测试，覆盖提交时间等于排序时间、重叠动作、零时长保存、失焦/空闲切片和跨夜挂机。
- [x] 5.5 新增对象周期样本分类与分位数输出，分别报告正耗时、同毫秒零耗时、未闭合、超过停顿阈值和停顿调整样本，并版本化停顿规则。
- [x] 5.6 使用合成夹具验收“选中对象到下一对象或翻页”口径，断言样本数、中位数、P75、P90、P95和边界原因可复现。

## 6. 分指标质量状态

- [x] 6.1 将质量诊断拆为动作耗时、对象周期、focused/active、上下文比较、顺序完整性和导出完整性等独立状态与覆盖率。
- [x] 6.2 保留兼容汇总 `quality_gate`，同时输出每项 `available/pass/degraded/fail`、分子、分母、阈值和稳定原因代码。
- [x] 6.3 增加测试证明 context coverage 失败只禁用功能比较，不会使边界完整的动作耗时与对象周期一起失效。
- [x] 6.4 增加焦点/空闲缺失测试，保证 wall 可用而 focused/active 明确降级且不等同 wall。

## 7. 紧凑分析包与版本迁移

- [x] 7.1 精简 `summary.json`，移除全量 `action_spans`、`semantic_actions` 和重复维度副本，只保留版本、质量、核心聚合和表索引。
- [x] 7.2 保持高基数 CSV/JSONL 的行数与字节上限，验证 `other` 汇总、代表性轨迹和取消导出的确定性行为。
- [x] 7.3 更新 manifest 与 measurement quality 输出，记录每个文件的哈希、总行数、导出/省略行数、v1-v4 分布和算法版本。
- [x] 7.4 增加大型合成会话 bundle 测试，断言汇总不重复嵌入明细、声明的大小限制生效且所有 SHA-256 可复核。

## 8. 行为分析界面中文化

- [x] 8.1 将 `behavior_analytics_dialog.py` 中窗口标题、开关、隐私说明、范围、日期选项、输出目录、状态、质量说明和按钮全部改为 Qt 可翻译字符串。
- [x] 8.2 将 `label_widget.py` 中行为分析菜单、导出拒绝、进度、取消、成功、失败和日志清理确认文案改为 `self.tr()` 或 `QCoreApplication.translate()` 模板。
- [x] 8.3 建立稳定质量/错误原因代码到中文翻译模板的映射，避免把内部英文异常直接显示为主要用户提示。
- [x] 8.4 调整对话框可伸缩布局和自动换行，保证长中文质量摘要、记录健康计数和长路径不会遮挡导出、清理或关闭按钮。
- [x] 8.5 更新 `zh_CN.ts` 中全部行为分析翻译，保留 JSONL、UTC、CSV 和文件名等技术标识，并检查中文环境无残留完整英文操作句子。
- [x] 8.6 运行 `scripts/compile_languages.py` 重新生成 `.qm` 和 `resources.py`，不得手工编辑生成资源，并验证 PyQt6 导入与现有语言加载。
- [x] 8.7 增加 PyQt offscreen 中文 UI 测试，覆盖静态控件、无项目会话拒绝、记录健康状态、部分质量降级、导出进度和长文案布局。

## 9. 文档、验证与实采验收

- [x] 9.1 更新 `docs/behavior_analytics.md`，说明 v4 边界、四层时间口径、指标级质量状态、版本断点和中文 UI 操作流程。
- [x] 9.2 运行行为分析 schema/session/action/metrics/quality/bundle 的最小相关纯 Python 测试集合并修复回归。
- [x] 9.3 以 `QT_QPA_PLATFORM=offscreen` 运行程序化更新 guard 和中文对话框的相关 PyQt 测试。
- [x] 9.4 对改动文件运行 Black 79列、flake8 和 py_compile；若修改 `.ts`，同时验证语言编译产物与资源导入。
- [x] 9.5 采集一段受控本地样本并重新导出，验收程序化 `attribute_edit` 为零、真实属性编辑一操作一事件、episode 正常闭合、动作区间守恒且 focused/active 有覆盖。
- [x] 9.6 使用受控样本复算“选中对象到下一对象或翻页”的周期分位数，确认现成导出无需外部补算即可得到正确结果，并记录与旧 v3 基线不可直接比较的说明。
