## 1. 增加对象位置查询

- [x] 1.1 新增不可变对象位置结果，包含标签、标签内稳定 offset、图片路径、Shape ID 和 shape_index。
- [x] 1.2 在 SQLite 索引实现按规范化图片路径和永久 Shape ID 的只读位置查询，并通过 READY 状态控制器转发。
- [x] 1.3 增加命中、未命中、重复 ID、非 READY 门禁和跨页 offset 测试。

## 2. 实现缩略图到主画面导航

- [x] 2.1 为缩略图窗口增加双击与 Enter 激活信号，普通单击仍只处理当前页选择。
- [x] 2.2 在主界面复用 dirty 处理和图片加载入口，按永久 Shape ID 选择并聚焦画布对象。
- [x] 2.3 增加同图定位、跨图定位、dirty 取消、对象不存在和单击不导航测试。

## 3. 实现主画面到缩略图反向定位

- [x] 3.1 在主画面单选变化时向已打开窗口发送完整对象身份，零选、多选、无永久 ID 时不联动。
- [x] 3.2 使用对象位置查询切换标签与页码、滚动并选择对应缩略图，未命中时保持原浏览状态。
- [x] 3.3 增加防重入与最后身份去重，测试跨标签、跨页、未命中和双向循环边界。

## 4. 复用数字标签快捷键

- [x] 4.1 向缩略图窗口提供现有数字绑定的只读解析回调，只在窗口活动且有有效选择时处理数字键。
- [x] 4.2 文本输入控件优先接收数字；已绑定数字通过现有 `relabel_requested` 和统一确认事务处理。
- [x] 4.3 增加已绑定多选、未绑定、无选择、文本焦点和不绕过确认链路的测试。

## 5. 收紧数据集生命周期

- [x] 5.1 在导入新数据集、关闭当前数据集和应用关闭时关闭并释放缩略图窗口。
- [x] 5.2 重新打开时使用新项目身份、索引上下文和缓存目录，不热重绑定旧窗口。
- [x] 5.3 增加数据集切换、关闭与重新打开缓存隔离测试。

## 6. 集成验收

- [x] 6.1 更新 Qt 翻译资源和用户文档，说明双向定位、数字快捷键与数据集关闭行为。
- [x] 6.2 运行新增索引/导航/快捷键测试以及现有缩略图、dataset-index、object-relabel 回归测试。
- [x] 6.3 运行 Black、Flake8、`py_compile` 和 OpenSpec 严格校验，确认未增加跨页选择、删除、字段编辑、多人协作或独立撤销功能。
