## 1. 基线与回归契约

- [x] 1.1 对照 `optimize-annotation-visual-encoding-and-label-batch-operations` 未完成的 7.6、7.9、9.3、9.4、10.1，记录可复用实现和冲突文件，避免建立第二套外观状态
- [x] 1.2 为 `unrelated_opacity=0.0/0.08/0.28/1.0` 添加精确的纯 Python Focus 强调失败测试，替换只断言 `< 1.0` 的弱契约
- [x] 1.3 添加 Qt offscreen 回归测试，证明当前零透明度仍进入 `Shape.paint()` 且标签/覆盖层门控不统一
- [x] 1.4 添加隔离选择矩阵测试夹具，覆盖单组、同组多选、跨组多选、未分组与空选择
- [x] 1.5 添加重复矩形规则的最小失败测试，覆盖同组 error、未分组 warning、不同 label 排除和稳定对象对去重

## 2. Focus 透明度与最终渲染决策

- [x] 2.1 修改纯 Python Focus 强调接口，使其显式消费已钳制的 `unrelated_opacity` 并保持默认 `0.28` 兼容行为
- [x] 2.2 增加不可变最终渲染决策类型，表达 geometry/text/GID/size-overlay/canvas-interactive 五类输出门控
- [x] 2.3 实现纯 Python 渲染决策解析器，按基础可见性、Focus、隔离、标签/GID偏好顺序合并状态
- [x] 2.4 为零透明度、隔离排除、基础过滤优先级和全局 Show Labels 优先级补齐决策解析器单测
- [x] 2.5 更新 Qt 外观适配器，使填充透明度同时乘以对象透明度并正确处理 0 与 1 边界
- [x] 2.6 验证设置即时应用仅触发合并后的画布刷新，不修改 shape、JSON、dirty、undo 或自动保存队列

## 3. Canvas 统一绘制门控

- [x] 3.1 在 `Canvas.paintEvent` 的视口裁剪后、`Shape.paint()` 前接入最终渲染决策，零输出对象不得进入几何绘制
- [x] 3.2 让标准标签布局与绘制循环消费最终文字门控，并移除未消费或重复的外观标签徽标路径
- [x] 3.3 让 GID 徽标消费最终 GID 门控，验证 `always/focus/never` 与隔离规则组合
- [x] 3.4 让普通矩形尺寸覆盖层消费最终 size-overlay 门控，防止隐藏对象残留尺寸文字
- [x] 3.5 让 Canvas 点击、框选、悬停候选和矩形边编辑统一排除 `canvas_interactive=false` 的隔离对象
- [x] 3.6 保证 QA/Inspector 数据仍存在且可导航，同时无关对象的普通画布输出在隔离时全部隐藏
- [x] 3.7 添加浅色、深色和纹理背景下的 offscreen 像素测试，确认零透明度没有边框、填充或文字残留
- [x] 3.8 添加密集当前图片绘制计数测试，证明零透明度对象不调用 `Shape.paint()` 且不建立标签布局

## 4. 瞬态选中/同组隔离状态

- [x] 4.1 新增纯 Python 隔离状态模型，以 image token、enabled、focused group 和 selected shape tokens 表达目标
- [x] 4.2 实现单组、同组多选、跨组多选和未分组选择的隔离目标派生规则
- [x] 4.3 将正式选择快照接入隔离状态更新，并保持 Canvas、标签列表和 Inspector 三种选择来源语义一致
- [x] 4.4 在清空选择、空白点击、图片切换/关闭、目标删除和不支持模式切换时清理隔离状态
- [x] 4.5 保证隔离只作用于已经通过标签筛选、组合筛选、用户隐藏和虚拟复核的基础可见对象
- [x] 4.6 保证标签列表或 Inspector 导航可以选择当前被隔离隐藏的对象并立即转移隔离目标
- [x] 4.7 为跨图重复 GID、删除最后目标、过滤同组成员和导航转移补齐纯 Python及 Qt 交互测试

## 5. 隔离入口、反馈与本地化

- [x] 5.1 新增可勾选的“隔离选中对象/同组对象”action，并通过快捷键 schema 选择无冲突默认快捷键
- [x] 5.2 将 action 放入视图菜单及适合密集审核的快捷入口，checked 状态与实际隔离状态双向同步
- [x] 5.3 在状态栏和画布角标显示隔离已启用、当前组或选中对象数量，并提供明确退出提示
- [x] 5.4 在设置说明中区分长期的无关对象透明度与瞬态隔离开关，保持隔离默认关闭
- [x] 5.5 为新增 action、状态、退出原因和设置提示添加翻译源字符串并更新各语言 `.ts`
- [x] 5.6 运行 `scripts/compile_languages.py` 重建 `.qm` 和 `resources.py`，不得手工编辑生成文件
- [x] 5.7 添加菜单状态、快捷键、状态提示和切图自动退出的 Qt offscreen 测试

## 6. 重复矩形纯 Python 质检规则

- [x] 6.1 在质量阈值 profile schema 中增加同组 error IoU、跨组/未分组 warning IoU 和边坐标容差字段及默认值
- [x] 6.2 实现有效矩形标准化与边坐标差计算，跳过非矩形、空 label、非法和零面积 bbox
- [x] 6.3 按文件和 label 分桶，并实现扫线或空间网格候选筛选以排除不可能达到阈值的对象对
- [x] 6.4 实现同组 `IoU>=0.95` 或四边差 `<=2px` 的 error 判定
- [x] 6.5 实现跨组或未分组 `IoU>=0.97` 的 warning 判定，并确保不同 label 永不进入重复规则
- [x] 6.6 为无序对象对生成稳定键与 issue_id，输出双方身份、索引、label、group_id、IoU、最大边差和触发原因
- [x] 6.7 将重复矩形 runner 注册到 L2 质检主驱动，并允许通过质量 profile 显式启用或关闭
- [x] 6.8 用穷举 oracle 对随机小数据集验证空间候选结果，覆盖网格边界、包含、相交、完全重合和不相交情况
- [x] 6.9 添加 person/head/face 嵌套不误报、同标签真实双目标 warning 和非法 bbox 分层处理测试

## 7. 报告与人工复核集成

- [x] 7.1 扩展 `QualityIssue`/report 序列化的 details 使用方式，保持旧报告可加载并保存重复对象双方证据
- [x] 7.2 确认 `review.tsv` 与 `report.json` 对每个重复对象对只输出一条稳定问题记录
- [x] 7.3 在质检复核队列展示另一对象索引、IoU 和触发原因，并保持现有 issue_id feedback upsert 语义
- [x] 7.4 实现从重复问题导航到主对象，并提供定位另一对象的非破坏性复核入口
- [x] 7.5 验证接受、误报、暂不处理和复扫合并均不修改阈值配置或标注 JSON
- [x] 7.6 添加报告兼容、队列加载、导航、反馈保留和 `resolved_after_rescan` 测试

## 8. 性能、质量门与交付

- [x] 8.1 建立包含多组重叠矩形的绘制基准，记录全显示、0.08 弱化、0.0 跳过和隔离模式的 paint 调用与耗时
- [x] 8.2 建立大规模同标签矩形质检基准，记录候选对数量、实际 IoU 计算次数、总耗时和与穷举 oracle 的一致性
- [x] 8.3 运行外观、Canvas 交互、标签显示、尺寸覆盖层、Focus、虚拟复核和质量输出的最小相关测试集
- [x] 8.4 使用可写 `BLACK_CACHE_DIR` 对变更 Python 文件运行 Black，并对变更模块和测试运行 flake8
- [x] 8.5 运行翻译编译与生成资源检查，确认未手工改动 `anylabeling/resources/resources.py`
- [x] 8.6 复核显示操作不进入 JSON、dirty、undo、autosave、质量反馈或导出数据，重复质检扫描保持只读
- [ ] 8.7 完成密集真实图片人工审核，对比默认 Focus、低透明度、零透明度和隔离模式并记录视觉遮挡反馈
- [x] 8.8 运行 `openspec validate fix-dense-rectangle-visibility --strict` 并修复所有规格或任务一致性问题
