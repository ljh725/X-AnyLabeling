# 实施验收记录（2026-09-07）

范围按用户修订收敛为 31 项：单标签对象统一展示、本页多选、仅修改 label；未引入跨标签或跨页选择扩展。实施前提交 `4e75c3ce`，备份标签 `backup/thumbnail-before-20260907`。

- 最终跨模块批次：242 passed，4 条既有 NumPy 二维 cross 弃用警告。覆盖缩略图管线/索引/浏览器、对象改标/主窗口接线、共享批量事务、键盘备份及矩形编辑语义共 11 个测试文件。
- 后续新增“真实主窗口连续 100 次跨类别选择”和“暂存转换期间源文件变化”两项回归通过；连同一项相邻原用例复测结果为 3 passed。合计覆盖 244 个不同用例，未为重复确认重跑全部测试。
- 真实 Qt、SQLite、QThread 与确认对话框完成单对象提交及撤销，验证其他字段/对象完全不变；窗口截图经目视检查。多选、翻页、显式定位、数字键、部分失败重试、切数据集守卫由相应专项覆盖。这是自动化交互与截图检查，未冒充用户原数据上的人工全流程验收。
- Black 格式检查及受影响源文件/测试的 Flake8 检查通过；`label_widget.py` 有 9 条实施前已存在的 lint 问题，已与备份节点逐条比对，没有新增。未修改这些无关问题。
- 四语言翻译已编译；`resources.py` 由语言编译脚本生成。
- 变更及 3 份正式规格的 OpenSpec 严格校验通过，`git diff --check` 通过。

## 性能

Windows 10 19045、Python 3.12.12、Qt 6.10.0；Intel64 Family 6 Model 165、12 逻辑核、约 15.75 GiB RAM。使用本地临时目录，物理存储介质未识别。三组模式 PNG，各 8 张图，104/504/2000 个对象，分辨率 640×480 / 1920×1080 / 3840×2160，每组 5 次。应用缓存初始为空，系统缓存未控制；不是用户实际数据集。

| 操作 | 修改前 p50 / p95（ms） | 修改后 p50 / p95（ms） |
|---|---:|---:|
| 窗口壳显示 | 3.92 / 23.81 | 2.06 / 11.09 |
| 重新激活 | 6.35 / 7.70 | 0.51 / 0.79 |
| 显式定位 | 3.26 / 4.76 | 4.46 / 7.14 |
| 关闭 | 1.46 / 13.12 | 0.73 / 1.39 |

窗口壳/激活满足 p95 ≤ 200 ms，定位满足 p95 ≤ 300 ms。冷首屏事件循环最大观测间隔 13.62 ms，低于 100 ms 预算。壳显示、激活、关闭 p95 降幅超过 30%；定位略慢，不能称为全项提速。

| 对象数 | 冷首屏平均：前 → 后（ms） | 首屏后进程 RSS（MiB） |
|---|---:|---:|
| 104 | 69.23 → 71.68 | 63.67–65.15 |
| 504 | 90.56 → 125.76 | 80.18–80.60 |
| 2000 | 145.04 → 137.93 | 129.17–129.62 |

冷首屏有波动，中组本次复测变慢，缓存写入累计均值约 79.99 ms；未认定所有冷加载瓶颈已消除。大组首屏各 worker 阶段累计均值：原图读取/解码 59.22 ms、裁剪 0.87 ms、缩放 2.09 ms、编码 9.65 ms、缓存写入 47.20 ms、主线程结果应用 2.19 ms。裁剪本身不是该样本主要开销；并发阶段累计值不能直接相加解释首屏时间。

观测请求队列峰值 20，对象请求限当前页最多 100，工作并发 2，内存缩略图缓存上限仍为 512。整个基准进程累计峰值 RSS 129.84 MiB（包含 Qt、索引、原图与缓存，非单对象成本）；修改前未采集 RSS，不能据此声称内存下降。没有扩大缓存上限或漏图换速度。

原始数据：`baseline.json`、`after.json`；复测脚本：`scripts/benchmark_thumbnail_review.py`。主线程磁盘缓存读取移入 worker、同图批次只解码一次、过期任务丢弃与非阻塞关闭均有专项断言。

## 风险结论

修复前专项测试确实复现 `shapes_backups` 越界；修复后空/短备份、对象删除/重排、自动重复、焦点丢失、切图均通过。真实主窗口 100 次跨类别选择未触发缩略图查询，显式快捷键仍可定位。原用户数据的整个项目卡死未在合成环境重现，不能断言所有卡顿都由这一异常解释。

单对象撤销通过共享事务反向改 label；校验完整标注文档的语义摘要和提交阶段文件状态，后续冲突拒绝写入。另修复共享暂存过程的源文件版本基线竞态，防止把转换期间发生的外部修改当作可覆盖版本。

使用说明见 `docs/thumbnail_review_usage.md`。上一页策略保留；正式规格同步，变更保持未归档状态。
