## 1. P0 — 风险复现与性能基线（经验 6、7、8）

- [ ] 1.1 按 canvas-090 文档复现程序化选择后移动/旋转释放异常，分别记录主画面跨类别选择卡顿、窗口激活卡顿和异常堆栈，避免混同根因。
- [ ] 1.2 建立固定小/中/大数据集场景，记录硬件、图片分辨率、对象数、存储与冷/热缓存；测量首开、激活、显式定位到其他标签的对象、连续翻页和关闭的端到端与事件循环延迟。
- [ ] 1.3 拆分查询、stat、缓存读写、原图读取/解码、裁剪、缩放、编码和显示耗时，输出简洁基线表并固化 design 中建议的验收预算或记录调整依据。
- [ ] 1.4 补充键盘专项回归，覆盖延迟备份、空/短备份、对象删除/重排、连续按键与按住键切图。
- [ ] 1.5 在有效键盘几何变更前准备一次初始快照，修复释放阶段防御与所有出口的 moving/rotating 状态收尾，验证实际变化通知和一次撤销。
- [ ] 1.6 用 Inspector、缩略图、标签列表和 Ctrl+A 入口完成 P0 回归；无变化不增加历史，切图不修改新文件。

## 2. P1 — 显式导航、单标签浏览与分页（经验 1、4、9）

- [ ] 2.1 移除主画面普通单选的自动反向定位，增加明确定位菜单/按钮，查重后配置组合键；无/多选时不猜测目标。
- [ ] 2.2 保留缩略图双击/Enter 导航及未保存内容守卫，分离导航焦点、键盘当前位置与本页选择，验证重入和旧请求隔离；显式换标签/页码沿用既有选择清理规则。
- [ ] 2.3 回归单标签筛选后的统一对象展示：不同图片的同类对象分别显示，稳定排序，每页最多 100 项，本页多选及永久身份不变。
- [ ] 2.4 确保同页缩略图回填不改变选择，切换标签/分页清除普通选择，仅恢复仍位于目标页的失败、冲突或取消对象。
- [ ] 2.5 下一页布局完成后回顶部并设置首个可用 current index，不新增选中项；处理空页、末页收缩。上一页保留现状。
- [ ] 2.6 在缩略图浏览器/导航测试中验证普通单选不跳转、同页定位不改变改标候选、组合键焦点规则和下一页位置。

## 3. P2 — 仅标签改名与单对象轻量撤销（经验 2、3、5）

- [ ] 3.1 将按钮和数字键统一到本页所选对象的不可变快照，按不同对象数区分单/多对象，同一图片多个对象按多对象处理。
- [ ] 3.2 更新确认界面：本页所选对象/涉及文件数、原标签、目标标签、对象清单、立即写盘及对应撤销说明；取消保留本页选择。
- [ ] 3.3 复用现有预检、备份、暂存验证、原子替换和恢复清单，所有修改只作用于 label，保持多对象不进入普通撤销栈。
- [ ] 3.4 增加最近一次成功单对象 label 修改的会话撤销记录和结果条按钮；取消、失败、无变化不创建或消费记录，新成功操作/关闭/换数据集按设计失效。
- [ ] 3.5 通过共享事务执行字段级反向修改，校验对象身份、预期标签和提交版本，处理未保存内容、对象消失/重复与后续冲突，禁止整文件旧内容覆盖。
- [ ] 3.6 协调单对象撤销、画布既有历史、文件刷新与索引屏障；成功/无需修改候选移出，失败/冲突项沿用既有按页恢复机制。
- [ ] 3.7 验证单对象撤销不改几何/其他字段/其他对象，批量无需普通撤销、恢复清单仍可用；数字键与按钮行为一致。

## 4. P3 — 生成进度与窗口性能（经验 6、7）

- [ ] 4.1 将扫描文件进度与当前视图缩略图请求/完成/失败进度分开，明确扫描不执行全量裁剪，缓存命中计数准确。
- [ ] 4.2 增加错误项重试及终态计数，处理滚动扩展请求集、换页、旧代次与重复完成，保证进度不永久等待失败项。
- [ ] 4.3 先显示窗口与占位内容，后台读取文件状态和磁盘缩略图；如基线证明查询阻塞，使用独立只读连接移出 UI 线程，不跨线程复用旧 SQLite 连接。
- [ ] 4.4 为可见项建立优先级和有界请求队列，清除未启动过期任务、在对象间协作取消；关闭窗口不在主线程等待后台任务。
- [ ] 4.5 保留同图同批次一次解码和并发上限，复用同路径文件状态；按基线优化主要耗时，不无界缓存整图或预裁剪全数据集。
- [ ] 4.6 合并有界结果更新，避免未变化卡片重复解码、整页重置和窗口激活时重复查询，保持滚动/焦点/候选。
- [ ] 4.7 增加当前视口优先、旧任务丢弃、慢缓存、关闭竞态和进度回归，用 P0 相同环境复测 p50/p95、解码次数、队列峰值与内存。

## 5. P4 — 集成验收与交付

- [ ] 5.1 更新新增/变更 UI 文案与翻译，运行语言编译脚本生成资源，不手改 resources.py。
- [ ] 5.2 按改动运行相关 1–2 个测试文件/选择器批次，检查受影响代码的格式和 lint；完整集成回归仅在最终跨模块验收时按需要执行，记录结果。
- [ ] 5.3 人工走通“单标签筛选 → 本页多选 → 翻页 → 显式定位 → 键盘微调 → 单对象改标/撤销 → 多对象确认提交 → 部分失败重试 → 切数据集”闭环。
- [ ] 5.4 在原故障工作流重复跨类别选择和窗口切换，确认零异常、无递归跳转、同页无意外丢选、无误写，并对照固化性能预算验收。
- [ ] 5.5 更新简明使用说明和 canvas-090 风险处理状态，记录上一页策略仍未变更、默认组合键及单次撤销限制；实现完成后再同步规格并按用户要求归档。
