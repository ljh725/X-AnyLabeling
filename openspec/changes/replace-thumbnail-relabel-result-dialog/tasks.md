## 1. 统一改标终态呈现接口

- [ ] 1.1 扩展 `run_object_relabel_flow()`，允许调用方接管结构化终态呈现，同时保持主菜单入口默认只显示一次现有结果对话框
- [ ] 1.2 调整 `LabelingWidget._launch_object_relabel()` 的完成顺序，在恢复 action 和 `_object_relabel_running` 前完成标记、索引、当前画布及入口专属结果同步
- [ ] 1.3 让缩略图入口关闭通用结果对话框并接收同一个 `ObjectRelabelResult`，保持 worker 排空、进度框关闭和 `BatchWriteGate` 释放语义不变
- [ ] 1.4 保留没有可信 `ObjectRelabelResult` 的 worker/事务致命错误路径，并确认该路径不会显示普通成功结果条

## 2. 缩略图窗口内结果条

- [ ] 2.1 在 `DatasetLabelThumbnailWindow` 中增加与 `summary_label` 分离的持久结果条，包含摘要、查看详情、可选“恢复本次…”及关闭控件
- [ ] 2.2 实现确定的终态分类和非零计数文案组合，覆盖全部成功、无需写入、部分完成、未完成、空结果以及预检/确认/暂存三种取消阶段
- [ ] 2.3 将成功结果显示为绿色、普通无写入和取消显示为中性、部分失败显示为橙色、结构化全部失败显示为红色，并让结果保留到下一次操作、主动关闭或窗口关闭
- [ ] 2.4 保持成功/无变化/已删除对象退出选择，失败/冲突/取消对象通过 `_retained_selection` 和 mutation error overlay 保留以便重试
- [ ] 2.5 仅当终态包含实际提交文件和有效 `manifest_path` 时显示“恢复本次…”，并在详情中展示和允许复制准确清单路径

## 3. 索引刷新屏障与输入门控

- [ ] 3.1 增加缩略图本地 `_relabel_refresh_pending` 状态，在提交结果到达后立即阻止基于旧页面再次改标
- [ ] 3.2 统一让 `_apply_digit_shortcut()`、`_request_relabel()` 和改标按钮 enabled 逻辑检查刷新屏障、控制器 busy 状态及索引 ready 状态
- [ ] 3.3 仅在索引同步成功且 `_load_page()` 用最新查询替换模型后解除刷新屏障；索引 stale/unavailable 时保持改标禁用并保留扫描恢复入口
- [ ] 3.4 保持换页和切标签清除普通选择，只恢复当前页内的失败、冲突或取消项，不引入跨页普通选择集合

## 4. 本次事务恢复安全

- [ ] 4.1 在 JSON 原子提交成功后记录每个 manifest entry 的 `committed_fingerprint`，并保持旧 manifest 的读取兼容
- [ ] 4.2 为恢复流程增加逐文件提交后状态预检：指纹不等于 `committed_fingerprint` 的文件报告冲突并跳过，其余通过校验的文件继续恢复，终态分别统计恢复、冲突和失败文件
- [ ] 4.3 让结果条“恢复本次…”直接传递准确 `manifest_path`，不打开文件选择框，同时复用数据集归属和当前 dirty 状态校验
- [ ] 4.4 在执行文件级恢复前显示明确确认，说明该操作不是普通撤销；对缺少可验证提交后状态的旧清单禁止从结果条无提示恢复
- [ ] 4.5 恢复成功后刷新对应 SQLite 条目、当前画布和缩略图页面，并在缩略图窗口内更新恢复终态

## 5. 翻译与自动测试

- [ ] 5.1 为结果条、计数片段、取消阶段、恢复入口、恢复冲突和刷新等待状态增加可翻译字符串并更新 `.ts` 文件
- [ ] 5.2 扩展对象改标流程测试，验证主菜单默认结果框兼容、缩略图不显示通用结果框、终态只交付一次及完成顺序
- [ ] 5.3 扩展缩略图 offscreen 测试，覆盖结果文案矩阵、严重度、结果持久性、失败项保留和普通选择不跨页
- [ ] 5.4 增加刷新窗口期测试，验证 `_reload_timer` 完成前按钮和已绑定数字键均不能发起新事务，索引失败时保持禁用
- [ ] 5.5 增加恢复测试，覆盖准确 manifest 直达、无提交时隐藏入口、提交后文件未变化可恢复、混合批次逐文件跳过后续编辑冲突且继续恢复其余文件，以及旧清单兼容行为
- [ ] 5.6 运行相关 pytest 选择器、Black/flake8 窄检查及 `scripts/compile_languages.py`，确认生成资源和现有主入口行为无回归
