## 1. 阶段 A：复现与差异更新基础

- [ ] 1.1 在隔离数据集复现单对象/批量改标整页闪烁、主画面自动保存无关对象触发重载；记录模型 reset、图片请求和锚点位置基线。
- [ ] 1.2 核对现有主规格与三个未归档缩略图变更，确认普通选择、显式导航、两条书签、复核筛选和每页 100 项边界不回退。
- [x] 1.3 新增纯 Python 页面差异计算，按完整永久身份区分增删、移动、文字/状态与图像内容变化；覆盖对象重排和同文件多对象单元测试。
- [x] 1.4 模型应用局部行增删/移动和数据更新，保留不变 QImage、有效选择、焦点及失败对象状态，移除自动同步中的整页 reset。
- [x] 1.5 调整渲染请求与回填校验，只补充新增/变化图片，拒收删除、重排、查询切换后的过期内容，覆盖迟到结果测试。

## 2. 阶段 A：保存通知与阅读位置

- [x] 2.1 保留并合并文件刷新路径，核对当前查询成员、显示内容、排序和总数；无变化页面不触碰视图，页外对象进入筛选不能漏更新。
- [x] 2.2 改标、文件恢复和复核状态更新接入差异同步；标签选项/总数独立更新，当前标签归零保持空筛选。
- [ ] 2.3 实现实时锚点与像素偏移捕获/恢复，覆盖当前对象退出后向后优先、向前回退和焦点承接但不自动勾选。
- [ ] 2.4 处理锚点跨页、末页缩短、整页退出、滚动边界和空结果；迟到锚点不得覆盖用户新滚动、翻页或筛选动作。
- [x] 2.5 把刷新屏障完成条件改为相关索引同步和差异应用完成，保留同步失败阻止改标的规则，不等待全屏图片生成。
- [ ] 2.6 更新现有“通知合并后重载一次”测试为行为验收：无关保存零 reset/零新增图片请求；改一项只更新必要图片；不变锚点在无边界夹紧时偏移误差不超过 2 px。
- [ ] 2.7 运行受影响的 browser、pipeline、书签和高级复核定向测试；人工连续处理卡片验证无全屏占位闪烁、位置可追踪，记录阶段 A 结果后进入 B。

## 3. 阶段 B：持久历史与提交一致性

- [x] 3.1 实现版本化纯 Python 历史模型与分页存储，包含操作头、逐对象前后值/结果、完整身份、事务及父操作引用、查询和阅读位置。
- [x] 3.2 迁移已有 ReviewStore 状态到兼容存储，保证复核状态和历史原子保存，历史与可丢弃缓存隔离且按数据集分开。
- [x] 3.3 在冻结请求、事务元数据和结构化结果中接入历史，覆盖单卡/批量改标、复核状态、现有单对象撤销；准确记录成功、部分失败、无变化与请求后取消。
- [ ] 3.4 按操作/事务 ID 幂等对账，实现提交后历史投影失败提示、启动中断恢复和待核实状态；不重复执行已提交标注。
- [ ] 3.5 兼容旧备份清单，保留可验证信息；缺少前后值/提交版本时禁用不支持的恢复，备份或原图缺失不删除历史。
- [ ] 3.6 添加存储、迁移、数据集隔离、重启保留、重复回调、复核状态回滚及提交后历史写入故障测试。

## 4. 阶段 B：历史面板与备份恢复

- [x] 4.1 新增可停靠/浮动历史面板与缩略图入口，分页列出时间、操作、数量和结果，支持按时间、文件、标签和结果筛选。
- [x] 4.2 实现逐对象展开、前后值、失败原因及按需当前原图预览，图片缺失显示文本信息；面板刷新不抢主卡片焦点。
- [ ] 4.3 接入永久身份显式定位和返回处理位置，覆盖对象退出旧筛选、对象删除/重复身份、未保存内容守卫，历史选择与普通改标候选隔离。
- [ ] 4.4 历史项绑定准确清单并复用文件级受保护恢复，展示文件范围，处理备份缺失、后续编辑冲突和部分恢复。
- [ ] 4.5 文件恢复生成关联历史并标记实际已恢复项，阻止重复消费；复用阶段 A 同步以保留阅读位置。
- [ ] 4.6 添加面板筛选、定位、书签不被伪造、文件恢复与重启查找测试；人工验收“一条批次可查到全部对象”，记录阶段 B 结果后进入 C。

## 5. 阶段 C：对象级字段撤回

- [x] 5.1 实现纯 Python 反向计划，为一次历史中的成功项逐对象生成 before/after 校验和恢复值，支持不同原标签及复核状态，排除无变化/失败/已撤回项。
- [x] 5.2 实现永久身份、当前字段、已知后续同字段操作及版本证据检查；允许有证据的无关几何/其他对象变化，证据不足和身份重复保守拒绝。
- [x] 5.3 label 反向计划接入现有事务引擎，保留备份、文件写锁、预检到提交版本检查和原子替换；逐对象冲突与逐文件失败准确隔离。
- [x] 5.4 复核状态撤回通过状态/历史共用事务执行，保留最新对象版本与复核状态有效性语义，不写标注 JSON。
- [x] 5.5 面板增加“撤回所选对象本次修改”，展示实际字段范围并确认，处理 dirty 状态，结果新增父操作关联并阻止重复撤回，不接管 Ctrl+Z 或添加自动重做。
- [x] 5.6 新增不同原标签、后续几何保留、同字段改后又改回、外部变化证据不足、重复/删除 ID、预检后文件变化及部分成功测试。
- [ ] 5.7 集成验证撤回/恢复后的当前画布、索引、缩略图、历史和刷新屏障一致，原记录仍可查，未成功项仍可解释和重试。

## 6. 文案、验收与规划闭环

- [ ] 6.1 增加中英文历史、位置降级、文件级恢复、字段撤回和待核实文案；按项目流程编译翻译资源，不手工编辑生成文件。
- [ ] 6.2 对修改模块运行适用格式/lint 和受影响测试，使用直接 conda 环境解释器与 Qt offscreen；不无理由重复全量测试。
- [ ] 6.3 用隔离数据集完成 A/B/C 联合人工验收：连续改标、无关自动保存、页外进入筛选、重启查历史、部分恢复和对象级纠错；记录通过证据与已知限制。
- [ ] 6.4 核对两份 delta specs 的场景覆盖及未归档变更冲突，运行 OpenSpec 严格校验；仅全部实施验收完成后勾完任务，本次规划不勾选实现项。
