## 1. 扩展 SQLite 对象索引

- [x] 1.1 新增不可变的缩略图对象引用与分页结果模型，定义图片路径、JSON 路径、稳定顺序、永久 Shape ID、标签和 bbox 契约。
- [x] 1.2 升级 `DatasetFilterIndex` schema，在可重建的 `shapes` 表中加入 Shape ID 与四个 bbox 字段，并补齐查询索引和旧缓存重建路径。
- [x] 1.3 扩展轻量 JSON 扫描，只从每个 Shape 提取索引字段和数值 points 的轴对齐 bbox，不加载完整根 JSON 或图片数据。
- [x] 1.4 实现按单标签统计与最多 100 行的稳定分页查询，并通过 `DatasetIndexController` 在 READY 状态暴露只读接口。
- [x] 1.5 增加 schema 重建、字段提取、非法 ID/bbox、稳定分页、同图多对象及控制器状态门禁的针对性测试。

## 2. 实现缩略图生成与缓存

- [x] 2.1 新增缩略图请求、结果、缓存键和 render generation 模型，确保旧标签、旧页面和已关闭窗口的结果可被确定丢弃。
- [x] 2.2 实现有界内存 LRU 与按数据集隔离的可丢弃磁盘缓存，缓存键覆盖图片 mtime/size、Shape ID、bbox 和目标尺寸。
- [x] 2.3 实现按可见性请求、按原图路径合并的后台生成器，使同一任务内一张原图只解码一次并裁剪多个 bbox；限制并发并去重在途请求。
- [x] 2.4 实现 bbox 向外取整、图像边界裁切、非正面积与图片读取失败的错误结果，且只在 UI 线程创建 `QPixmap`。
- [x] 2.5 增加缓存命中/失效、LRU 上限、同图合并解码、错误隔离、取消和过期 generation 丢弃测试。

## 3. 构建标签缩略图窗口

- [x] 3.1 新增可关闭、拖动、缩放并正确释放的独立顶层非模态窗口、分页模型和网格 delegate，使用占位项渐进替换缩略图，不为每个对象创建独立 QWidget。
- [x] 3.2 接入标签列表及对象计数、单标签选择、每页最多 100 项、稳定前后翻页和空结果状态。
- [x] 3.3 实现当前页对象多选、选中对象数/文件数摘要、错误项禁选以及标签或页面切换时清空当前选择。
- [x] 3.4 接入索引 missing/syncing/stale/failed 与 READY 空标签状态展示，在非 READY 状态隐藏旧结果、禁用提交，并在窗口内提供刷新/重建入口。
- [x] 3.5 增加 PyQt offscreen 测试，覆盖分页、选择、占位替换、错误项、快速切页、窗口关闭后的异步结果和真实顶层窗口生命周期。

## 4. 复用统一跨图对象改标流程

- [x] 4.1 从现有“已标记对象改标”入口提取可接收不可变 `MarkedObjectRef` 快照的统一协调方法，并保持原入口行为和测试兼容。
- [x] 4.2 将当前页选中项转换为完整对象键快照，复用目标标签选择、影响对象/文件数量，并在确认中明确提示立即写盘且不进入普通撤销栈。
- [x] 4.3 让缩略图来源调用现有 dirty 文件处理、对象 ID 预检、暂存、备份、原子提交、恢复清单和终态结果处理，禁止新增 JSON 写盘实现。
- [x] 4.4 将结构化结果回传窗口：成功、无变化或确认删除的对象退出选择，失败对象保留选择和错误状态以便检查或重试。
- [x] 4.5 增加缩略图快照到改标计划、取消零写入、当前文件未保存处理、部分失败状态保留和原对象改标入口的回归测试。

## 5. 完成 JSON—SQLite—视图刷新闭环

- [x] 5.1 对每个成功提交文件调用现有单文件索引刷新，并增加窗口可监听的刷新完成或延迟状态通知。
- [x] 5.2 合并一批文件的刷新完成通知后重新查询当前标签；处理页尾回退、成功对象退场和下一批补位，避免逐文件整页重载。
- [x] 5.3 当 JSON 已提交但 SQLite 刷新失败时将窗口转为 stale、禁用继续提交，并通过窗口内刷新/重建入口恢复。
- [x] 5.4 增加全部成功、部分失败、刷新延迟、刷新失败、批量通知合并及当前页缩短后的闭环测试。

## 6. 集成、翻译与验收

- [x] 6.1 在主标注界面增加单一“标签缩略图”动作和可复用窗口生命周期管理，关闭后清理对象引用并保持 `label_widget.py` 仅承担薄协调职责。
- [x] 6.2 为新增文案使用 Qt 翻译接口，更新 `.ts` 文件并运行 `scripts/compile_languages.py` 生成翻译资源。
- [x] 6.3 补充用户文档，明确 SQLite 是派生索引、每页最多 100 项、提交立即写盘且不进入普通撤销栈、恢复使用现有恢复清单。
- [x] 6.4 运行新增索引/缓存纯 Python 测试、缩略图窗口 offscreen 测试及现有 dataset-index/object-relabel 回归测试，并确认任务清单声称的场景均有对应断言。
- [x] 6.5 对受影响 Python 文件运行 Black、Flake8 和 `py_compile`，确认未引入多人协作、跨进程锁、对象删除、字段编辑或独立撤销功能。
