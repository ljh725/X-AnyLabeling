## 1. 身份不变量与纯逻辑

- [x] 1.1 定义文件内 Shape 身份规范化结果和结构化诊断，区分 missing、invalid、duplicate 及保留/重建位置。
- [x] 1.2 实现 O(n) 集合规范化器：保留首次出现的有效非空字符串，为缺失、非法和后续重复项生成不与已声明集合冲突的 UUID4 hex。
- [x] 1.3 实现只读集合验证器，能够报告空 ID、非法类型、重复 ID 及对应 Shape 数组位置。
- [x] 1.4 增加纯逻辑测试，覆盖历史非 UUID 字符串保留、缺失/非法修复、重复首项保留、生成器碰撞重试和大集合线性行为。

## 2. LabelFile 加载、保存与迁移

- [x] 2.1 将集合规范化器接入 `LabelFile` 加载，在 Shape 对 UI 和下游模块可见前建立非空唯一不变量。
- [x] 2.2 暴露或记录加载诊断，同时保证仅因旧文件补 ID 或冲突修复不会将文件标记为用户已修改。
- [x] 2.3 在 `LabelFile.save()` 写临时文件前运行只读身份预检，并通过包含冲突位置和 ID 的 `LabelFileError` 阻止歧义 JSON 落盘。
- [x] 2.4 增加 LabelFile 文件级测试，覆盖旧 JSON 加载、真实修改后持久化、保存—重载身份稳定以及保存前异常重复拒绝。
- [x] 2.5 验证保存失败仍保持现有原子写语义，不覆盖目标文件且不遗留临时文件。

## 3. Shape 生命周期入口

- [x] 3.1 审计所有 `Shape` 构造、反序列化、深复制和合并入口，并形成“恢复既有对象/创建新对象”的调用点清单。
- [x] 3.2 保证普通编辑、撤销/重做快照和删除后撤销沿用原 ID，并增加相应 Canvas 定向测试。
- [x] 3.3 保证 Duplicate、内部剪贴板和系统剪贴板粘贴为每个新对象生成当前文件内唯一 ID，并覆盖多选复制测试。
- [x] 3.4 修正导入为新对象、AI 生成及其他绕过标准构造器的入口，使其显式使用新对象身份语义并增加代表性测试。
- [x] 3.5 验证身份字段继续独立于 `group_id`、`flags`、`attributes` 和 `kie_linking`，几何、标签、属性与描述编辑不会改变 ID。

## 4. 下游引用与交换格式边界

- [x] 4.1 审计行为分析、Inspector、质检和虚拟复核中的 shape 下标、QA ID、运行时对象地址及持久 ID，标注其生命周期与是否写入持久输出。
- [x] 4.2 将需要跨编辑、保存或重载关联对象的引用改为 `xanylabeling_shape_id` 或 `(project_id, image_id, shape_id)`，保留并明确命名仅用于瞬时布局/匹配的 runtime 或 QA 身份。
- [x] 4.3 增加行为事件和持久复核数据的契约测试，确保不会把数组下标或 Python 对象地址写成跨生命周期 Shape 引用。
- [x] 4.4 扩充 VOC、YOLO、COCO 转换器边界测试，验证私有 ID 不导出、源 JSON 不修改且业务字段语义保持不变。

## 5. 文档与验证

- [x] 5.1 更新开发文档，说明身份作用域、完整对象键、旧文件惰性迁移、冲突诊断、生命周期矩阵和交换格式边界。
- [x] 5.2 运行 Shape 身份、LabelFile、Canvas 生命周期、行为引用和转换器边界的最小相关 pytest 集合并修复回归。
- [x] 5.3 对所有修改的 Python 文件运行 Black 79、相关 flake8 检查和 `py_compile`，确认没有手工修改生成资源文件。
- [x] 5.4 运行 `openspec validate harden-persistent-shape-identity --strict`，确认规范、场景和任务均可被 apply 工作流解析。
