## 1. 基线、契约与版本

- [x] 1.1 保存当前受支持形状、Canvas 用户入口、现有语义动作、零时长动作、未接入滚轮分支和分析包字段的匿名验收基线。
- [x] 1.2 扩展事件 schema、分析算法、工作流阶段、序列规则、时间分解和 bundle 契约版本常量，明确新版断点。
- [x] 1.3 扩展版本化动作目录字段，加入适用形状、入口、动作模式、开始条件、允许终态、目标、必需上下文、预期事件数和回放 ID。
- [x] 1.4 为全部支持形状和用户入口填写动作目录，显式标记不适用动作而不是留下隐式缺口。
- [x] 1.5 增加动作目录 schema 校验和双向覆盖测试，拒绝无目录入口、无回放条目和无实现条目。
- [x] 1.6 验证默认关闭时新增目录和适配层不改变现有 Canvas、标签、保存、undo 或切图行为。

## 2. 统一语义动作通知和记录上下文

- [x] 2.1 定义 Canvas 到 LabelingWidget 的结构化语义动作通知，覆盖 span 开始、终态提交和 burst 输入且不直接写日志。
- [x] 2.2 在通知中携带动作名、shape 引用、稳定 edit_target、input_source、动作 ID 和安全起止摘要。
- [x] 2.3 扩展 LabelingWidget 行为适配层，将 shape 引用转换为项目、图片、shape、episode 和功能状态身份。
- [x] 2.4 为每次动作建立唯一 action ID，并使重复结束、重复回调和旧低层通知保持幂等。
- [x] 2.5 扩展安全上下文构建器，稳定输出 shape_type、size_bucket、complexity_bucket、initial_source 和适用功能状态。
- [x] 2.6 为无活动对象的视图动作保留 image visit 归属和 `no_active_object` 原因，不静默丢弃。
- [x] 2.7 增加隐私白名单测试，证明通知、日志和导出不包含原始标签、完整 points、自由文本、绝对路径或鼠标采样坐标。

## 3. Canvas 通用几何动作覆盖

- [x] 3.1 复核并统一鼠标 press/release 的 `geometry_adjust` 开始、成功、no_change、cancelled 和 interrupted 终态。
- [x] 3.2 复用 Canvas 命中结果记录 `move`、`vertex` 和 `edge`，禁止 telemetry 或分析层根据坐标重新猜测目标。
- [x] 3.3 为 rectangle 的 left、right、top、bottom 和稳定角目标提供确定性映射。
- [x] 3.4 为 polygon、rotation、quadrilateral、point、line、circle、linestrip 和 cuboid 的适用移动、顶点和边编辑接入统一动作通知。
- [x] 3.5 验证多选、控制键选择框、边上加点、顶点删除和右键路径不会误发普通拖动动作。
- [x] 3.6 隔离旧 `shape_edited`、`shape_moved` 等兼容回调，禁止其与新版 ActionSpan 重复进入精确计数。
- [x] 3.7 增加全部支持形状的参数化 Canvas 回放，断言一次操作一个终态事件、对象归属和 shape_type 正确。

## 4. 滚轮、键盘、缩放和平移覆盖

- [x] 4.1 在矩形滚轮整体缩放分支接入对象 `rectangle_adjust` burst，并记录 `scale` 目标、输入次数和净变化。
- [x] 4.2 在矩形滚轮边缘调整分支接入 `rectangle_adjust` burst，并记录具体边、输入次数和净变化。
- [x] 4.3 将矩形复核滚轮微调、普通矩形滚轮编辑和画布视图滚轮按分支及修饰键确定性区分。
- [x] 4.4 统一方向键连续微调的对象身份、目标、输入次数、静默结束和 no_change 规则。
- [x] 4.5 扩展 zoom burst，记录开始比例、结束比例、输入次数、选中对象状态和可用的 size_bucket。
- [x] 4.6 扩展 pan burst，记录方向类别、输入次数、起止边界和图片或对象归属，不记录原始视口坐标。
- [x] 4.7 使 burst 绑定开始时的图片和对象身份，并在静默、对象切换、切图、失焦、模式冲突和关闭时可靠 flush。
- [x] 4.8 增加滚轮矩形调整、Ctrl+滚轮缩放、普通滚轮平移、键盘微调和边界 flush 的回放测试。

## 5. 新对象创建工作流

- [x] 5.1 增加 `creation_workflow_id` 及创建状态机，允许对象 shape ID 生成前记录创建意图和阶段。
- [x] 5.2 在 R 键或矩形创建命令入口开始 `r_then_label` 创建工作流并记录 intent 时间。
- [x] 5.3 在数字快捷键入口开始 `digit_prefill` 创建工作流，记录预置标签方式和到绘制开始的间隔。
- [x] 5.4 为手工几何绘制建立 create-draw span，覆盖开始、提交、取消、无效标签拒绝、切图和关闭中断。
- [x] 5.5 为 R 后标签对话框建立 create-label span，覆盖打开、确认、取消和验证失败，不保存标签文本。
- [x] 5.6 数字预置路径在无实际标签输入时不生成虚假 create-label span。
- [x] 5.7 在创建提交获得 shape ID 后，将临时工作流、创建阶段、`shape_created` 和后续精修绑定到同一对象工作流。
- [x] 5.8 为复制、粘贴、AI 和导入统一记录 `copy_paste`、`ai`、`import` initial_source 和创建事实。
- [x] 5.9 使取消创建幂等关闭临时工作流且不生成成功对象，不污染下一次创建方式。
- [x] 5.10 增加 R 后输入标签、数字预置、复制粘贴、AI、导入、取消和失败创建的受控回放测试。

## 6. 对象选择、标签、属性和生命周期

- [x] 6.1 保持已有对象只在用户真实单选时开始编辑 episode，并继续区分画布、列表、多选、程序同步和恢复来源。
- [x] 6.2 验证创建工作流不会因对象提交后的程序化选择产生第二个重叠 episode。
- [x] 6.3 实现统一标签编辑会话，从 focus-in、对话框打开或编辑命令开始，在确认、失焦提交、取消或中断时结束。
- [x] 6.4 将连续 textChanged 更新收敛为一个 `label_edit` 终态事件，并记录真实耗时与 no_change。
- [x] 6.5 为 group_id、difficult、visibility 和其他支持属性保留用户 guard、净变化摘要和当前对象身份。
- [x] 6.6 在对象切换、取消选择、删除、切图、项目关闭和异常中断前按顺序结束标签 span、burst、创建流程和对象 episode。
- [x] 6.7 验证失焦和空闲只切分 activity segment，不错误增加对象返回次数或创建新 episode。
- [x] 6.8 增加标签多字符编辑、取消、程序化刷新、属性提交、对象切换和生命周期终止的 PyQt offscreen 测试。

## 7. 纯 Python 对象工作流时间线

- [x] 7.1 定义隐私安全的工作流、动作段、间隔、时间层、完整性和排除原因数据模型。
- [x] 7.2 实现按 app session、sequence_no、monotonic time 和 event ID 的确定性事件排序。
- [x] 7.3 实现已有对象选择型工作流重建，使用显式选择和结束事实确定边界。
- [x] 7.4 实现创建型工作流重建，将 creation workflow token 在提交后关联到对象身份和后续动作。
- [x] 7.5 将图片级无对象 zoom/pan 和操作前间隔保留在 image visit 时间线上，不错误归到后选对象。
- [x] 7.6 为 incomplete、cancelled、unclosed、legacy 和超长轮次保留结构化完整性状态及排除原因。
- [x] 7.7 使 timeline builder 成为对象摘要、序列、瓶颈和代表轨迹的共享事实输入，移除各统计函数的重复边界猜测。
- [x] 7.8 增加 A→B→A、创建后精修、同毫秒选择、无对象缩放、多选、切图和未闭合范围的时间线测试。

## 8. 时间层、间隔和守恒

- [x] 8.1 实现 focus/idle 状态机，将 false 恢复事件正确关闭失焦或空闲区间。
- [x] 8.2 将动作区间裁剪到工作流边界并求半开区间并集，保留动作事实但避免时间贡献重复计算。
- [x] 8.3 计算 wall、focused、active 和动作并集四层时间，缺失边界时按依赖维度降级而非补造。
- [x] 8.4 将 active 剩余时间确定性分类为 pre_action_gap、inter_action_gap 和 unattributed，并记录规则版本。
- [x] 8.5 计算 image_to_first_operation 和 target_search_gap 图片级间隔，确保不重复计入多个对象。
- [x] 8.6 为每个完整对象工作流和项目范围计算守恒校验、误差值和失败原因。
- [x] 8.7 修正现有 idle true 延伸到 episode 结束、恢复后仍算等待的问题并增加回归测试。
- [x] 8.8 增加重叠动作、相邻动作、短间隔、长间隔、失焦恢复、空闲恢复和隔夜停顿的区间代数测试。

## 9. 工作流阶段、序列和瓶颈指标

- [x] 9.1 扩展版本化阶段映射，覆盖图片操作前、目标查找、创建意图、创建几何、创建标签、导航、几何、标签属性、AI、质检、返工、保存、间隔、等待和未归属。
- [x] 9.2 实现 `object_workflow_summary`，输出每轮身份、类型、创建方式、shape 上下文、时间层、各阶段时间、缩放、修改、保存、返工和完整性。
- [x] 9.3 实现对象身份级累计摘要，区分 episode 数、返回次数、创建方式、累计 active、阶段构成和返工时间。
- [x] 9.4 重写 3 至 6 阶序列耗时，使每次窗口只累计自身动作和允许的内部间隔。
- [x] 9.5 增加序列时间规则测试，证明多窗口不会重复复制整轮耗时并保持确定性排序。
- [x] 9.6 实现动作和阶段的非重叠项目时间贡献，输出 total、share、cumulative share、count 和 object coverage。
- [x] 9.7 实现 `project_bottlenecks`，输出单对象 median、P75、P90、P95、返工时间、样本量、覆盖率和稳定排名。
- [x] 9.8 对工作流类型、创建方式、shape_type 和 size_bucket 提供低基数分层瓶颈事实，样本不足时显式不可用。
- [x] 9.9 禁止本地统计生成视觉辨识、犹豫、功能因果或效率损失标签，并用输出契约测试固定中性措辞。

## 10. 全量导出和兼容契约

- [x] 10.1 新增 `object_workflow_timeline.jsonl` 固定 schema、字段顺序和每轮一行写入器。
- [x] 10.2 新增 `object_workflow_summary.csv` 固定列和 null/排除原因序列化规则。
- [x] 10.3 新增 `project_bottlenecks.csv` 固定列、排名和版本字段。
- [x] 10.4 修正 `object_episodes.csv` 的 actions、started/ended、wall/focused/active 和 action_duration 字段与实际行结构不一致问题。
- [x] 10.5 保留 representative traces 的有界选择，但明确其为补充样本并禁止作为全量统计输入。
- [x] 10.6 在 manifest 记录新增文件 schema、算法版本、全量行数、排除数、截断或分片状态、大小和 SHA-256。
- [x] 10.7 为超长单轮时间线实现显式分片或可审计截断引用，禁止用静默丢行满足包大小限制。
- [x] 10.8 扩展 bundle 自检，验证 manifest 行数、CSV/JSONL schema、对象覆盖、哈希和原子发布。
- [x] 10.9 实现 v1-v4 日志只读兼容和新版能力级降级，旧日志缺失的创建、scale 或 span 时间保持 null。
- [x] 10.10 增加混合版本、取消导出、写入失败、已存在有效包和隐私净化的导出回归测试。

## 11. 性能、质量与端到端验收

- [x] 11.1 扩展 1 千、10 万和 100 万事件生成夹具，包含全部形状、两种手工创建、滚轮、标签编辑、重叠动作和空闲恢复。
- [x] 11.2 基准时间线构建、区间分解、全量 JSONL/CSV 写入、峰值内存、文件大小和取消响应。
- [x] 11.3 验证大范围导出仍在后台执行，UI 保持响应且取消不发布半成品分析包。
- [x] 11.4 运行目录驱动的全部语义动作回放，要求适用入口覆盖率 100%、一次性终态通过率 100% 和必需对象引用缺失为 0。
- [x] 11.5 验证支持 span/burst 的动作耗时覆盖率至少 95%、完整对象轮次闭合率至少 99% 和等价低层重复率低于 5%。
- [x] 11.6 验证全量时间线有效轮次覆盖率 100%、每轮及项目时间守恒通过率 100%，任何排除均有结构化原因。
- [x] 11.7 用固定回放验证 R 后输入标签与数字预置创建的阶段耗时、缩放上下文和后续精修归属。
- [x] 11.8 用匿名实测日志对比旧版和新版动作数、零时长率、未归属率、对象覆盖和 bundle 大小，记录版本断点结论。
- [x] 11.9 运行行为分析纯 Python 定向测试、PyQt offscreen 集成测试、相关 py_compile、Black 和 scoped flake8。
- [x] 11.10 更新行为分析说明、导出文件字典、时间口径、隐私说明和“外部大模型只解释事实”的使用文档。

## 12. 最终检查和实施交付

- [x] 12.1 对照两个规格逐条建立 requirement-to-test 追踪表并确认所有场景已有自动或受控回放证据。
- [x] 12.2 检查脏工作树并确认只修改本变更范围文件，保留所有无关用户改动。
- [x] 12.3 运行 OpenSpec strict validation、任务状态检查和最终定向验收，记录无法运行的环境限制。
- [x] 12.4 使用一个包含编辑、R 创建、数字创建、缩放、滚轮调边、标签修改和返工的完整样例导出三份新文件并人工核对。
- [x] 12.5 确认本地确定性输出在完全不使用外部大模型时仍能给出对象时间线、阶段构成和项目瓶颈排名。


## 实施验收记录

- 已完成事件目录、Canvas/LabelingWidget 语义动作适配、创建工作流、对象时间线、时间分解、序列与瓶颈导出。
- 已完成 v1-v4 只读兼容、隐私净化、代表轨迹补充样本声明、v4 manifest 文件哈希/大小/截断状态。
- 定向纯 Python 回归 123 passed；新增时间线/目录/导出契约 40 passed；定向 Qt 回归 87 passed，修复 fake widget 兼容回归后再通过 15 passed。
- 完整 tests 回归为 1198 passed、16 failed；失败项为仓库已有 settings schema 计数基线与轻量 fake widget。fake widget 失败已修复并单独复测通过；settings schema 失败来自本变更之外的既有工作树改动，未覆盖或回滚。
- 已通过 OpenSpec strict validation。
