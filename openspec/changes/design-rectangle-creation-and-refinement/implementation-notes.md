# 实施记录

- 实施前备份：`codex/backup-rectangle-workflow-20260908`，提交 `ce65c073125384d612802c1f3337d6fb735525c4`。采用独立 Git index，未切换当前分支或改变原暂存区。Git 无法读取的 `.pytest_tmp_remove_nested` 临时目录未纳入。
- 备份包含实施前 Git 可见源码、翻译及未提交设计；当前工作区的既有缩略图与标签脚本改动保留。
- 旧精修组件继续提供几何、增益和反馈。本变更补齐显式活动边、键盘轴向授权、500ms 分段、最新撤销快照及鼠标拖动/键盘选择状态分离；旧变更的人工验收任务没有被自动勾选。
- 矩形约束沿用现有编辑器：x/y 范围分别为 0..width-1、0..height-1，最小宽高 1px；保留浮点坐标。普通矩形创建仍沿用已有身份和分组流程。
- UI 动作均有可见按钮，并加入快捷键设置，不预占数字标签键。新工作台及按对象放大默认关闭。
- 真实数据：用户指定的课堂动作数据，1,544 份 JSON，31,204 个矩形满足精修范围，834 个矩形越过图像边界。越界框不自动更改。
- 已抽取 150 个不同源图片上的对象，五条件各 30；现有标签为对照，视觉难度和边界口径等待人工确认。
- 150 个抽样框通过四极值坐标往返检查，真实窗口的单边微调/撤销和中文文案检查通过；源标签哈希校验保持不变。
- 工作台文字反馈放在图像外，避免沿用旧 HUD 在高倍下遮挡轮廓。截图位于 `artifacts/rectangle_workflow_trial/workbench.png`。
- 自动化工程验收与人工速度/质量/疲劳验收分开记录。人工数据尚未采集，不能将运行测试耗时视为标注效率。

## 人工试点执行约定

使用 `trial.tsv`，按清单交错顺序先练习后测量。新建组比较两点与四极值；修正组比较拖边、步进、步进加对象放大。修正实验先在独立副本建立并记录相近难度的初始偏移，三组使用同一偏移分布；不对源标签施加扰动。

计时从定位/进入对象开始，到明确完成，包含选边、缩放、标签确认和返工。填写耗时、返工、误选边、疲劳、最终框和独立质量结论。清晰/模糊/遮挡由人工标记，不根据 difficult 字段猜测。原图容差及可见框/完整框口径需先确定。

待真实试用数据齐全后，分场景比较质量合格率、耗时中位数/P75及疲劳。当前证据仅支持工程功能与兼容性通过，不能宣布达到设计中的 15% 效率目标。

## 工程验收结果（2026-09-08）

- 最终定向回归：149 passed，4 warnings，47.50s；日志见 artifacts/rectangle_workflow_trial/final-tests.log。覆盖创建/修边、撤销、视图、设置及既有精修兼容。
- 修改文件通过 Black 格式检查；新增模块、测试和相关小模块通过 Flake8。LabelWidget 中既有 F841/F541/C901 及 runtime_applier.apply_change 既有复杂度 21 未在本次改动中扩大，也未计为全部 lint 通过。
- git diff --check 和 OpenSpec strict 校验通过；四语言翻译资源已重新编译。
- 设置 schema 测试取消已过时的固定总数断言，改为唯一键、配置覆盖和必要入口校验；保留现有 Appearance 分类。
- 工程任务 1.1–5.4 全部完成，整体 23/26。6.1–6.3 保持未完成：边界口径及原图容差未确认，人工操作时间、疲劳和独立质量结果尚未采集。
