## 1. 依赖和交互入口核对

- [x] 1.1 对照当前代码与旧精修变更，登记活动边授权、方向键轴向、撤销、增益、反馈和视图任务的唯一归属及已通过证据。
- [x] 1.2 核对现有动作、数字标签键、输入焦点和 viewport 契约，确定新增可配置动作的无冲突入口，不改变普通两点默认行为。
- [x] 1.3 明确测试用图像边界和最小矩形尺寸规则、shape 创建身份/分组规则及重画非法矩形入口。

## 2. 公共矩形修正流程

- [x] 2.1 建立仅面向唯一普通矩形的显式修正状态和四个选边按钮，接通画布点击、Tab/Shift+Tab 与活动边锁定。
- [x] 2.2 在现有步进组件上补齐显式授权、轴向限制、1px/5px、滚轮累计与 Ctrl 缩放路由，保证模式内不误回落为整框移动。
- [x] 2.3 接通精修/粗调拖动、原边参照、位移和步长反馈，以及拒绝越界、翻转或最小尺寸违规的提示。
- [x] 2.4 衔接步进合并、拖动撤销、Esc 分层取消和完成修正；核对输入框焦点与按钮返回画布焦点。
- [x] 2.5 接通下一个对象的既有保存/导航流程，覆盖失败或取消、跨图身份、旧活动边和滚轮累计清理。
- [x] 2.6 完成对应纯逻辑与 Qt 定向测试：单轴不变性、不同缩放下步长、hover 不授权、空活动边、撤销分组和焦点优先。

## 3. 四极值创建流程

- [x] 3.1 新增独立草稿控制器，完成上右下左顺序、单轴坐标、候选合法性、回退、取消和完成状态，避免依赖正式点 shape。
- [x] 3.2 增加四极值工具入口、步骤提示、临时点与边预览，验证一次点击仅输入一次且相邻同位置极值可用。
- [x] 3.3 接入普通矩形创建、预选标签、缺标签确认与取消后保留草稿，保证只创建一个 shape 和一个创建撤销单元。
- [x] 3.4 接通提交后的选中新框、公共修正及同标签继续新建；补齐草稿 Ctrl+Z/Backspace/Esc、切工具和切图生命周期。
- [x] 3.5 完成草稿控制器及 Qt 集成定向测试，覆盖无效输入、浮点坐标、草稿不持久化、标签取消、创建与修正撤销分离。

## 4. 观察与连续对象处理

- [x] 4.1 复用 viewport 控制层实现以指针原图坐标为锚点的局部聚焦，验证草稿各阶段缩放不改已确认坐标。
- [x] 4.2 实现默认关闭的按对象放大选项，按目标长边、倍率上限及上下文范围计算视图，并显示实际比例。
- [x] 4.3 实现查看周围/恢复入口和单会话快照，保证修边、切边和提交不自动重聚焦，手动视图优先且不跨图恢复旧快照。
- [x] 4.4 核对局部放大镜的观察职责与现有显示开关，验证仅观察不引发标注 dirty、几何撤销或源图修改。
- [x] 4.5 完成视图定向测试，覆盖边缘目标、超大/超小框、留白受限、手动干预、切图失败和成功后的清理。

## 5. 集成与使用说明

- [x] 5.1 接通可配置动作、默认关闭的新入口/观察选项与用户设置兼容，新增 UI 文案完成翻译及资源编译。
- [x] 5.2 验证新框及修正结果的保存重载、身份/Group ID 保持，以及 Inspector、缩略图沿既有刷新时机获得最新几何。
- [x] 5.3 补充精简操作说明，区分已提交步进与未提交拖动、完成修正与质检通过、清晰边界与不可判断边界。
- [x] 5.4 运行受影响测试文件与必要格式/lint 检查，记录结果；验证关闭新入口后普通两点和原有编辑流程可继续使用。

## 6. 人工效率与质量试点

- [ ] 6.1 在试点前锁定数据集边界口径、容差、分层样本、训练与交错顺序，以及设计中总耗时和质量指标定义。
- [ ] 6.2 新建框比较两点和四极值；已有框比较拖边、单边步进及步进加对象放大，记录中位数/P75、返工、误选边、疲劳和未完成原因。
- [ ] 6.3 按独立质量复核和拟定效率门槛给出各场景结论，校准显示参数；证据不足时保持可选，不把试点目标写成已实现收益。

## 7. 四区域点击修边（cls3-120/130）

- [x] 7.1 实现无 Qt 的有限四区域判定、对角线窄带及定向几何测试。
- [x] 7.2 接入选中矩形的悬停预览、按下锁定、释放提交、拖动晋级、撤销和生命周期清理。
- [x] 7.3 同步四语言提示、设计/实现/使用说明和分区点击规格。
- [x] 7.4 完成 Qt 事件与既有矩形操作回归、格式与 lint 检查，记录验证结果。（新增 63 + 原有回归 124 passed；7 个 Python 文件 Black/Flake8、OpenSpec 严格校验通过；详见 cls3-130 r3。）
