## 1. 共享基线、版本与配置

- [x] 1.1 固定现有 v1/v2 事件、月度 JSONL、统计结果和数据包的回归夹具，并记录当前受影响测试的通过基线。
- [x] 1.2 增加相互独立的事件 schema、分析算法、工作流阶段映射、返工规则、分位数规则和数据包契约版本常量。
- [x] 1.3 为小时批写、刷新间隔、过渡间隔、紧凑包行数/字节上限、代表轨迹上限和双范围最小样本量增加默认配置与配置校验。
- [x] 1.4 建立 v2 月度、v3 小时、v2/v3 混合、序号缺口、manifest 缺失、重叠动作和双范围对照的固定测试夹具。
- [x] 1.5 更新隐私字段白名单与净化计数契约，验证图片像素、完整 points、自由文本和绝对路径在记录与导出两层都不可出现。

## 2. 第一阶段：工作流覆盖契约与事件 schema

- [x] 2.1 将现有覆盖矩阵升级为版本化、机器可校验的动作目录，字段包含工作流、入口、动作、开始条件、允许终态、上下文、预期事件数和回放用例。
- [x] 2.2 补齐手动创建、几何编辑、标签属性、AI 创建与修正、Inspector/质检、导航、保存、删除、撤销/重做和异常退出的覆盖行。
- [x] 2.3 让行为分析文档矩阵与机器动作目录共享或校验同一事实来源，并增加漂移检测测试。
- [x] 2.4 扩展事件 schema，加入应用会话内 `sequence_no`、记录缺口事实、标准终态、中断原因和必要的时间/关联字段。
- [x] 2.5 更新事件目录、payload 白名单、序列化、反序列化和严格校验，使 v3 必需字段按事件类型验证且旧 schema 继续可读。
- [x] 2.6 在遥测协调器中实现应用会话内严格递增序号分配，并验证正常事件、拒绝入队事件和恢复写入时的序号行为。
- [x] 2.7 增加参数化契约测试，逐行证明一次用户操作只产生一个最终可计数动作且必需上下文完整。

## 3. 第一阶段：语义动作、轮次与时间边界

- [x] 3.1 审计全部现有行为接入点并生成“覆盖矩阵行 → 实际入口 → 测试”的可追踪清单，标出缺失、重复和旧低粒度路径。
- [x] 3.2 统一整体移动、矩形边/角、关键点和其他几何编辑的 ActionSpan 开始、success/cancelled/no_change/interrupted 终态及净变化摘要。
- [x] 3.3 统一键盘微调、滚轮缩放和视图平移的 burst 合并，并验证静默、对象切换、图片切换、失焦和关闭边界。
- [x] 3.4 接入标签、属性、group_id、创建、删除、撤销、重做、AI、Inspector、质检、保存和模式切换的语义动作与结果。
- [x] 3.5 移除或隔离会重复计数的 `shape_edited` 等旧低层路径，保留旧事件兼容适配但禁止其进入 v3 精确耗时。
- [x] 3.6 完善应用/项目/图片访问/对象/episode 身份和对象切换、取消选择、删除、切图、项目关闭、异常退出的幂等闭合规则。
- [x] 3.7 完善 focused/idle/active segment 与系统等待边界，验证空闲恢复不增加返回次数且墙钟跳变不影响单调耗时。
- [x] 3.8 补齐安全上下文分桶、功能实际参与、撤销/重做关联、AI 初始来源和保存后再编辑所需的关联事实。

## 4. 第一阶段：小时 JSONL、批量写入与 manifest

- [x] 4.1 将新事件路由改为 UTC `events-YYYY-MM-DD-HH.jsonl`，保证无事件小时不创建空分片且旧月度文件不再被追加。
- [x] 4.2 将后台消费者改为有界队列批量读取、活动句柄复用和按批次/间隔刷新，保持记录调用非阻塞。
- [x] 4.3 实现跨小时轮换、关闭时限时 flush、重复关闭幂等和迟到/墙钟回跳事件的受控追加策略。
- [x] 4.4 实现分片统计累积器，收集事件数、实际时间范围、按应用会话序号范围、丢弃/错误、schema 分布和文件大小。
- [x] 4.5 使用临时文件和原子替换发布小时 manifest，并在完成分片上计算 JSONL SHA-256。
- [x] 4.6 在队列拒绝或写入恢复时持久化 recording_gap 或等价 manifest 会话段，确保丢失不会只停留在内存状态。
- [x] 4.7 实现 manifest 缺失、陈旧、校验不一致和当前活动分片的只读诊断与可重建路径。
- [x] 4.8 为批量边界、跨小时、跨 UTC 日期、系统时钟回跳、写入失败、崩溃残留和关闭超时增加纯 Python 测试。

## 5. 第一阶段：保留期、兼容与运行状态

- [x] 5.1 将保留期清理改为删除完整过期且非活动的小时分片及其 manifest，边界小时不逐行重写。
- [x] 5.2 扩展文件发现和格式识别，使旧月度 JSONL 与新小时 JSONL 可在同一目录只读共存且不会被自动迁移或改写。
- [x] 5.3 在记录状态中公开启用、当前分片、已接收、已写入、已丢弃、编码/写入错误、关闭超时和最近错误。
- [x] 5.4 更新本地行为分析界面与配置迁移，显示记录/存储状态、隐私边界和保留期，不在 UI 中加入上传或模型入口。
- [x] 5.5 增加混合目录、重复 event_id、过期清理、只读旧文件和记录开关运行中切换的回归测试。

## 6. 第一阶段：受控回放与质量门禁

- [x] 6.1 建立覆盖整体移动、边/角调整、关键点、微调、标签/属性、创建/删除、撤销/重做、AI、质检、保存和 A→B→A 的端到端回放集。
- [x] 6.2 为失焦、空闲、图片加载失败、切图、项目关闭、运行中开关记录和异常终止增加边界回放。
- [x] 6.3 计算并验证受支持动作耗时覆盖率 ≥95%、episode 闭合率 ≥99%、等价低层重复率 <5% 和必需引用缺失为 0。
- [x] 6.4 对启用/关闭记录执行 UI 延迟、队列、内存和日志增长基准，确认批写没有引入超出既有门槛的回归。
- [x] 6.5 在第一阶段门禁中验证默认关闭、本地运行、无网络/模型调用、隐私白名单和任何丢失均可观测后，再允许切换新写入路径。

## 7. 第二阶段：范围模型与分片预筛选

- [x] 7.1 定义可序列化的单范围和双范围过滤契约，明确 UTC 闭区间、本地时区转换、请求范围和实际范围字段。
- [x] 7.2 实现当前项目、最近 1/7/30 天、自定义时间和全部本地数据的范围构造器，并在无活动项目时拒绝当前项目导出。
- [x] 7.3 实现小时分片名解析和范围求交，只选择可能相交的小时文件，并为旧月度文件保留月级候选逻辑。
- [x] 7.4 在首尾小时、旧月度文件和时区/DST 边界逐事件精确过滤，验证包含起止边界且不重复或遗漏事件。
- [x] 7.5 在新旧来源重叠时按 event_id 去重并输出来源重复计数，保证源文件始终只读。
- [x] 7.6 为跨小时、跨日、跨月、空范围、反向范围、无当前项目和双范围不重叠约束增加测试。

## 8. 第二阶段：流式读取、重放与测量质量

- [x] 8.1 增加逐事件生成器和批次回调接口，替代 UI 导出路径中“读取全部事件后统一排序”的强制流程。
- [x] 8.2 按 application_session_id + sequence_no 流式校验会话内顺序，并对旧 schema 输出 legacy ordering 质量事实。
- [x] 8.3 扩展读取质量分类，覆盖 parse、schema、required-field、reference、time、terminal、state-version、sequence-gap、manifest 和 duplicate-source。
- [x] 8.4 将 feature state、ActionSpan、ObjectEpisode、ActivitySegment、保存和返工事实重放改为可增量消费的有限状态机。
- [x] 8.5 重写统计聚合调用路径，避免按 episode 反复扫描全部事件和按序列窗口重复求和等非线性热点。
- [x] 8.6 实现两遍式代表轨迹流程：第一遍只保留候选 ID 和排名摘要，第二遍重新扫描相交分片提取有限事件。
- [x] 8.7 用固定小数据集证明流式与现有批量算法在适用指标上结果一致，并验证缺失字段只影响依赖指标。

## 9. 第二阶段：时间分解与确定性统计

- [x] 9.1 实现半开单调时间区间的裁剪、排序、并集、交集和差集工具，并覆盖重叠、相邻、空区间和异常边界。
- [x] 9.2 从重放状态构造每个 episode 的 wall、focused、active、action、system_wait、transition_gap 和 unattributed 区间。
- [x] 9.3 计算并输出 `attributed_action_ms`、`system_wait_ms`、`transition_gap_ms`、`unattributed_active_ms` 和比例，验证总量守恒且非负。
- [x] 9.4 建立版本化工作流阶段映射，覆盖导航、创建、几何、标签属性、AI修正、检查质检、返工、保存、等待和未归属。
- [x] 9.5 扩展动作与阶段聚合，输出次数、对象覆盖数、结果率、耗时覆盖率、total、mean、median、P75 和 P95。
- [x] 9.6 生成 `time_contribution` 事实表，计算稳定排名、时间占比和累计占比，但不产生瓶颈标签或自然语言建议。
- [x] 9.7 扩展返工聚合，统一撤销/重做、反向调整、创建/修改后删除、保存后再改、重复返回和 no_change 的规则版本与耗时。
- [x] 9.8 重构 episode、object 和 image 汇总以支持流式状态及高基数上限，并输出总数、代表数和 other 汇总。
- [x] 9.9 扩展 measurement_quality，加入序号连续性、manifest 完整性、未归属率、旧顺序占比、截断和各表有效样本/排除原因。
- [x] 9.10 使用重叠动作、系统等待、短过渡、空闲、异常长轮次和未闭合轮次夹具验证全部时间口径。

## 10. 第二阶段：优化前后双范围事实对照

- [x] 10.1 让基线和对照范围复用完全相同的 schema 采用、算法、阶段映射、返工规则、分位数和质量门槛配置。
- [x] 10.2 输出两组的事件/episode/对象样本量、覆盖率、动作与阶段时间、吞吐、返回、撤销、返工和未归属事实。
- [x] 10.3 按 shape 类型、尺寸、复杂度、初始来源、功能状态和工作流类型生成有最小样本门槛的低基数分层对照。
- [x] 10.4 计算可复算的绝对差值和相对变化，对非随机数据固定标记 observational，对缺组或样本不足输出结构化不可比较原因。
- [x] 10.5 用任务构成相同、任务构成偏移、单组、样本不足、覆盖率不足和零基线夹具验证对照结果与空值约定。

## 11. 第二阶段：紧凑数据包、后台导出与界面

- [x] 11.1 定义新版 bundle schema 和固定字段顺序，包含 manifest、质量、动作、阶段、时间贡献、episode/object/image、返工、对照和代表轨迹文件。
- [x] 11.2 对高基数表实现确定性代表行、`other` 汇总、行数/字节上限和总数/导出数/省略数记录。
- [x] 11.3 扩展代表轨迹选择，覆盖 common、median、P75、longest、rework、failure、unattributed_anomaly 和 measurement_anomaly，并实施分类及总字节上限。
- [x] 11.4 在导出层再次执行字段白名单、匿名化、净化/截断计数，并为每个文件生成大小和 SHA-256。
- [x] 11.5 使用目标父目录内唯一临时目录写包并原子发布，验证成功、失败、取消和目标已存在都不覆盖已有结果。
- [x] 11.6 实现行为分析导出后台 worker，提供分片/批次进度、取消令牌、错误和最终质量结果信号。
- [x] 11.7 扩展导出界面以选择预设/自定义/当前项目和双范围，显示明确本地时区、数据质量与“仅导出事实、不调用 AI”说明。
- [x] 11.8 将 `label_widget.py` 的同步全量导出入口切换到 worker，确保取消检查贯穿读取、重放、聚合、二次扫描和发布。
- [x] 11.9 删除或禁止任何模型调用、上传、提示词和自动自然语言结论路径，并增加静态/运行时边界测试。

## 12. 全链路性能、验收与文档

- [x] 12.1 建立可重复的 1 千、10 万和 100 万事件生成夹具，覆盖多会话、多小时、多对象、返工和双范围分布。
- [x] 12.2 基准分片预筛选、流式读取、聚合、二次轨迹扫描和写包的耗时、峰值内存、打开文件数及取消响应，并保存验收阈值。
- [x] 12.3 验证 100 万事件导出不在 UI 线程执行、界面保持响应、取消在规定批次内生效且峰值内存不随事件总数线性持有事件对象。
- [x] 12.4 对同一输入连续导出两次，验证除允许的生成时间字段外所有 CSV/JSON 行、排序、分位数、截断选择和校验值确定一致。
- [x] 12.5 运行行为分析相关纯 Python、Qt offscreen、混合格式、隐私、性能和端到端测试，并修复所有新增回归。
- [x] 12.6 对修改文件运行 Black、Flake8 和必要的编译检查；仅在跨模块回归需要时扩大 pytest 范围。
- [x] 12.7 更新用户文档，说明第一阶段记录内容、小时存储、保留期、第二阶段文件契约、质量解释、未归属时间和外部分析边界。
- [x] 12.8 建立“规格 Requirement/Scenario → 任务 → 测试”的追踪表，并确认两个能力的全部场景都有自动测试或明确人工验收步骤。
- [x] 12.9 执行 OpenSpec 严格校验和最终隐私/无 AI 审计，确认所有产物与实现任务已准备进入 apply 阶段。



















