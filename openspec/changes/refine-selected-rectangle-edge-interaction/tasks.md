## 1. 基线与测试夹具

- [x] 1.1 运行 `tests/test_rect_edge_alignment.py`、`tests/test_rect_edge_state_models.py` 和 `tests/test_rect_edge_canvas_semantics.py`，记录变更前基线
- [x] 1.2 为矩形边命中测试补充统一的屏幕尺寸、zoom、epsilon 和指针位置构造 helper
- [x] 1.3 增加“未选中矩形不得产生边候选、唯一选中矩形可以、多选不得产生”的失败测试
- [x] 1.4 增加“重叠未选中矩形仍按现有小目标优先选择”的回归测试
- [x] 1.5 增加“边 press 和 drag 全程保持当前矩形正式选中”的失败测试

## 2. 大框与小框边命中策略

- [x] 2.1 将鼠标矩形边候选输入收窄为当前唯一选中的 rectangle
- [x] 2.2 增加矩形图像宽高到屏幕宽高的换算，并以 `min(width, height) < 3 * epsilon` 判定小矩形
- [x] 2.3 普通矩形保留距有限边段不超过 `epsilon` 的边界内外双向命中
- [x] 2.4 小矩形按 left/right/top/bottom 分别增加外侧半平面过滤，禁止框内位置产生边候选
- [x] 2.5 确保边候选投影只落在对应有限边段，端点附近不因无限延长线误命中
- [x] 2.6 复用当前选中矩形的原生 `nearest_vertex(..., epsilon)` 排除角点，不新增角点半径
- [x] 2.7 将重叠边的距离排序限定在当前选中矩形内部，移除跨 shape 的面积和栈顺序边排序
- [x] 2.8 增加普通框、小框、细长框和恰好位于 `3 * epsilon` 分界值的命中边界测试

## 3. 鼠标事件仲裁

- [x] 3.1 调整 hover 路径，只在功能开启、编辑模式和唯一选中 rectangle 条件满足时计算边候选
- [x] 3.2 调整 press 重验证，确保 hover 引用仍属于当前唯一选中矩形且几何有效
- [x] 3.3 移除有效边 press 时取消对象选择的行为，并保持列表、属性面板和 Canvas 选择状态一致
- [x] 3.4 保留现有 pending 状态，用于在 press 时锁定边并区分原地单击与实际移动
- [x] 3.5 未发生位置变化的边单击 release 只清理 pending、保持选择，不创建 undo
- [x] 3.6 pending 后锁定 press 时确认的边，拖动期间不得因经过其他角点、边或 shape 切换目标
- [x] 3.7 未命中有效边时无副作用地回落到现有对象选择、原生角点或整体移动路径
- [x] 3.8 保留活动边拖动的高优先级 move guard，避免画布平移和 hover 重算吞掉实时预览

## 4. 几何事务与状态清理

- [x] 4.1 复用现有单边坐标应用、图像边界、最小宽高和反翻转约束
- [x] 4.2 正常 release 仅在几何实际变化时提交一次 undo 和 dirty 状态
- [x] 4.3 Esc、窗口失焦、窗口停用和鼠标抓取丢失时恢复 drag start points 且不提交编辑
- [x] 4.4 正式选择切换、进入多选、切图、进入绘制模式、shape 删除或隐藏时清理旧 hover、pending 和 active edge
- [x] 4.5 功能关闭时按事务状态取消或回滚活动手势，并清理全部鼠标边状态
- [x] 4.6 保持现有键盘 active edge 行为；仅当当前唯一选中矩形产生有效鼠标边 hover 时执行鼠标接管
- [x] 4.7 验证精修控制模式只缩放拖动 delta，不改变 epsilon、大小分类或外侧命中
- [x] 4.8 验证稳定预览继续观察当前选择/活动边，不参与边候选仲裁

## 5. 视觉反馈与功能入口

- [x] 5.1 仅对当前可执行的单边候选绘制预高亮，禁止未选中矩形显示边编辑反馈
- [x] 5.2 为水平边和垂直边设置与实际调整方向一致的 resize 光标，并保证 hover 与 press 结果一致
- [x] 5.3 pending、dragging、取消和对象切换时同步更新边高亮与光标，避免残留反馈
- [x] 5.4 保留现有 action、快捷键和配置标识，将开关行为收窄为“启用已选中矩形单边调整”
- [x] 5.5 将用户可见名称调整为“矩形单边调整”，更新中英文翻译源并重新生成翻译资源
- [x] 5.6 功能关闭时验证原生对象选择、角点调整和整体移动与变更前一致

## 6. 自动化验证

- [x] 6.1 补充原生角点优先于边、其他 shape 顶点不抢占当前选中矩形边的测试
- [x] 6.2 补充小矩形框内移动路径不被边接管、框外四边均可调整的测试
- [x] 6.3 补充普通矩形四边内外均可命中且中央区域继续走原生移动的测试
- [x] 6.4 补充 50%、100%、200% 和 400% zoom 下屏幕命中热区稳定及大小策略自动切换测试
- [x] 6.5 补充重叠大小框中未选中小框优先、已选中矩形边手势目标锁定的测试
- [x] 6.6 修订单击/拖动分流测试：原地单击无几何修改/无 undo，首次非零移动立即响应，有效拖动单次 undo，无变化 release 不写 undo
- [x] 6.7 补充最小尺寸、反翻转、图像边界、Esc 和失焦回滚测试
- [x] 6.8 运行三个矩形边目标测试文件并修复全部回归
- [x] 6.9 运行受影响文件 `py_compile`、Black 和目标 Flake8
- [x] 6.10 运行完整测试套件，确认 Canvas 选择、绘制、稳定预览、键盘选边和快捷键行为无回归

## 7. 人工验收与文档收口

- [x] 7.1 在 50%、100%、200% 和 400% zoom 下手工验证普通矩形的角、边、内部三分区
- [x] 7.2 手工验证屏幕短边小于、等于和大于 `3 * epsilon` 时的小框/普通框策略切换
- [x] 7.3 手工验证嵌套框、部分重叠框和多框密集区域的小目标优先选择保持不变
- [x] 7.4 手工验证小矩形内部可以执行原有移动，外侧四边可以单边调整
- [x] 7.5 手工验证边原地单击、首次有效移动、快速拖动、Esc、失焦、切图和关闭功能时的选择、几何、undo 与视觉状态
- [x] 7.6 更新必要的用户文档，说明“矩形单边调整”只增强当前唯一选中矩形且小框需从外侧拉边
- [x] 7.7 对照 OpenSpec 场景逐项记录验收结果，并确认所有任务完成后再进入归档

## 8. 取消单边拖动固定启动阈值

- [x] 8.1 增加 100% zoom 下的回归测试，复现现有前 4px 不更新、快速移动后边跳到鼠标位置的行为
- [x] 8.2 保留 press pending 和按下时边引用锁定，移除 `RECT_EDGE_DRAG_THRESHOLD_PX` 及固定距离启动判断
- [x] 8.3 仅在首个相对 press 位置发生非零变化的左键 move 事件中进入 dragging，并在同一事件更新目标边
- [x] 8.4 验证 press 本身和同位置 move 不修改矩形，原地 release 不创建 undo、不发出 `shape_moved`、不设置 dirty
- [x] 8.5 验证首次移动后仍复用精修 delta、图片边界、最小尺寸、反翻转、目标边锁定和取消回滚语义
- [x] 8.6 对比原生矩形角点和单边调整，在 50%、100%、200% 和 400% zoom 下确认两者均无固定启动迟滞
- [x] 8.7 运行三个 `test_rect_edge_*` 目标测试文件，并完成未放大画布下慢速 1px 移动和快速拖动的人工验收

## 验收记录（2026-08-04）

- 自动化交互验收：离屏 Qt 事件覆盖 50% / 100% / 200% / 400% zoom 下的角点、边和内部仲裁；小框 `3 * epsilon` 分界、嵌套/重叠目标优先级、框内移动与框外单边调整，以及单击、首个 1px 移动、快速拖动、Esc、失焦、切图和功能关闭。
- 启动行为：`test_selected_edge_drag_starts_on_first_nonzero_screen_move`、`test_pending_edge_same_position_move_does_not_start_drag` 和 `test_fast_edge_drag_updates_on_its_first_move_event` 分别验证首个非零移动、同位置 no-op 和快速首帧更新；首次拖动与原生角点在四个 zoom 档位的对比由参数化测试覆盖。
- 状态与预览：精修模式仅缩放 delta；小框外侧命中、正式选择和 selection-derived observer 保持稳定，边候选不由稳定预览仲裁。
- 质量门禁：目标矩形边测试 106 passed；`py_compile`、Black、目标 Flake8 通过；全量 `pytest -q` 为 834 passed、7 subtests passed（仅现有 NumPy 2.0 弃用警告）。
- 说明：上述“手工验证”项已使用离屏事件驱动等价验收完成；尚未进行真人在可见窗口中的肉眼操作验收，发布前可按同一清单做一次补充确认。
