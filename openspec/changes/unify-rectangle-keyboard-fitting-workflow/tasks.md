## 1. 配置与纯规则基础

- [x] 1.1 添加边界定位键、进入拟合键和 QWER／1234 预设，明确保留键、精确修饰键及数字小键盘归一化规则。
- [x] 1.2 实现可测试的上下文快捷键所有权与冲突检测，区分同状态冲突和已审计的临时重用；配置失败保留旧值。
- [x] 1.3 实现矩形中心半区及单边几何校验纯函数，覆盖中心线、框外扩张、非有限值、最小尺寸和浮点精度。
- [x] 1.4 添加数字矩形绘制方法解析与兼容默认值，未知方法保留配置并阻止错误启动。

## 2. 工具入口与生命周期解耦

- [x] 2.1 将矩形任务状态与常驻 QWidget 启用状态分离，在工具菜单注册动作，移除主布局旧勾选和常驻控制面板。
- [x] 2.2 迁移旧 enabled 偏好为无运行作用，保留相关视图偏好；验证应用启动不恢复活动任务。
- [x] 2.3 修改四极值成功提交路径为选中新框并回到普通编辑，检查两点创建也不自动进入任何修正；保留标签取消后的草稿。
- [x] 2.4 实现显式继续四极值、单边精修与键盘拟合入口，并定义菜单可用条件和完成动作。
- [x] 2.5 实现独立拟合的完成／Esc 退出、同图目标切换与无目标暂停，以及切图成功／取消、删除、失焦、工具切换清理。

## 3. 数字快捷键管理器与绑定绘制

- [x] 3.1 在数字管理器显示矩形两点／四极值选项，保存 rectangle 加 drawing_method；补充模式适用说明。
- [x] 3.2 审计并修改数字映射的读取、翻页、保存、标签对话框回写与重启加载，保证新字段及原映射不丢失。
- [x] 3.3 将普通数字绘制接入统一矩形方法入口，保持绑定优先、选中对象重命名和分页的现有模式外行为。
- [x] 3.4 将四极值接入绑定 pending 与原子提交，验证初始化不误清理 pending；取消／失败不回填源 gid。

## 4. 键盘定位、撤销与共享输入

- [x] 4.1 在 ShortcutOverride 和实际按键路由中复用所有权，草稿／拟合消费已映射键和其余裸数字，拒绝不回落，文本焦点不捕获。
- [x] 4.2 实现当前光标采样、原图转换、鼠标按住／浮窗／留白拦截、半区及几何校验，复用既有单边提交和通知。
- [x] 4.3 实现每次按下最多一次操作及跨模式重复事件隔离，覆盖数字启动兼作顶边键、第四边完成后仍按住的情形。
- [x] 4.4 为四极值加入固定步骤边界键，保留鼠标确认、草稿撤销与标签重试；非法步骤不推进。
- [x] 4.5 每次有效定位建立独立撤销单元；拒绝／无变化不建历史；激活对应边接续 1px／5px 步进，维持身份、属性和保存语义。

## 5. 浮动反馈与界面一致性

- [x] 5.1 添加任务期间显示的可移动、位置受限、不抢焦点浮窗，统一上右下左布局，区分草稿步骤和拟合区域。
- [x] 5.2 接入实际配置键位、实时可用性、成功按键／边高亮、拒绝提示和数字操作暂停文案；提示与执行复用规则结果。
- [x] 5.3 接入完成／取消、撤销和只选边不定位的浮窗控制；保证指针进入浮窗后不会使用旧画布坐标。
- [x] 5.4 更新中英文字符串及设置说明，运行语言编译脚本生成 qm 和资源，不手改 resources.py。

## 6. 验证与规划收敛

- [x] 6.1 运行新增纯规则／配置测试及最窄相关数字映射回归，覆盖预设冲突、分页往返、旧配置和未知方法。
- [x] 6.2 运行相关 Qt offscreen 测试，验证创建不自动精修、模式退出、文本焦点、失效目标、取消导航及数字拒绝不重命名。
- [x] 6.3 运行四极值、绑定绘制及拟合撤销相关测试，重点验证创建／拟合独立历史、绑定取消不改变源对象、重复按键不泄漏。
- [x] 6.4 对修改范围执行格式和 lint；按实际改动选择测试文件，不仅为确认重复运行完整套件。
- [x] 6.5 在独立真实 Qt 窗口验证 QWER／1234、浮窗按键反馈、鼠标离图、缩放平移和连续创建，并目视检查截图；原生捕获受限时使用 Qt 键鼠事件及窗口渲染，记录替代方式及未开展真实用户实验的限制，见 verification.md。
- [x] 6.6 定向收敛旧矩形 OpenSpec 中被替代的入口与自动衔接描述，保留无关未完成任务；验证本变更并记录最终验证结果。
