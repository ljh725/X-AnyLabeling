## 1. 手动扫描与共享控制器边界

- [x] 1.1 为共享索引控制器增加暂停/恢复尚未触发自动刷新计时器的窄接口，保持默认 `ON_CACHE_ATTACH` 策略且不取消运行中的任务。
- [x] 1.2 在缩略图打开/关闭生命周期取得并释放自动刷新暂停租约，保证窗口本身不新增后台扫描。
- [x] 1.3 将窗口恢复控件收敛为“开始扫描”按钮，按索引连接状态调用现有 refresh/rebuild，并复用 progress/busy/终态信号显示进度、禁用提交和成功后加载首页。
- [x] 1.4 增加自动刷新触发源、暂停恢复、打开零启动、扫描进度、忙碌禁用和完成加载测试。

## 2. 四周裁剪上下文与缓存失效

- [x] 2.1 在缩略图渲染中将 bbox 四周各扩大对应轴尺寸 15%（左右按框宽、上下按框高），并裁到图像边界。
- [x] 2.2 将裁剪策略版本纳入 `ThumbnailCacheKey`，保证旧的无 padding 内存/磁盘缓存不命中。
- [x] 2.3 更新正常、四向边界、无效 bbox、同图合并和缓存版本测试。

## 3. 定位框与选中态视觉区分

- [x] 3.1 在缩略图模型增加单一定位对象状态与角色，反向定位成功时设置白色框，未命中时清除白色框。
- [x] 3.2 区分程序化定位选择和用户手动选择；白框持续到下一次反向定位或用户手动改变选择。
- [x] 3.3 调整 delegate：改标候选使用加粗提亮的蓝色外框，定位目标使用独立白色内框。
- [x] 3.4 增加反向定位、未命中、手动清除、多选边框和两种语义共存测试。

## 4. 集成验收

- [x] 4.1 更新四种语言 `.ts` 和编译资源，并更新用户文档与新设计中对旧 padding 决策的定向修订。
- [x] 4.2 运行新增测试及既有 dataset-index、缩略图、导航和 object-relabel 回归。
- [x] 4.3 运行 Black、Flake8、`py_compile` 与 OpenSpec 严格校验，确认未增加自动标注、删除、字段编辑或跨页队列。
- [x] 4.4 提交实现并创建完成备份标签。
