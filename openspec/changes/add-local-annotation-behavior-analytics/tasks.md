## 1. 契约、配置与测试夹具

- [ ] 1.1 建立 `anylabeling/services/behavior_analytics/` 纯 Python 包，定义事件 schema 版本、分析算法版本、稳定枚举和公共类型。
- [ ] 1.2 定义语义事件目录、各 `event_type` 的必需/可选 payload 字段以及隐私字段白名单，并增加 schema 校验测试。
- [ ] 1.3 在默认配置中加入总开关、保留期、空闲阈值、burst 静默窗口、队列上限和代表性轨迹上限，验证升级后总开关仍为关闭。
- [ ] 1.4 创建覆盖跨午夜、对象来回选择、状态版本变化、失焦/空闲、撤销删除和损坏尾记录的固定事件夹具。

## 2. Shape 身份与格式边界

- [ ] 2.1 为 shape 模型增加显式 `xanylabeling_shape_id`，实现 UUID 生成、JSON 读写和未知旧文件的惰性补 ID。
- [ ] 2.2 调整 dirty 判定，使仅在内存补 ID 不触发已修改状态，并验证发生真实标注变化后的保存会持久化 ID。
- [ ] 2.3 覆盖新建、复制、粘贴、导入、AI 创建、编辑、删除和撤销恢复的 ID 生命周期，增加碰撞和身份保留测试。
- [ ] 2.4 审计 VOC、YOLO、COCO 及其他现有转换路径，确保专用 ID 不导出且不改变 `group_id`、`flags`、`attributes`、`kie_linking` 语义。

## 3. 会话、时间和对象轮次

- [ ] 3.1 实现应用会话、项目会话、自然日切片、图片访问和对象交互轮次状态机，生成并关联相应 ID。
- [ ] 3.2 实现使用本机随机盐的 `project_id`、`image_id` 匿名稳定标识，不在日志中保存绝对路径或盐值。
- [ ] 3.3 同时记录 UTC、当地日期、时区偏移和单调时间，处理本地午夜切片及系统墙钟跳变。
- [ ] 3.4 实现对象轮次的开始/结束规则，验证 A→B→A 保持 A 的 shape 身份但生成两个 episode。
- [ ] 3.5 记录窗口聚焦、失焦、空闲开始/结束和关闭原因，为 wall/focused/active 时间计算提供边界。

## 4. 功能状态版本

- [ ] 4.1 建立 feature registry，逐项定义稳定键、类型、`configured`、`active` 和 `used` 的权威判定方式。
- [ ] 4.2 覆盖规格所列编辑模式、矩形精修、精确调整、导航/筛选、Inspector/质检、AI、自动保存和快捷键模式，并排除纯外观项。
- [ ] 4.3 在项目会话开始写版本 1 启动快照，无法读取时写版本 0 和 `state_unknown`。
- [ ] 4.4 把一次设置事务的多个有效差异合并为一个变化事件和一次版本递增，无有效变化时不递增。
- [ ] 4.5 确保后续普通事件引用当前状态版本，并测试状态重建、缺失版本和首次实际使用场景。

## 5. 语义事件采集与高频动作聚合

- [ ] 5.1 在项目打开/关闭、图片进入/离开、保存、焦点和应用退出入口接入生命周期事件。
- [ ] 5.2 在 shape 新建/选择/删除、标签/属性变化、撤销重做和输入来源入口接入对象语义事件。
- [ ] 5.3 为几何拖拽建立开始、提交、取消动作段，记录起止摘要、净变化、结果与单调耗时，不记录移动采样点。
- [ ] 5.4 为滚轮缩放、键盘微调和平移实现可配置 burst 聚合，并测试静默窗口、图片切换和异常中断边界。
- [ ] 5.5 接入模式、AI、Inspector、质检和复核动作，确保事件能引用项目、图片、shape、episode 和状态版本。
- [ ] 5.6 为现有矩形框复核精修统计增加统一适配器或共享关联 ID，并通过回归测试证明同一动作不被双重计数。

## 6. 本地写入、恢复与清理

- [ ] 6.1 实现有界内存队列和后台 JSONL 分片批量写入，UI 线程不得同步等待常规磁盘 I/O。
- [ ] 6.2 实现有限时 flush、写入错误放行、队列丢弃计数、单条大小限制和内部健康摘要。
- [ ] 6.3 实现 JSONL 尾记录恢复、未知可选字段兼容、未知 schema 隔离和未闭合动作段标记。
- [ ] 6.4 实现按保留期自动清理及按项目/时间范围显式清理，确保不修改标注 JSON 和图片。
- [ ] 6.5 增加并发生产、队列满、目录不可写、异常退出和大批量动作的可靠性与性能测试。

## 7. 阶段二读取与精确时间统计

- [ ] 7.1 实现按自然日、项目会话和明确起止时间筛选日志分片，并输出实际范围、时区和输入版本元数据。
- [ ] 7.2 实现逐行校验和质量分类，分别统计读取、采用、跳过、损坏、未知版本和缺失引用数量。
- [ ] 7.3 实现项目会话、功能状态和动作段重放，使字段缺失的事件只退出依赖该字段的指标。
- [ ] 7.4 实现 wall、focused、active 和动作单调耗时计算，并用跨午夜、长失焦、空闲和墙钟跳变夹具验证。

## 8. 阶段二行为聚合

- [ ] 8.1 按动作、来源、结果、项目、自然日、图片、对象输出次数及总计、均值、中位数、P75、P95。
- [ ] 8.2 实现连续上下文内的相邻转移和 3 至 6 阶动作序列统计，禁止跨会话、图片、episode 或长空闲拼接。
- [ ] 8.3 实现撤销、取消、失败、反向调整和重复编辑回路统计，并记录明确的识别规则版本。
- [ ] 8.4 按复合对象身份和 episode 输出编辑次数、返回次数、累计/单轮活跃耗时和主要动作构成。
- [ ] 8.5 按 feature 的 configured/active/used 状态输出观察性对比、样本量和差异值，缺失状态版本的事件不得进入对比。
- [ ] 8.6 用固定夹具验证所有聚合结果和排序完全确定，并覆盖样本不足、耗时缺失及百分位边界。

## 9. 压缩分析包与应用内入口

- [ ] 9.1 实现分析包 manifest，以及 `summary.json`、六个规定 CSV 和 `representative_traces.jsonl` 的固定字段契约。
- [ ] 9.2 实现常见、中位数、P75、最长、失败/回路轨迹的确定性选择、固定 tie-breaker、分类上限和总字节上限。
- [ ] 9.3 在导出层执行隐私字段白名单、匿名化和净化计数，并为每个输出文件写入大小与 SHA-256。
- [ ] 9.4 使用临时目录和完成后原子发布，处理取消、失败、同名结果和空范围，保证不覆盖已有完整分析包。
- [ ] 9.5 在现有 UI 结构中加入内部设置和行为分析入口，提供开关、范围选择、进度、取消、结果位置、隐私说明和清理操作。
- [ ] 9.6 确认阶段二代码和 UI 不包含模型调用或上传路径，只展示确定性统计和分析包导出结果。

## 10. 验收、文档与发布保护

- [ ] 10.1 为两个能力规格的所有场景建立纯 Python 单测或最小 Qt offscreen 集成测试，并运行受影响测试集合。
- [ ] 10.2 增加端到端流程测试：启用记录、跨两个对象操作、切换功能状态、关闭项目、按自然日与项目会话分别生成分析包。
- [ ] 10.3 建立启用/关闭两种模式的 UI 延迟、内存、日志增长和大型日志统计基线，修复超过约定阈值的回归。
- [ ] 10.4 更新用户文档，说明默认关闭、本地存储、shape ID 生命周期、时间口径、隐私、清理和外部大模型边界。
- [ ] 10.5 运行 Black、flake8、相关 pytest 和 OpenSpec 严格校验，确认现有标注保存、转换器与矩形精修统计没有回归。
