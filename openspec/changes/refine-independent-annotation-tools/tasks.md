## 1. 实施前核对与回归基线

- [x] 1.1 核对缩略图增量刷新、矩形拟合、创建／精修等在途变更，列出已有可复用能力及与本次规格冲突的入口，避免覆盖无关未提交修改。
- [x] 1.2 复现两个管理器和语言在功能修改后重启丢失的问题，记录保存前、提交后、退出后、重启后的三个字段及有效读写路径；使用隔离配置，不重置用户实际配置。
- [x] 1.3 确认现有排序查询、永久对象身份、显式定位、几何撤销和配置提交接入点，确定不冲突的两个轴向默认键位及现有快捷键设置接入点。

## 2. 配置保存与恢复修复

- [x] 2.1 为默认工作目录、自定义工作目录、显式配置文件与临时参数明确持久配置来源，修复实际读取文件和保存文件不一致的路径。
- [x] 2.2 修复两个管理器、设置控制器及退出保存的局部更新与共享状态合并，避免旧副本覆盖新映射或语言。
- [x] 2.3 实现兼容默认值补全与必要的字段迁移，保留原用户配置和未知用户字段，明确区分合法空映射与读取失败。
- [x] 2.4 完善配置可靠写入和错误返回，失败保留原文件，读取损坏配置不静默覆盖，界面不误报保存成功。
- [x] 2.5 验证语言选择及时持久化并在启动阶段恢复，不通过修改默认语言掩盖故障。
- [x] 2.6 增加针对性回归测试，覆盖交叉保存、关闭回写、功能升级补默认项、主动清空、自定义来源、临时覆盖及读写失败，并验证修复前复现用例通过。

## 3. 同图空间相邻排序与位置稳定

- [x] 3.1 添加独立的同图空间相邻排序选项，保持默认排序和类别筛选语义，并为该选项接入既有偏好保存机制。
- [x] 3.2 实现 design D2 的按图中心最近邻序列、距离同值稳定排序及无效 bbox 末尾处理，先完成单图纯逻辑验证。
- [x] 3.3 将同图完整排序接入分页前结果，保持图片序列和每页最多 100 项；按图后台计算、支持过期任务取消，不触发全库裁剪。
- [x] 3.4 将身份次序与对象内容分离，几何更新只失效裁剪缓存并更新内容，自动同步、翻页和窗口激活不得重排。
- [x] 3.5 实现改类移出和删除后的立即移除、同图新进入对象追加、失败保留，以及页边界变化后的去重、补位和选择清理。
- [x] 3.6 将主动刷新接入按最新坐标重排，保留有效身份锚点及合理回退，区分用户主动刷新与后台文件同步。
- [x] 3.7 验证排序下的双击与 Ctrl + Alt + T 显式定位，覆盖主窗口修改同图其他对象、单屏换图返回和双屏不抢焦点。
- [x] 3.8 增加近邻与远点反例、同坐标、无效 bbox、跨图及跨页排序、改框冻结次序、改类失败、成员新增删除的针对性测试。

## 4. 矩形点击调整与界面设置

- [x] 4.1 实现按一下 Kx／Ky 进入轴向编辑、按已提交中心半区判边及只取对应坐标，不保留与规格冲突的中心禁区。
- [x] 4.2 在主窗口“视图”的“矩形点击调整”分组添加可勾选“角点击调整”开关，默认关闭；实现启用后鼠标指定角再点击新点，固定另一对角，不增加角快捷键。
- [x] 4.3 添加贯穿工作区域的参考线、目标边或角突出和新矩形轮廓，正确处理缩放、平移及图像坐标变换。
- [x] 4.4 接入一次点击一个几何事务与撤销项，角调整两边一起提交；保持对象和当前方式选中，允许连续切换调整。
- [x] 4.5 实现一次 Esc 或关闭角开关退出并保留选择，以及切图、目标删除、失焦、方式切换时取消未提交状态；开关勾选与实际角模式同步，边快捷键直接启动并退出角模式。
- [x] 4.6 增加半区边界、固定预览中心、越中心指定角、无变化／无效提交、逐次撤销、连续调整和退出的纯逻辑与 Qt offscreen 回归测试。
- [x] 4.7 在现有快捷键设置中加入“边点击调整：修改 X（左／右边）”和“边点击调整：修改 Y（上／下边）”两项，支持编辑、清空、恢复默认和冲突检查，保存后更新绑定并跨启动恢复。
- [x] 4.8 验证角开关的无有效选择禁用、开启选角、关闭恢复拖角及启动默认关闭；验证两个轴向绑定独立设置、冲突不覆盖、清空仅禁用对应入口和文本输入不触发。

## 5. 保留独立创建并退役旧功能

- [x] 5.1 从现有流程化工具中提取或复用四极值创建能力，提供独立矩形创建入口，完成创建不自动进入其他流程。
- [x] 5.2 退役修正框功能本身，移除专属入口、启停开关、快捷绑定和状态恢复；仅复用被保留功能需要的通用几何辅助，不以新名称保留旧交互。
- [x] 5.3 明确轴向点击、角点击开关、原有边编辑和默认角拖动的事件归属，验证同一手势只有一个提交者。
- [x] 5.4 退役旧流程菜单、工具栏、快捷入口、HUD 编排和自动步骤跳转，忽略或迁移退役字段而不清空无关配置，不将旧修正框开启状态转给角开关。
- [x] 5.5 验证独立创建、边点击调整、角开关、默认拖角、工具切换取消预览，以及旧配置启动不能复活修正框和退役流程。

## 6. 集成验收与交付

- [x] 6.1 更新新增和退役入口涉及的中英文文案及快捷键说明；按项目脚本编译翻译资源，不手工编辑生成文件。
- [x] 6.2 执行本次实际修改对应的格式、静态检查和相关测试，记录命令与结果，不以无关完整测试套件代替针对性验证。
- [ ] 6.3 使用实际代表图片验收重复框对照、密集邻座、跨页位置和改框后主动刷新；确认耗时不阻塞 UI，不声称算法识别全部重复框。
- [ ] 6.4 分别按单屏和双屏操作验收：缩略图改类、双击原图、顺手修改其他对象、显式返回、内容同步和保持浏览位置。
- [ ] 6.5 执行两个管理器及语言的真实关闭重启验收，包含旧配置升级和失败反馈，确认保留全部原有有效值。
- [ ] 6.6 核对四份规格的每个场景及迁移／回滚说明，更新任务完成状态和验证记录；仅在实现验收完成后进入 OpenSpec 归档。

### 上轮验证记录（2026-09-15，不能代表全部设计场景完成）

- 配置、轴向判定、空间排序、Canvas 预览与快捷键设置相关测试：72 passed。
- 已执行 scripts/compile_languages.py，更新中英文及日/韩资源。
- Black 已覆盖本次修改文件；Flake8 仅报告既有 label_widget 复杂度/F841 项。
- 真实单屏/双屏和完整应用重启仍需在目标机器按场景操作确认，代码已提供对应状态、定位与取消路径。

### 点击调整反馈复查（2026-09-15）

- 第 4 节已按真实 Qt 键鼠事件、配置重新读取和实际渲染验证；详细根因、命令、结果见 [click-adjustment-verification.md](click-adjustment-verification.md)。
- 继续复查并完成第 5 节：以独立 RectangleCreation 替代旧流程组件，清理旧流程、键盘拟合与 HUD；数字绑定创建改接独立入口。
- 最终相关回归 147 passed，覆盖点击调整、独立创建、旧配置不恢复退役功能、原生编辑及设置。早期 117 项和渲染复测已被本轮覆盖，不重复计数。
- 第 6 节物理单屏/双屏及真实用户数据集重启集成验收未执行，撤销此前不准确的完成标记；未执行 OpenSpec 归档。

