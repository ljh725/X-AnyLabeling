## 1. 建立回归基线

- [x] 1.1 新增视口重置状态测试文件，用失败用例复现“非当前图片重置后仍继承上一张视图”和三类缓存未形成统一契约的问题。
- [x] 1.2 新增 PyQt offscreen 菜单测试，用失败用例复现 `populate_mode_actions()` 后普通画布菜单丢失三个重置选项的问题。
- [x] 1.3 为待移动 shape 使用的第二套画布菜单建立内容基线，记录复制/移动动作、三个重置动作及既有菜单项的预期顺序。

## 2. 第一类目标：统一重置状态模型

- [x] 2.1 在小型独立模块或 `ViewportController` 的窄接口中定义结构化重置结果，区分目标图片数、实际缓存清理数、当前图片命中和待默认打开标记数。
- [x] 2.2 实现稳定去重的目标解析，过滤无效目标，并保证当前、指定文件到末尾、当前数据集全部三个范围与 `fn_to_index` 顺序一致。
- [x] 2.3 实现一次性“下次强制默认视图”标记，使非当前重置目标在首次加载时跳过精确历史、`keep_prev_viewport`、`keep_prev_scale` 和旧滚动回退。
- [x] 2.4 使当前图片的强制默认标记在立即复位时被消费，并验证用户随后产生的新缩放和平移仍可正常保存。
- [x] 2.5 将视口历史、`zoom_values` 和水平/垂直 `scroll_values` 纳入同一次状态提交；为异常路径增加快照恢复或等价的一致性保护。
- [x] 2.6 增加数据集生命周期清理接口，在替换文件列表、关闭数据集和打开形成新数据集边界的独立文件时清除历史、默认标记、旧缩放、旧滚动和 `last_loaded`。

## 3. 第一类目标：同步当前画布和用户反馈

- [x] 3.1 抽取唯一的当前图片默认视图应用函数，统一设置 FIT_WINDOW、Fit Window/Fit Width 勾选、缩放控件、画布比例、滚动条和导航器。
- [x] 3.2 调整 `load_file()` 的恢复分支，使强制默认结果有独立分支且优先于所有继承和旧缓存回退，同时保持普通切图行为不变。
- [x] 3.3 将三个顶部/画布入口和三个文件列表入口统一接到结构化重置流程，删除重复或仅按缓存数判断成功的分支。
- [x] 3.4 修改成功提示以有效目标图片数计数；覆盖当前图片无缓存时显示 1、范围为空时不报告成功、异常时显示失败。
- [x] 3.5 验证任意范围重置均不改变 shape、选中状态、dirty 状态、标注 JSON、图片文件、文件检查状态和继承配置。

## 4. 第二类目标：修复画布右键菜单

- [x] 4.1 将三个重置 QAction 以及复制/移动 QAction 保存为可复用成员，避免它们只存在于初始化局部变量中。
- [x] 4.2 实现单一 `_rebuild_canvas_context_menus()` 路径，按固定顺序从空状态重建普通画布菜单和待移动 shape 菜单，并重新绑定筛选子菜单引用。
- [x] 4.3 修改初始化和 `populate_mode_actions()` 统一调用菜单重建函数，移除会只恢复 `self.actions.menu` 的旧清空逻辑。
- [x] 4.4 增加重复重建测试，确认两个菜单始终各有三个重置选项，且动作、分隔符和筛选子菜单不丢失、不重复、不累积。
- [x] 4.5 增加菜单触发集成测试，分别从两套画布菜单触发当前、区间和全部入口，验证调用范围与顶部菜单一致。

## 5. 国际化与说明文档

- [x] 5.1 将重置菜单、提示和错误反馈改为稳定的英文 `tr()` source，并更新 `en_US.ts`、`zh_CN.ts`、`ja_JP.ts`、`ko_KR.ts`。
- [x] 5.2 运行 `scripts/compile_languages.py` 重新生成 `.qm` 和 `resources.py`，确认生成代码仅由编译脚本修改。
- [x] 5.3 更新视口状态管理说明，明确“全部”是当前数据集、非当前目标使用一次性默认标记、反馈按目标图片计数以及两套画布菜单入口。

## 6. 验收与质量门禁

- [x] 6.1 运行新增的纯状态测试，覆盖去重范围、三类缓存、继承开关、一次性标记、异常一致性和数据集生命周期。
- [x] 6.2 使用 `QT_QPA_PLATFORM=offscreen` 运行新增的画布菜单和当前视图 UI 测试，并运行受影响的现有 canvas interaction 测试。
- [x] 6.3 对修改的 Python 文件运行 Black（79 字符）、Flake8 和 `py_compile`，修复新增代码的类型标注与 Google 风格 docstring 问题。
- [x] 6.4 执行 5,000 个模拟文件标识的批量重置测试或基准，确认保持 O(N) 内存处理且不触发 shape 遍历、图片解码或文件 I/O。
- [x] 6.5 按规格逐项人工验收顶部菜单、文件列表右键、普通画布右键和待移动 shape 画布右键，并记录四种语言下的菜单与反馈结果。
