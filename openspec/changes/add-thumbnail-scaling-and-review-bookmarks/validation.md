# 验证记录

修改前节点 `3db000fe`。本轮只实现统一卡片缩放和两条检查记录。

- 真实鼠标右边缘、下边缘和右下角拖动测试：100 项不变、整页统一尺寸、选择不变、无额外查询或导航。
- 拖动期间不新增渲染请求；结束后保留旧图预览并在后台替换为更高分辨率图像。无位移边缘点击、失去焦点、边缘双击和页尾第 81 项重排均专项验证。
- 连续浏览三页但不再点击后，关闭记录第三页最后一项，同时保留第一页单击对象；启动全新 Python 进程读取 QSettings，验证两条不同记录已持久保存。
- 覆盖数据集隔离、损坏设置、空页面保留上次记录、取消选择/键盘导航不改写单击记录、尺寸上下限与持久化、保存失败提示，以及标注文件字节不变。
- 联合批次 `test_thumbnail_scaling_bookmarks.py`、`test_dataset_thumbnail_browser.py`、`test_object_relabel_widget.py`：78 passed。随后新增页尾缩放用例并重测完整中文截图用例：2 passed。总计 79 个不同用例通过，其中本轮新增 13 个。
- 新文件和缩略图浏览器 Flake8 通过；所有改动 Python 文件 Black 检查通过。四语言 TS 已编译，resources.py 由脚本生成。
- `preview-220.png`、`preview-340.png` 使用真实 Qt 绘制，已检查记录可读、统一尺寸和排列、缩放手柄及底栏无重叠。

记录在正常关闭窗口或软件时写入应用设置，重启后再次打开同一数据集的缩略图可查看。它们是人工核对提示，不代表此前对象全部检查完成；不自动导航。测试使用临时合成数据，不修改用户标注。
