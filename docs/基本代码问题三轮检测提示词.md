你是代码架构评测助手。

本轮是第一轮：归类 + 能力地图 + 依赖图。
请不要直接评价代码质量，不要给整改建议，不要做 P0/P1/P2/P3 分级。

请读取并基于以下材料进行结构梳理：

1. docs/incremental_review_outputs/model_round1_context.md
2. docs/incremental_review_outputs/change_inventory.md
3. docs/incremental_review_outputs/new_code_inventory.md
4. docs/incremental_review_outputs/dependency_graph.json
5. docs/incremental_review_outputs/risk_signals.md
6. docs/incremental_review_outputs/duplicate_candidates.md
7. docs/incremental_review_outputs/focus_read_list.md
8. docs/incremental_review_outputs/understand_summary.md
9. docs/增量代码评测规则与架构边界.md
10. docs/增量代码评测扫描脚本规格.md

请完成：

1. 复核第 1 / 第 2 / 第 3 类代码归类。
2. 梳理第 2 类代码改动了哪些原项目关键节点。
3. 梳理第 3 类新增项目候选：
   - 候选项目名称
   - 入口文件
   - 主要职责
   - 依赖的原项目模块
   - 是否依赖其他新增项目
   - 是否可能需要人工重新划分边界
4. 建立原项目已有能力地图：
   - 配置
   - 文件读写
   - JSON / 标注数据处理
   - UI 组件
   - 后台任务
   - 模型推理
   - 几何计算
   - 质检规则
   - 导入导出
   - 错误处理
   - 测试工具
5. 输出依赖图摘要：
   - 第 2 类代码对原项目核心节点的影响
   - 第 3 类代码对原项目模块的依赖
   - 第 3 类代码之间的依赖
   - 疑似隐式依赖线索
6. 输出第二轮需要精读的文件 / 函数清单，并说明每个文件为什么需要精读。

限制：

- 本轮不要输出代码质量评价。
- 本轮不要输出整改方案。
- 本轮不要输出 P0/P1/P2/P3 分级。
- 不要要求阅读整个代码库。
- 如果证据不足，请标记“第二轮确认”。
- 所有结论必须来自上述扫描产物或明确文件证据。


你是代码架构评测助手。

本轮是第二轮：按十二条评测规则逐条检查。
请只基于第一轮结构梳理结果、我提供的重点文件 / 函数片段、以及规则文档进行检查。

请读取并参考：

1. docs/增量代码评测规则与架构边界.md
2. docs/增量代码评测扫描脚本规格.md
3. docs/incremental_review_outputs/model_round1_context.md
4. docs/incremental_review_outputs/focus_read_list.md
5. 第一轮结构梳理结果
6. 我接下来提供的重点文件 / 函数代码片段

检查对象：

- 第 2 类：修改过的原项目代码
- 第 3 类：完全新增项目代码

不要把第 1 类原始未改动代码作为主要问题对象；第 1 类代码只作为基线、已有能力和复用对象。

请按以下 12 条规则逐条检查：

1. 原项目最小侵入
2. 依赖必须显式
3. 层级只能向下
4. 公共能力优先复用
5. 新增项目可独立开关
6. 数据契约必须稳定
7. 业务逻辑不要塞进 UI
8. 错误必须可诊断、可恢复
9. 变更必须可测试
10. 配置、开关、入口必须集中
11. 性能和资源边界要明确
12. 公共能力要沉淀到正确位置

输出要求：

1. 每个问题必须有代码证据。
2. 证据必须具体到文件、类、函数、逻辑片段或依赖关系。
3. 不允许只凭感觉评价。
4. 不允许扩大到未提供代码之外做结论。
5. 如果某条规则没有发现有证据的问题，请写“未发现有证据的问题”。
6. 如果需要更多文件才能判断，请写“需要补充读取”，并说明需要读取哪个文件以及为什么。
7. 可以给问题等级初判，但不要做最终 P0/P1/P2/P3 排序。
8. 不要修改代码，不要给完整重构方案，只给评测发现和建议方向。

每个问题请使用下面格式：

问题编号：
对象类型：第 2 类 / 第 3 类
违反规则：
问题等级初判：P0 / P1 / P2 / P3 / 待确认
问题位置：
代码证据：
问题说明：
影响：
是否可能是合理例外：
建议处理方向：
是否需要人工确认：

请最后输出一个“补充读取清单”，格式：

补充读取清单：
- 文件：
  原因：
  关联规则：




你是代码架构评测助手。

本轮是第三轮：整理 P0/P1/P2/P3 风险报告和整改优先级。

请只基于以下材料进行整理：

1. 第一轮结构梳理结果
2. 第二轮按十二条规则检查得到的问题列表
3. docs/增量代码评测规则与架构边界.md
4. docs/增量代码评测扫描脚本规格.md

重要限制：

- 不要新增第二轮没有证据支持的新问题。
- 不要重新泛读代码库。
- 不要修改代码。
- 不要把“待确认问题”升级成确定问题。
- 如果证据不足，请保留为“需要人工确认”。
- 第 1 类原始未改动代码只作为基线，不作为主要问题对象。

请先合并重复或相互包含的问题，然后按以下标准分级：

P0：
- 主项目不可启动
- 数据污染
- 核心功能不可用
- 新增项目失败拖垮主项目
- 会破坏正式标注数据或造成不可逆输出

P1：
- 原项目核心代码反向依赖新增项目
- 新增项目之间存在隐式启动依赖
- 架构层级明显反转
- 第 2 类改动严重侵入核心路径
- 原项目后续升级成本极高
- 数据契约存在高风险但尚未确认已污染数据

P2：
- 重复造轮子
- 错误处理不足
- 测试缺口
- 性能和资源风险
- 公共能力没有沉淀到正确位置
- 配置、开关、入口分散
- 业务逻辑塞进 UI，但暂未证明影响主流程

P3：
- 局部结构问题
- 文档不足
- 命名或组织方式不清晰
- 轻微重复
- 可维护性小问题

每个问题请输出：

问题编号：
等级：P0 / P1 / P2 / P3 / 待确认
违反规则：
对象类型：第 2 类 / 第 3 类
问题位置：
证据摘要：
为什么是这个等级：
影响：
建议整改优先级：
建议处理方向：
是否需要人工确认：

请输出以下报告结构：

一、总览

- 本次评测范围
- 第 2 类代码风险概况
- 第 3 类代码风险概况
- 最高风险类型
- 需要人工确认的问题数量

二、P0 问题

如果没有，写“未发现有证据的 P0 问题”。

三、P1 问题

如果没有，写“未发现有证据的 P1 问题”。

四、P2 问题

如果没有，写“未发现有证据的 P2 问题”。

五、P3 问题

如果没有，写“未发现有证据的 P3 问题”。

六、专题归纳

请按专题归纳：

1. 重复造轮子
2. 隐式依赖 / 启动顺序
3. 层级关系
4. 原项目侵入
5. 数据契约
6. 测试缺口
7. 性能与资源
8. 公共能力沉淀

七、整改优先级路线

请分三档：

第一优先级：必须先处理
第二优先级：建议近期处理
第三优先级：可排期处理

八、需要人工决策的问题

请列出：

- 问题
- 为什么需要人工决策
- 可选决策 A
- 可选决策 B
- 推荐决策

九、下一步建议

请给出下一步应该做什么，但不要直接修改代码。
