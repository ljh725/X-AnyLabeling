# 功能对照表维护方法（纯净版 vs 魔改版 代码对比法）

> 本文是《菜单功能对照表.md》和《魔改增强功能提炼.md》的**生成/更新方法说明**，可迁移到任何项目。
>
> - **输入**：① 纯净版源代码（官方基准）；② 魔改版源代码；③ 现有两张表；④ 用户说明文档（README# 编号来源）。
> - **输出**：更新后的两张表 + 一份《变更报告》（新增/状态变化/冲突清单）。
> - **核心原则**：纯净版是唯一判官。条目是否官方、是增强还是改写，全部以代码对比结果为准，不凭印象。

---

## 〇、锚点约定（整套体系的命脉）

表格中的每一行必须能通过"标识符"反查到代码。开工前先确认本项目 action 的注册模式：

- 本项目：`标识符 = action(` （位于 `anylabeling/views/labeling/label_widget.py`）
- 新项目：先 grep 找出等价模式（如 `addAction(`、`QAction(`、命令注册表等），把模式写进对照表头部"来源"一栏。

**标识符必须全表唯一、与代码字面一致**，这是机器可校验的唯一约束。

---

## 一、阶段 1：结构化提取（不逐行读大文件）

5千/8千行级别的文件**禁止线性通读**，全部通过 grep 先建立索引，再按需读窗口：

| 提取物 | 命令模式 | 产出 |
|---|---|---|
| action 注册点 | `grep -n " = action(" 文件` | 标识符 → 行号 清单 |
| action 明细 | 对每个命中点读 ±15 行窗口（action 调用常跨多行） | 标识符 + 显示文本 + 快捷键 + 槽函数 |
| 函数清单 | `grep -n "^    def \|^def "` | 函数名 → 行号 |
| 配置/快捷键项 | `grep -n "config\.\|setShortcut\|default_shortcut"` 等 | 隐形改动候选清单 |

**纯净版与魔改版各做一遍**，得到两套结构清单。后续所有对比都是清单对清单，不是全文对全文。

---

## 二、阶段 2：三层对比与状态判定

以**函数为最小对比单元**，按以下顺序逐层收敛：

### 第 1 层：action 层（找"有 UI 入口"的差异）

| 纯净版 | 魔改版 | 判定 | 状态 |
|:---:|:---:|---|:---:|
| 无 | 有 | 全新功能入口 | ✅魔改 |
| 有 | 有，槽函数代码一致 | 官方原样 | 📦官方 |
| 有 | 有，槽函数代码有实质差异 | 官方功能增强 | 🔧增强 |

> 实质差异的口径：改了行为/逻辑/默认值算增强；仅改注释、变量重命名、格式化**不算**。

### 第 2 层：非 UI 层（找"没有 UI 入口"的改动）

对应"有些改动没有菜单入口"的情况。对**不直接挂任何 action 的共享函数**做对比，按改动性质归类：

| 改动性质 | 归类 | 表中体现 |
|---|---|---|
| 改变现有交互手感（滚轮步长、拖拽行为、控制柄数量…） | 行为/手感增强 | 计入统计，无独立行 |
| 新增/调整快捷键、配置项 | 快捷键/配置 | 计入统计 |
| 修复原版缺陷 | BUG 修复 | 计入统计 |

### 第 3 层：跨文件改动

若魔改还在其他文件（dialogs、tools、模型层等）新增了可从菜单触发的功能，回溯其调用链：能从某个 action 一路调用到的，归属该 action 的行；完全独立入口的（如独立对话框类），在"其它菜单一览"节补充。

---

## 三、阶段 3：与现有表核对（增量更新，不推倒重写）

对比结果与现有《菜单功能对照表.md》逐条核对，分四类处理：

1. **新增条目** → 插入对应菜单节的正确位置，README# 若一时对不上先标 `—` 并记入变更报告。
2. **状态变化**（官方→增强、增强→魔改等）→ 更新状态列，记入变更报告。
3. **消失条目**（代码中已删）→ 从表中移除，记入变更报告。
4. **冲突条目**（现有表状态与代码对比结论不符）→ **以代码为准改表**，但必须在变更报告中列出冲突清单及证据（两边行号 + diff 摘要），供用户确认是不是历史口径问题。

---

## 四、阶段 4：统计与双表再生

1. **第 1 层（全量表）**：重算各节"原 N 项"、总统计、去重说明（同一 action 出现在工具栏+菜单+右键多处只算一次）。
2. **第 2 层（提炼表）**：**只允许从第 1 层机械过滤生成**，绝不单独手改；每节头部更新"原 N 项，提炼后 M 项"。
3. 更新两表头部"来源"注释和统计口径说明。

---

## 五、阶段 5：验证清单（交付前必查）

- [ ] 每个 ✅/🔧 条目的标识符在魔改版代码中 grep 可命中；
- [ ] 每个 📦官方 条目的标识符在纯净版中同样命中且槽函数 diff 为空；
- [ ] 抽样 10 条人工核对状态与 diff 内容一致；
- [ ] 两层表的合计数、去重口径互相吻合；
- [ ] 变更报告完整（新增 N / 状态变化 N / 删除 N / 冲突 N）。

---

## 六、交付物格式

| 交付物 | 内容 |
|---|---|
| 更新后的《菜单功能对照表.md》 | 全量表 |
| 更新后的《魔改增强功能提炼.md》 | 由第 1 层过滤再生 |
| 《对照表变更报告》（可附在回复中） | 四类变更清单 + 冲突证据 + 未决问题 |

## 七、开工前需要用户确认的三件事

1. 纯净版源码的路径，以及与魔改版的**文件对应关系**（哪些文件参与对比，尤其入口文件是否同名同路径）；
2. README# 编号本轮是否需要同步更新，还是只更新表、编号留 `—` 待补；
3. 非 UI 层改动（手感/快捷键/BUG 修复）本轮是否需要逐条列明细，还是只更新统计占比。
