# 标注项目总结

## Q1：什么是数据标注？

- **提问**
  - 你知道什么是数据标注吗？如果知道，请用大白话解释一下。
- **回答**
  - 知道。用大白话说：**数据标注，就是给一堆原始数据"贴标签、写答案"，让电脑/AI能看懂、能学。**
  - 比如：
    - 给一张照片，人用框把猫圈出来，写上"猫"——这就是图片标注。
    - 给一段录音，人把里面说的话打成文字——这是语音标注。
    - 给一句评论"这手机真好用"，标上"正面/好评"——这是文本标注。
    - 给一段开车视频，把车、行人、红绿灯都圈出来——这是自动驾驶里的标注。
  - 打个比方：
    - 你想教小孩认猫，你会指着猫说："这是猫。"
    - 数据标注就是干这个，只不过不是教一个小孩，而是**成千上万次地给AI当老师、给答案**。
  - AI本身一开始啥也不懂，它需要大量"带答案的例子"来学习。原始数据就像一堆没整理的材料，数据标注就是把这些材料做成**AI能用的教材和习题答案**。
  - 一句话总结：**数据标注 = 人工给数据加说明、贴标签，把"乱七八糟的数据"变成"AI能学习的带答案资料"。**

## Q2：目标检测标注的最短流程

- **提问**
  - 用简短的大白话说一下：图像标注中，目标检测标注的最短流程是什么样的？
- **回答**
  - 最短就这几步：
    1. 打开图片。
    2. 找到要标的东西。
    3. 用方框把它框住。
    4. 选好它是什么，比如人、车、猫。
    5. 保存，换下一张。
  - 一句话：**看图 → 框目标 → 打标签 → 保存。**

## Q3：标注员在前三步中起什么作用

- **提问**
  - 这4个步骤中，最后一步保存可以自动进行，我们把它排除掉。前三个步骤里，看图、画框、给标签，人工标注员在这个过程中起到什么作用？还是用大白话解释。
- **回答**
  - 人工标注员就干三件事：
    1. **看图**：判断图里有什么，哪些要标，哪些不用标，遮挡、模糊的怎么算。
    2. **画框**：决定目标在哪、框多大、边界卡到哪，尽量框准。
    3. **给标签**：判断这个东西到底是什么，选对类别，比如人、车、猫。
  - 一句话：**目标检测就是教AI"哪里有啥"。人工标注员负责看懂、圈准、认对，给AI标准答案。**
  - 保存只是自动记下来，不负责判断。

## Q4：标注是否等于"主观判断转数据"

- **提问**
  - 我能不能理解为：人是将主观上的判断转化为数据的过程？
- **回答**
  - 可以，八九不离十。
  - 更准确地说：**人是把"自己看懂图片后的判断"，按照统一规则，翻译成机器能读的数据。**
  - 比如目标检测里：
    - 看图：判断图里有没有车、人、猫。
    - 画框：判断它在哪、多大，转成坐标数据。
    - 给标签：判断它是什么，转成类别数据，比如"车"。
  - 所以本质就是：**人做判断 → 按规则整理 → 变成数据给AI学。**
  - 但要注意两点：
    1. **不是纯主观瞎猜**，得按标注规则来，不然每个人标得不一样，AI就学乱了。
    2. 它更像是把"人的认知判断"**结构化、客观化**，变成机器能吃的答案。
  - 一句话：**可以理解为人把判断翻译成数据，但是"按规则翻译"，不是随便发挥。**

## Q5：单个对象的操作步骤拆解

- **提问**
  - 落实到具体的标注过程中，一个标注人员在一张图片中将一个对象辨识出来，画出相对准确的对象框，再给予对应的标签。这个过程在操作上大概需要几个步骤？
- **回答**
  - 落实到具体操作，大概可以拆成 **5～6 个小步骤**：
    1. **扫图找目标**：看整张图，找到要标的东西。
    2. **判断是什么**：心里先确定它是人、车、猫还是别的。
    3. **拖出框**：在目标上拉一个方框。
    4. **调整框**：拉边角，让框尽量贴紧目标，不大不小。
    5. **选标签**：在类别列表里点对应的标签。
    6. **核对确认**：看一眼框和标签对不对，不对就改，然后确认/下一张。
  - 所以最核心就是：**找目标 → 判类别 → 画框 → 调框 → 打标签 → 核对。**
  - 如果熟练，画框和调框可能一气呵成，但逻辑上大概是这几步。保存自动不算。

## Q6：判断层与操作层的两分法

- **提问**
  - 在这几个步骤中，找目标、判别应该属于视觉判断层面；画框、调框、给标签属于手动操作层面。可以分为两个部分，这样可以理解吗？
- **回答**
  - 可以，基本能这样理解，但别把它切成完全分开的两段。
  - 更准确地说：
    - **视觉判断层**：找目标、判断是什么、判断边界大概在哪。
    - **手动操作层**：画框、调框、点选标签、确认。
  - 但要注意：
    1. **给标签**表面是点选，背后其实也有判断，只是判断完再点。
    2. **画框、调框**虽然是手上动作，但眼睛和脑子一直在参与，比如框大了小了、边界对不对。
    3. 实际操作不是"先全判断完再操作"，而是**边看、边判、边画、边改**。
  - 所以可以粗略理解成：**脑力判断 + 手上操作 = 标注过程。**
  - 一句话：**可以分成"判断"和"操作"两部分来理解，但它们在实际标注中是互相配合、来回切换的。**

## Q7：如果由 AI 划分步骤

- **提问**
  - 如果刚才的5~6个步骤由你来划分，你会怎么划分？
- **回答**
  - 如果让我来划，我会砍成 **4步：找、认、框、查**。
    1. **找**：扫一遍图，看哪些目标要标，哪些不用标。
    2. **认**：判断它是什么，然后在类别里选对应标签。
    3. **框**：画框、调框，让框尽量贴紧目标。
    4. **查**：核对有没有漏标、框歪没歪、标签对不对，确认。
  - 这样划的好处是：
    - "判断是什么"和"选标签"合并，因为选标签只是把判断结果点出来。
    - "画框"和"调框"合并，因为实际是一气呵成的动作。
    - "核对"单独拿出来，因为它是最后的质量把关。
  - 如果按层面理解，就是：**脑判断（找、认、查） → 手操作（框） → 眼脑再复核（查）。**
  - 一句话：**我会分成"找目标、认目标、框目标、查结果"四步。**

## Q8：耗时判断——手操作是否更费时间

- **提问**
  - 从我的标注过程中累积的经验看，我没有相对准确的数值统计。但从感受上来说，认、找、查、对比框和点选标签，脑判断的部分相对来说没有那么费时间，手操作的部分更费时间。你是否认可这个判断？如果不认可，给出你的判断。
- **回答**
  - 我**大方向认可**，但不想说得太绝对。更准确地说：**在多数普通目标检测标注里，手操作通常是主要耗时，尤其是画框和调框。** 认类别、找目标、点标签，熟练后往往比较快，甚至接近条件反射。
  - 我的判断大概是这样：
    - **常规图、目标清楚、类别少、规则明确**：**画框+调框 > 找目标/查漏 > 认类别 > 点标签**。这时候你说的"手操作更费时间"基本成立。
    - **复杂图，比如目标小、密集、模糊、遮挡、类别多、规则细**：脑判断可能反超，尤其是"找目标"和"查漏"会很耗时。这时候不是手慢，是眼睛和脑子先卡住了。
  - 另外，"点选标签"虽然算手操作，但通常不是大头，除非类别特别多、要翻列表、要记快捷键。"对比框"其实一半是眼睛判断，一半是手调，也常常很耗。
  - 所以我的结论是：**你的感受在常见标注场景下是对的：手操作更费时间，画框调框通常是大头。** 但更准确的说法是：**不是脑判断不费时间，而是熟练后脑判断被压缩了，手操作成了瓶颈；任务一复杂，脑判断又会冒出来变成大头。**

## Q9：类别数、实例数与耗时的递增关系

- **提问**
  - 你的结论我是认可的。当图片中的标签类别过多时，大脑的判断会变慢；标签数量更多时，也会更慢；标签在图片中的显示相互叠加时，判断也会更慢。
  - 我目前比较有效的处理方式是每次只做一个类别。当前我有一个学生动作的6类标签，每张图中有30个学生。这种时候，我每次只做一个动作，5000张图里面相当于要走6轮。但这也比一次性处理6类更简单一些，标签通过筛选之后会少很多。还有后续就是数量过多之后，操作也会更慢。这是个递增的关系，数量、类别的增加和数量的增加都会引起头脑辨识和手动操作的时间增加。
- **回答**
  - 认可，而且你这个观察很实战。更准确地说：**类别数增加、实例数增加、标签叠加，会让"脑判断"和"手操作"一起变慢，而且不是简单相加，是互相放大。**
  - 可以这样理解：
    - **类别多**：标签列表长、容易混，大脑决策变慢。
    - **实例多**：每个都要找、框、确认，手操作次数直接涨。
    - **标签叠加**：视觉干扰大，找目标和核对更费眼、更费脑。
    - **两者叠加**：比如6类×每张30人，认知负荷和操作负荷一起上来，时间会明显增加。
  - 你现在的做法——**每次只做一个类别，5000张走6轮**——本质上是在做"降维"：
    - 把一次"6分类判断"拆成6次"是不是这个动作"的二分类判断。
    - 好处是每轮标签少、干扰少、判断更清晰，准确率和心理负担都会好一些。
  - 但代价也很明显：
    - 每张图要看6遍；
    - 定位、扫图、切换轮次都有重复成本；
    - 总时间不一定最少，但可能更稳、更不容易错。
  - 所以你的策略可以总结成：**用"多轮重复"换"每轮更清晰"。**
  - 如果动作之间容易混、标签叠加严重，这个策略很合理。如果动作区分很明显，也可以考虑2～3类一组，减少轮次，省总时间。
  - 一句话结论：**你说得对，类别和数量增加都会让标注变慢，而且是递增的；单类多轮是实战中很有效的降维办法，但它是拿重复看图换判断清晰度。**

## Q10：标签前置是否已是最短路径

- **提问**
  - 在操作层面，我想的、能够减少时间的方式叫做标签前置。我用一个功能，将数字键1~9对应9个标签。在人脑辨识完成对象之后，我便通过数字键先获得标签，然后再进入标框阶段。标完之后，这个框会自己将标签填入。这样会比之前的步骤节约一些时间。但从流程上讲，似乎没有更多方式了。手动标注上，好像这样已经是最短路径了。
- **回答**
  - 认可，你这个"标签前置"是很实战的优化。
  - 它的本质是：**把"画完框再选标签"变成"先按数字键拿标签，再画框，框自动填标签"。**
  - 好处是：左手键盘选标签，右手鼠标画框，鼠标不用在画布和标签列表之间来回跑，手眼切换少，所以感觉更快。
  - 但严格说，它不一定是"步骤变少"，而是**动作更顺、切换更少**。流程大概是：**看目标 → 按数字键选标签 → 画框/调框 → 自动填标签 → 确认下一个。**
  - 如果是在**多类混合、纯手动、逐目标标注**的情况下，这确实已经非常接近最短路径了。
  - 不过，结合你说的"每次只做一个类别，5000张走6轮"，其实还有更短一招：**锁定当前类别。**
    - 既然这一轮只标"举手"，那就把"举手"设成当前标签，画一个框自动就是"举手"，根本不用每个目标都按数字键。
    - 流程变成：**看目标 → 画框 → 自动填当前标签 → 下一个。**
    - 这比"按数字键 → 画框"还少一步。
  - 所以结论是：
    - **多类混标**：标签前置基本是最短路径之一。
    - **单类多轮**：锁定标签直接画框更短。
    - 再想快，就只能靠工具辅助了，比如自动吸附边缘、复制上一帧框、AI预标注人工修正，那些就不算纯手动了。
  - 一句话：**你的方向对，标签前置很好；但"最短"要看场景。单类轮标时，锁定类别直接画框才是更短路径。**

## Q11：手动标注还有哪些提速方式

- **提问**
  - 对于手动标注，你认为还有什么可以缩短时间的方式吗？
- **回答**
  - 纯手动标注到你这个程度，已经接近极限了。再想缩短，主要不是"减少步骤"，而是**减少手移动、减少重复劳动、减少犹豫、减少切换**。
  - 可以试这些方向：
    - **1. 锁定当前类别，比标签前置更短**
      - 单类轮标时，直接把当前类锁死，画框自动填标签，连数字键都不用按。
      - 流程变成：**看目标 → 画框 → 微调 → 下一个。**
    - **2. 全键盘化，少动鼠标**
      - 数字键选标签，Enter确认，Tab下一张。
      - 方向键微调框，Shift+方向键加速，Ctrl+方向键改大小（看工具支持）。
      - 滚轮缩放，空格拖拽画布，Esc取消，Delete删除。
      - 把常用键映射到鼠标侧键或脚踏板。
    - **3. 复制框、复用框**
      - 复制上一个框，粘贴后微调。
      - Alt拖拽复制框。
      - 如果是视频抽帧或相似图，复制上一张的框，再删改。
      - 学生大小差不多，设默认框尺寸，单击出框，再微调。
    - **4. 视觉减负**
      - 只显示当前类标签，隐藏其他类。
      - 当前类高亮，其他类灰掉。
      - 标签按频率排序，常用放1~3键。
      - 分块扫描，把图分四块或九块，逐块找，减少漏标和重复扫视。
      - 小目标先放大再标。
    - **5. 流程优化**
      - 先粗标后精调：先快速框完，再统一调框。
      - 先易后难：简单图快速过，难图集中处理。
      - 质量检查后置：不要边标边查，分批抽查。
      - 规则明确：遮挡多少算标、边界怎么定，提前定死，减少犹豫。
      - 如果动作区分明显，可以尝试2~3类一组，减少轮次；如果容易混，就继续单类轮标。
    - **6. 半自动辅助（实际最省时）**
      - 预标注：模型先跑一遍，人工只改错。
      - 边缘吸附、智能框、自动贴合。
      - 视频跟踪、关键帧插值。
      - 自动下一张、自动保存。
  - 最推荐的组合是：**锁定类别 + 默认框 + 方向键微调 + 复制框 + 自动下一张 + 预标注修正。**
  - 一句话：**纯手动还能压榨的是"手移动"和"重复画框"；再快，就得靠工具和AI预标注了。**

## Q12：AI 预标注工作流的结构分析

- **提问**
  - 我有一个场景：学生6类标签，5000张图片，总计6类标签、20万个对象，都是由模型预标注的。我的工作内容便从标注框变成了修改两大类：1. 修改标签类别。2. 修改检测框本身。整体操作逻辑没有大变化，还是头脑辨识加手动操作。
  - 标签修改：选中标签后可以修改，也可以通过数字快捷键1~9来修改。因为有一个前置的选中对象动作，所以不会和标注产生冲突。为了应对某一类中有2万个标签的情况，我按照现有标注框，在标注框周围做了截取，也就是以人物为目标做一个小框截取，做成缩略图形式。这样可以一次性检查超过100个目标，效率是有提升的。
  - 这是针对标签类别的处理，但最耗时的还是检测框、矩形框本身的修改。因为这个动作属于直接修改坐标值，需要给入老辨识出来的目标边界。我们也可以说，就是目标检测不准，也就是目标边界不准。在给定边界之后，程序其实并不知道我给入的参数对应的是某一条边。这个时候，我还需要给程序具体修改哪一条边界框的指令。这是目前的两个部分。你分析一下这两个流程，从目前的描述来看，有没有结构问题或逻辑缺陷？
- **回答**
  - 有，而且不是小问题。你现在的流程能跑，但结构上**不完整**，逻辑上也有**顺序反了**的地方。
  - **一、标签修改流程的问题**
    - 你用"按现有框截缩略图 + 批量检查 + 数字键改标签"，效率确实高，但有一个大前提：**它默认框是准的。** 可现实是框经常不准。于是会出现：
      1. **用错框做缩略图，标签判断被污染**：框偏了、框大了、框小了，缩略图就可能切掉头、切掉手、带进别人。动作类别判断很依赖身体姿态，缩略图一残，标签就容易判错。
      2. **缩略图丢了上下文**：如果动作是单人动作，缩略图还行；如果动作跟周围人、空间位置有关，缩略图就不够。批量看100个很爽，但可能看得越快，错得越顺。
      3. **只处理已有框，漏检对象永远不出现**：模型没检出来的人，不会进缩略图，也不会进你的标签修改流程。最后数据里就少人。误检框反而可能被你改个标签留下来。所以你的流程里其实缺了两步：**补漏检、删误检**。
      4. **批量检查缺少状态追踪**：一次100个，哪些改过、哪些没改、改到哪一批，如果没有明确标记，容易重复或漏掉。
  - **二、框修改流程的问题**
    - 你说框修改最耗时，而且程序不知道你输入的值对应哪条边，还要额外指定边。这里逻辑上有个明显问题：**你把"空间操作"变成了"数值输入 + 边指令"。**
    - 正常图形界面应该是：**先选边 → 再拖/调 → 程序自动知道你在改哪条边。**
    - 你现在是：**先给值 → 再告诉程序这是哪条边。** 顺序反了，所以多了一步。不是程序笨，是交互设计缺了"先选操作对象"这个状态。应该做成：拖边、拖角、方向键调边，程序自然知道改的是左、右、上、下。
    - 另外，框不准不止一种：
      - 整体偏了：该拖整体。
      - 单边不准：该拖单边。
      - 大小不对：该缩放。
      - 框进两个人：该拆分或重画。
      - 完全错：该删了重标。
    - 你现在如果都按"改坐标值"处理，等于把所有错误塞进同一种操作，肯定慢。
  - **三、整体结构上的大问题**
    - 你只列了两大类：**改标签、改框**。但 AI 预标注后，人工要处理的其实是四类：1. 改标签；2. 改框；3. 补漏检；4. 删误检。后两类没进流程，结构就不完整。
    - 还有一个依赖问题：**框不准 → 标签缩略图不准 → 标签判断不准。** 所以先批量改标签、再改框，可能顺序不对。更合理的是：**先补漏删误 → 再粗修框 → 再批量核标签 → 最后精修框 → 抽检。**
  - **四、一句话总结**
    - 有结构问题，也有逻辑缺陷：
      - **结构缺陷**：只修不增删，漏检误检没位置处理。
      - **逻辑缺陷**：用不准的框生成标签检查单元；框修改让程序先收数值再猜边，顺序反了。
      - **流程缺陷**：标签和框分开处理，但错误是耦合的，应该按对象和错误类型分流。
    - 你不是操作不够快，是流程里有些步骤本来就不该由人硬扛。

## Q13：缩略图与框修改流程的补充讨论

- **提问**
  - 先说缩略图修改的问题。第三个，漏检对象处理不到。这个我是知道的，因为它是根据已有的数据进行裁剪，所以说没有检出来的，没有办法，这个只能再走一轮。批量检查缺少状态追踪，这个已经实现了，但我不太会怎么操作。我都是手动记录已经做到某一页，或者具体是哪一张图片。框的准确性相对还是比较高的，能够通过框判断出目标的动作是哪一类。
  - 最后一个缩略图上下文丢失的问题，这是做了一个跳转的功能。某一张图我不知道它具体功能的时候，我会点击这张缩略图，它会直接跳转到这个对象，它是在哪一张图上的某一个位置，并且会将这个对象的标签名高亮。也就是说，我可以通过这个方式来追回上下文。这个功能目前比较明显的错误是：把几十页里的标签都集中在一起展示。我发现，同一个目标如果出现了两次，检测时都会把这个目标截出来。也确实因为缺失上下文，所以这个问题比较难发现，是在后面逐章检查的时候才发现这类问题的。最后通过脚本进行 IoU 检测，才将这种重复检测框去除掉。
  - 关于框的修改：手动流程中，选中框后，再选择某一个角或者某一条边，然后拖动到指定位置。这样做其实是最简单、最方便的。但实际操作中有一个比较明显的问题，而且我也无法克服：手会抖。手的拖动是一个连续状态，但边界点是离散状态，我没有办法每次都将框准确地拖到边界上。就算我已经正确认识到了这个点，产生的结果也是第一轮粗调之后，还需要两轮到三轮把这个框调精细。这样一分钟只能做4~8个对象。而我使用数值输入加上键盘指令，或者叫边指令的方式，最大的耗时其实是在准确给出边指令上。虽然流程上可能会多一步，但实际耗时反而更少。
  - 补漏标和删误检这两个动作，前面的方式没办法操作，都是逐张图片进行修改。以上就是这次工作的全部流程和结构。
- **回答**
  - 你的补充把很多问题说清楚了。我重新梳理一下，只指出现在真正还剩的结构性问题。
  - **一、缩略图流程**
    - 你已经解决了：跳转追上下文、手动记录进度、IoU 脚本去重。
    - 现在真正的结构性问题只剩一个：**缩略图视图天然丢上下文，导致"重复框"这类错误只能在后期靠脚本发现。**
    - 这不是你操作的问题，是视图设计的问题。缩略图把对象从原图里抽出来，按标签集中展示，好处是批量快，坏处是：
      - 同一张图的两个框截同一个目标，你看不出来；
      - 同一目标被检两次，缩略图里就是两张很像的小图，你不容易发现；
      - 只有回到原图，或者靠 IoU 脚本，才能抓出来。
    - 所以流程上缺的是：**在缩略图视图里加一个"来源图分组"或"同图相邻显示"。**
      - 比如同一张图的缩略图排在一起，或者缩略图上带一个小角标显示"第几张图、第几个框"。
      - 这样你不用跳转，也能一眼看出"这两个框来自同一张图、位置还重叠"。
    - 一句话：**你不是缺检查，是缺"同图上下文"在缩略图里的可见性。**
  - **二、框修改流程**
    - 你说手动拖框会手抖，数值+边指令反而更快。这个我认可。
    - 但这里有一个关键点：**你现在慢，不是因为数值输入慢，是因为"边指令"要额外给。**
    - 如果工具支持：先点左边 → 再输入数值 → 程序自动改左边；或者选中框后，方向键直接调某条边；或者鼠标滚轮微调边界——那就不需要"输入值 + 告诉程序是哪条边"这两步了。
    - 你现在的操作是：**选中框 → 判断改哪条边 → 输入数值 → 告诉程序改哪条边 → 确认**
    - 理想操作是：**选中边 → 输入数值 / 按方向键 → 完成**
    - 少的就是"告诉程序改哪条边"这一步。这不是你手速的问题，是交互设计没有"先选边"这个状态。
    - 所以框修改的结构问题是：**边指令和数值输入没有合并成一个动作。**
  - **三、补漏标和删误检**
    - 你说这两个只能逐张图做，这是对的。但这里有一个结构性问题：**你把"补漏"和"删误检"放在了流程最后。**
    - 如果预标注召回率不高，漏检多，那最后补漏会非常痛苦。更合理的是：**预标注后先快速过一遍"有无明显漏检/误检"，再进入标签和框的精修。**
    - 因为漏检和误检会直接影响你对标签和框的判断。一张图少了一个人，你在缩略图里根本不知道；一张图多了一个误检框，你可能会给它改个标签留下来。
    - 所以流程顺序上：**先补漏删误 → 再批量核标签 → 再精修框 → 最后抽检**，比现在的顺序更稳。
  - **四、整体结构总结**
    - 你现在的流程是：**预标注 → 缩略图批量改标签 → 逐张改框 → 逐张补漏删误 → IoU 去重**
    - 结构性问题有三个：
      1. **缩略图缺同图上下文** → 重复框只能后期脚本抓。
      2. **框修改缺"先选边"状态** → 边指令多一步，拖框又手抖。
      3. **补漏删误放在最后** → 漏检误检会影响前面的标签和框判断。
    - 如果只能改一个，我建议先改第 2 个：**让工具支持"选中边 → 方向键/数值微调"，程序自动知道改哪条边。** 这个对你每分钟 4~8 个对象的瓶颈，提升最直接。
    - 一句话：**你的操作已经很快了，现在卡你的是工具交互缺状态，不是人手不够快。**

## Q14：巨量数据下的整体流程优化

- **提问**
  - 还有一个问题，就是总的标签类别和标签的总数量。就像前面说的，5000张图片、20万个对象，也就是有20万个标签。我现在的操作是这样的：先查看6类标签的分布数量情况。当前这个数据集中，有一类动作最少，总共只有562个标签；第二少的是11000个标签，第三个是21000个。剩下的三类标签分别是38000、37000、44000个。
  - 数据分布中，我先把最少的几类依次做了，剩下的换了一个标准。我发现某一类动作经常会被简称为另一类动作，所以就把这类动作和另一类动作先后处理。比如 listening 和 raise hand，也就是听写和举手，容易相互检错，我就把这两类挨着做了。然后因为听写的辨识难度比较低，这也是一个维度。但是听写、举手，还有一个未标定动作，这是三类。这三类花了很久时间。
  - 我的问题是，在这种巨量数据标签的情况下，除了前面讨论的单个对象修改流程可以提速之外，还有没有其他方法可以优化这个流程？如果单个标注速度作为一个基数，这没有办法再改了，那还有什么方式可以减少总耗时？应该是结构上的问题。
- **回答**
  - 有。你现在卡的不是"单个对象怎么改"，而是**总工作量怎么被结构放大**。
  - 如果单对象速度是固定基数，总耗时大概等于：**总耗时 = 对象数 × 单对象耗时 × 重复查看系数 × 判断难度系数 + 切换成本**。你能优化的，就是后面三个系数。
  - **一、最大结构问题：按类别轮播，同一张图被看很多遍**
    - 5000张图、6类，你每次只做一类，相当于：**每张图至少看6遍。** 这就是最大的隐藏成本。单对象再快，也扛不住重复看图、重复定位、重复进入状态。
    - 优化方向：**把"按类别轮播"改成"按图分组，混合处理"。** 但不是全混合，而是：**主类轮播 + 混类合并。**
    - 具体做法：
      - 数量大的类，比如44000、38000、37000，继续单类轮播。
      - 数量小的类，比如562、11000、21000，合并成一轮或两轮处理。
      - 容易混的类，比如 listening 和 raise hand，放在同一轮里做，但用**成对判断**，而不是分开两轮。
    - 这样可以把6轮压到3～4轮，直接减少重复看图次数。
  - **二、第二个结构问题：没有利用模型置信度**
    - AI预标注通常会输出置信度。如果工具能按置信度排序，你就可以：
      - **高置信度**：快速扫，不改或只抽检。
      - **中置信度**：重点看。
      - **低置信度**：仔细改。
    - 这样不是20万个对象平均用力，而是：**把人力集中在模型不确定的对象上。** 如果模型还行，可能只有20%～30%的对象需要你真正动手。这就是结构上省时间，不是手速问题。
  - **三、第三个结构问题：容易混的类，顺序可能反了**
    - 你说 listening 和 raise hand 容易混，所以放一起做。这个方向对，但要看你怎么放：
      - 如果两类都少，放一起做，成对判断，很快。
      - 如果两类都多，放一起做，大脑一直做精细区分，会很累，反而慢。
    - 更优的是：**先做容易的类，建立判断标准；再做容易混的类，用标准去套。**
      1. 先做数量最多、特征最明显的类。
      2. 再做数量中等、特征清楚的类。
      3. 最后做容易混的类，这时候你脑子里已经有清晰对比了。
  - **四、第四个结构问题：兜底类会拖慢整体**
    - "未标定动作"这种类，判断标准模糊，每遇到一个都要犹豫。它会严重拉高判断难度系数。
    - 优化方式：
      - 把它放到最后做，不要混在中间。
      - 给它单独定死规则：什么情况算未标定，什么情况不算。
      - 如果规则实在模糊，先跳过，最后集中处理。
  - **五、第五个结构问题：补漏删误放在最后**
    - 这个前面说过，但放在总量20万的场景下更严重：**漏检和误检会污染标签判断。** 比如一个误检框，你给它改了标签，等于把错误留下来了。后面再查，成本翻倍。
    - 更合理的是：**预标注 → 快速补漏删误 → 再批量核标签 → 再精修框 → 抽检。** 补漏删误不需要精修，只要判断"有没有多、有没有少"，速度很快。
  - **六、总结：结构上能省时间的五个方向**
    1. **减少轮次**：小类合并，容易混的类成对做，别6类走6轮。
    2. **利用置信度**：高置信度快过，低置信度重点改。
    3. **调整顺序**：先易后难，兜底类最后。
    4. **补漏删误前置**：别让错误污染后面的标签和框。
    5. **同图上下文可见**：缩略图按图分组，减少重复框和漏检。
  - 一句话：**单对象速度你已接近极限，再省时间要靠减少重复看图、减少无效对象、减少判断犹豫。** 这三件事都是结构问题，不是手速问题。

## Q15：流程总结的确认与修正

- **提问**
  - 我总结一下你说的，你看我的总结对不对？首先拿到数据之后先删误检和补充漏检，这样5000张先走一轮，然后就是按照标签类别进行合并的处理，也就是说不要每一类都走一遍，应该按照容易误检的类走，或者是从数量少的先做。这个我需要你详细再讲一下，关于置信度，这个不一定有用。实际情况中，有些类别的置信度都不高，大部分有些只有零点四几的置信度。第三个结构中，你说先做容易的类，建立判断标准，再做容易混的类。又说先做数量最多，特征最明显的类，到这里，我是否能够理解：首先应该做容易辨识的，而不是把数量放在第一位？那假如最容易辨识的数量最多，也是这样进行判断吗？
- **回答**
  - 你的总结大方向对，但有几个点需要修正和细化。
  - **一、先纠正你的总结**
    - **第一点：先补漏删误，对。** 但要注意：这一轮不需要精修，只判断"多了还是少了"。速度应该很快，不要在这一轮改标签、精调框。如果精修了，后面还得再改。
    - **第二点：不每类走一遍，对。** 但"按容易误检的类走"和"从数量少的先做"是两套逻辑，不能混在一起说。下面详细讲。
    - **第三点：置信度，你说得对。** 如果大部分只有0.4几，置信度确实没用。它区分度太低，按它排序等于没排。这一条可以放弃。
    - **第四点：先做容易的，还是先做数量多的？** 你问到了关键。我前面说得不够清楚，现在重新说。
  - **二、类别处理顺序，到底怎么排**
    - 原则是：**先做"特征明显"的类，不管数量多少。在特征明显的类里，优先做数量多的。特征模糊、容易混的类，放后面。**
    - 具体排序：
      1. **特征最明显、数量最多的类**：先做。因为好认，做起来快，还能快速建立判断标准。
      2. **特征明显、数量少的类**：接着做。反正量少，很快过完，顺便把标准补全。
      3. **容易混的类，成对做**：放中间。比如 listening 和 raise hand，放同一轮，成对判断。
      4. **特征模糊的类、兜底类**：最后做。比如"未标定动作"，规则最模糊，放最后集中处理。
    - 所以回答你的问题：
      - **如果最容易辨识的类数量最多，那就先做它，没矛盾。**
      - **如果最容易辨识的类数量很少，也先做它，因为快，还能建立标准。**
      - **数量不是第一优先级，辨识难度才是。**
    - 一句话：**先做"好认的"，好认的里面先做"量大的"；难认的放后面。**
  - **三、为什么"从数量少的先做"不完全对**
    - 你前面说"先把最少的几类依次做了"，这个策略在**单类轮播、每类走一遍**的前提下是对的，因为量少，快速清掉，心理负担小。但在**合并处理**的前提下，就不一定了。
    - 如果最少的那类恰好很难认，你先做它，等于一开始就啃硬骨头，标准没建立，效率低，还容易错。
    - 所以：
      - **量少 + 好认**：先做。
      - **量少 + 难认**：不急，放后面。
      - **量多 + 好认**：优先做。
      - **量多 + 难认**：中间做，等标准清晰了再啃。
  - **四、合并处理具体怎么合**
    - 你说6类走6轮，我们目标是压到3轮左右：
      - **第一轮：好认的类合并做。** 把特征明显、不容易混的类放一起，比如3类。缩略图按类分组，但同一轮里可以切换。
      - **第二轮：容易混的类成对做。** 比如 listening + raise hand，放同一轮。缩略图混排，逼自己成对判断，反而比分开做更准。
      - **第三轮：兜底类、模糊类集中做。** 比如"未标定动作"，最后单独处理。
    - 这样6轮变3轮，重复看图次数直接减半。
  - **五、补漏删误前置，具体怎么做**
    - 不是让你5000张精修一遍，而是：**预标注 → 快速扫一遍，只做两件事：1. 明显漏检的补上。2. 明显误检的删掉。**
    - 不改标签，不调框，不纠结边界。这一轮可以很快，目标是**让数据里没有"多一个人、少一个人"这种大错**。然后再进入标签和框的精修。
  - **六、最终总结**
    1. **先补漏删误**，快速过一遍，不精修。
    2. **类别合并处理**，6轮压到3轮。
    3. **顺序：好认的先做，好认里量大的优先；难认的、容易混的、兜底的放后面。**
    4. **置信度没用就放弃**，不纠结。
    5. **单对象速度不变，省时间靠减少轮次、减少无效对象、减少犹豫。**
  - 一句话：**先清大错，再按"好认优先、量大优先"合并轮次，难认的放最后。**

## Q16：四项分类如何套用到 6 类标签

- **提问**
  - 这里面有一个量少量多好做难认的四项分类，我的问题是：6类标签怎么用这4个来框呢？假如说，好认量多里面有两类，这两类是放到一起做吗？另外，从6轮压缩到3轮，你举个例子说明一下，应该怎么压缩？我不太明白。
- **回答**
  - 可以，四项分类不是四个筐，是给6类标签**打两个标签**：好认/难认、量多/量少。
  - 然后按优先级排：**好认量多 > 好认量少 > 难认量多 > 难认量少**，但容易混的类要单独成对，兜底类最后。
  - **一、6类怎么套四项分类**
    - 假设你的6类大概是：

      | 类别 | 量 | 辨识 | 备注 |
      |---|---|---|---|
      | 讨论 | 44000 | 好认 | 量多 |
      | 写作 | 38000 | 好认 | 量多 |
      | 阅读 | 37000 | 好认 | 量多 |
      | 举手 | 21000 | 难认 | 和听写易混 |
      | 听写 | 11000 | 难认 | 和举手易混 |
      | 未标定 | 562 | 难认 | 兜底类 |

    - 套进去就是：
      - **好认量多**：讨论、写作、阅读
      - **难认量中**：举手、听写，而且易混
      - **难认量少**：未标定，兜底
    - 所以处理顺序是：
      1. 先做好认量多的：讨论、写作、阅读
      2. 再做易混对：举手 + 听写
      3. 最后做兜底：未标定
  - **二、好认量多里面有两类，放一起做吗？**
    - **可以放同一轮做，但不是混在一起瞎标。** 放同一轮的意思是：同一张图，一次把这几类都检查掉，不用关掉再开、不用重新扫图。
    - 具体有两种做法：
      - **做法A：工具支持同图多类显示**。一张图同时显示讨论、写作、阅读的预标注框。你在一张图里把这几个类都过完。这样5000张只走一遍，处理3类。
      - **做法B：工具只支持单类筛选**。那就同一轮里按顺序切：先筛讨论，过完；再筛写作；再筛阅读。但不用重新打开图片、重新进入状态。省的是切换成本，不是查看遍数。
    - 所以：**好认量多的两类，可以放同一轮，但最好按类分组过，不要全混在一屏。**
  - **三、6轮怎么压到3轮？举个例子**
    - 原来的6轮：
      - 第1轮：只做讨论，5000张走一遍
      - 第2轮：只做写作，5000张再走一遍
      - 第3轮：只做阅读，5000张再走一遍
      - 第4轮：只做举手，5000张再走一遍
      - 第5轮：只做听写，5000张再走一遍
      - 第6轮：只做未标定，5000张再走一遍
      - 总共：**6遍图**。
    - 压缩后的3轮：
      - **第1轮：好认量多合并**（讨论 + 写作 + 阅读）。如果工具支持多类显示，一张图一次看完这三类。如果不支持，就同一轮里切筛选，但不用重新开图。这轮处理3类。
      - **第2轮：易混对成对做**（举手 + 听写）。一张图同时看这两类，成对判断：是举手？是听写？还是都不是？这轮处理2类。
      - **第3轮：兜底类最后做**（未标定）。规则最模糊，放最后集中处理。这轮处理1类。
      - 总共：**3轮**。
  - **四、关键前提**
    - 能不能真从6轮变3轮，取决于工具：
      - **工具能同图多类显示**：才能真正5000张走3遍。
      - **工具只能单类筛选**：那缩略图可能还是每类过一遍，但工作轮次从6个变成3个，切换和重新进入状态少了。
    - 所以最理想的是：**同一张图，一次显示多个类的预标注框，人工按类分组检查。**
  - 一句话总结：**四项分类是排序工具：好认量多先做，好认量少跟着做，难认量多中间做，难认量少/兜底最后做。好认量多的几类可以放同一轮，按类分组过。6轮压3轮就是：好认类一轮、易混对一轮、兜底类一轮。**

## Q17：多类同显下的框遮挡与调框速度

- **提问**
  - 首先工具可以做到多类别同时显示。假如好认的两类数量多，好认不代表它们的检测框都是准的。那检测框之间的相互遮挡应该怎么做呢？如果检测框需要调整，就看具体调框的速度吗？
- **回答**
  - 多类同时显示，适合"核标签、补漏删误"，**不适合精调框**。框一多、一叠，眼睛先乱，手更慢。所以关键不是"怎么在乱框里硬调"，而是**分阶段、分显示状态**。
  - **一、框相互遮挡，怎么处理？**
    - 先分清两种遮挡：
      - **1. 只是视觉上框叠框**：就是多个矩形重叠，看不清边界。处理办法：
        - 只显示边框，不填充颜色。
        - 当前框加粗、高亮，其他框变细、半透明。
        - 一键隐藏其他类，只留当前类。
        - 再进一步，只留当前框，其他框全隐藏。
        - 用对象列表点选，选中哪个，哪个自动置顶。
        - 调完一个就锁定一个，避免误碰。
        - 一句话：**像PS图层一样，当前层置顶，其他层锁住或隐藏。**
      - **2. 框本身重复或误检**：两个框框的是同一个目标，或者一个目标被检成两个类。处理办法：
        - 先跑IoU去重，重复框直接删。
        - 同一目标多个类，按互斥规则保留一个。
        - 误检框直接删，不要改标签硬留。
      - **3. 真实目标被遮挡**：比如学生被桌子挡住、被前面的人挡住。这个不是工具问题，是规则问题。提前定死：框可见部分，还是框完整目标？规则定了，调框才不犹豫。
  - **二、好认的两类量多，能一起做吗？**
    - 可以一起**核标签**，但精调框最好分开。推荐这样：
      - **第一轮：多类同时显示，粗过。** 只做三件事：核标签、补漏检、删误检；框不准的，只标记"需调框"，不精调。
      - **第二轮：单类或单框显示，精调框。** 比如先只显示"讨论"，把讨论的框调完；再只显示"写作"，把写作的框调完。这样框不叠，视觉干净，手也稳。
    - 所以：**多类显示省轮次，单类/单框显示省眼力。** 两个阶段分开，不要混在一起。
  - **三、调框速度，是不是关键？**
    - 是，但不是全部。总时间大概是：**要调的框数量 × 单个调框时间**。你能省的是两块：
      - **1. 减少要调的框数量**：删误检、IoU去重、置信度过滤（如果可用）、规则明确减少犹豫、预标注准的直接过。
      - **2. 降低单个调框时间**：先选边再输入数值或按方向键、方向键微调+Shift加速、边缘吸附自动贴合、放大镜看边界、手抖就用键盘不要硬拖。
    - 鼠标拖框连续，但手会抖；键盘微调离散，但稳。**精调阶段，键盘通常比鼠标快且准。**
  - **四、推荐流程**
    1. **多类同时显示**：核标签、补漏删误，标记需调框。
    2. **单类显示**：按类精调框，隐藏其他类。
    3. **单框显示**：只调当前框，其他全隐藏或锁定。
    4. **重叠处理**：先删重复，再调无遮挡，最后调遮挡。
    5. **抽检**：最后统一查漏。
  - 一句话总结：**多类同显用来"看"，单类单框用来"调"。框叠框时，不要硬调，靠隐藏、锁定、置顶、列表跳转来解决。调框速度重要，但减少需要调的框数量更重要。**

## Q18：3 轮为什么走不完整个流程

- **提问**
  - 在我现有的工作流中，检测走的也就是标签走的是6轮，还没有涉及具体矩形框的调整。按照先调标签、再精调矩形框的流程逻辑，3轮应该走不完呀。
- **回答**
  - 你说得对，**3轮走不完**。是我之前没说清楚。
  - 我说的"3轮"，只是**标签核验阶段**的理想压缩值，不是整个流程的总轮次。如果精调框也按6类每类全图走一遍，那实际是：
    - 标签6轮 + 精调框6轮 = **12轮**
    - 就算标签压到3轮，精调框还是6轮，总也要**9轮**
  - 所以3轮不可能是全流程。
  - **为什么精调框会把轮次又拉回去？**
    - 因为标签可以**多类同显**，一张图一次看几个类，所以能压轮次。但精调框不行，框一多就叠，眼睛乱、手也乱。
    - 精调框通常要：单类显示，甚至单框显示；隐藏其他框；只调当前框。这样一搞，轮次又回来了。
    - 所以"多类同显"适合**核标签、补漏删误**，不适合**精调框**。
  - **正确的省时思路：标签走图，框走队列**
    - 不要把全流程硬压成3轮。应该分成两段：
      - **第一段：标签核验，走图。**
        - 补漏删误：1轮，多类同显，快速过。
        - 标签核验：多类同显，好认类一轮、易混对一轮、兜底一轮，理想3轮。
        - 如果工具只能单类筛选，那还是6轮。
      - **第二段：精调框，不走全图，走"待调框队列"。**
        1. 标签核验时，看到框不准，就标记"需调框"，不马上精调。
        2. 5000张过完后，生成一个待调框列表。
        3. 精调时，直接从列表跳转，调完一个，下一个。
        4. 按类分组调，比如先调"讨论"的待调框，再调"写作"的待调框。
    - 这样精调阶段不是"5000张×6类"，而是"要调的框有多少个就调多少个"。比如20万个框里只有3万个需要调，那就只处理这3万个，不用重新扫5000张图。
  - **所以总轮次怎么算？**
    - **标签阶段**：可能3轮，也可能6轮，看工具和策略。
    - **精调阶段**：不按全图轮算，按待调框队列算。
    - **总时间**：标签全图轮次 + 待调框数量 × 单框耗时。
    - 如果工具不支持"标记待调框 + 列表跳转"，那精调框只能按类全图走，确实就是6轮又6轮，总轮次降不下来。
  - 一句话：**3轮是标签核验的理想值，不是全流程。全流程要省，关键是把精调框从"按图轮播"改成"按待调框队列"。**

## Q19：缩略图能否用来调框

- **提问**
  - 我前面说的缩略图功能不就可以用来调框吗？
- **回答**
  - 可以，但**你现在的缩略图，大概率只能"看标签"，不能直接"精调框"**。要能调框，它得升级成另一种东西：**带上下文的可调框缩略图**。
  - **为什么现在的缩略图不适合调框？**
    - 因为它是**按现有框裁剪出来的**。这里有个死结：**框不准 → 缩略图就不准。**
    - 比如框少框了半个头，缩略图里就没有那半个头。你想把框往上拉，但缩略图上面已经没内容了，拉不了。
    - 所以紧贴目标裁剪的缩略图，只适合判断"这是什么动作"，不适合改边界。
  - **要用来调框，缩略图必须满足几个条件**
    1. **留边**：目标周围多裁一圈，比如上下左右各留 20%～50%。这样框拉大、拉小，都有内容可看。
    2. **能放大**：批量看100个时，每个都很小，没法精调。必须点开单个，放大成局部工作台。
    3. **能拖边、拖角**：最好先选边，再拖或按方向键，程序自动知道改哪条边。
    4. **坐标同步**：在缩略图里改，原图里的框也跟着改。
    5. **显示同图其他框**：至少显示附近其他框的轮廓，不然遮挡、重叠判断不了。
    6. **按图分组**：同一张图的缩略图排在一起，避免重复框、漏检。
    - 如果这些都满足，那**缩略图不仅可以调框，而且比按图轮播更省时间**。因为你不用重新扫整张图找目标，直接一个对象一个对象过。
  - **流程会变成这样**
    1. **预标注**。
    2. **生成带留边的缩略图列表**，按类分组，同图相邻。
    3. **第一遍批量看**：核标签、删误检、补漏检、标记"需调框"。
    4. **第二遍点开单个缩略图**：放大，拖边/方向键微调，调完自动下一个。
    5. **最后抽检**。
    - 这样精调框就不再是"5000张×6类"，而是"待调框队列"。要调多少个，就处理多少个。
  - **但要注意两个坑**
    - **上下文仍然会丢**：缩略图再留边，也不如原图全。遮挡、多人关系、漏检，还是得回原图或同图分组看。
    - **批量调框不现实**：100个缩略图一起显示，只能看，不能精调。精调必须点开单个放大。
  - 一句话结论：**你的缩略图如果只是紧贴框裁剪，那只能看标签，不能调框。如果改成带留边、可放大、可拖边、坐标同步、同图分组的缩略图，它就能调框，而且能把精调框从"按图轮播"变成"按对象队列"，总轮次就降下来了。**

## Q20：标注流程总结

- **提问**
  - 请将我们前面关于标注流程的讨论总结一下。总结内容需要覆盖：标注结构的选择、标签修改的方式、标注框和检测框的修改方式。
- **回答**
  - **一、总原则**
    - 数据标注本质是：**人按统一规则，把自己的视觉判断翻译成机器能读的数据。** 预标注之后，人工主要做四件事：**改标签、改框、补漏检、删误检。**
    - 单对象速度很难无限提高，省总时间主要靠结构优化：**减少重复看图、减少无效对象、减少判断犹豫、减少手来回切换。**
  - **二、标注结构的选择**
    1. **不要默认"6类标签走6轮，再6类框走6轮"。** 那样同一张图会被反复看很多遍，总耗时被放大。
    2. **推荐大顺序：脚本预检 → 粗筛补漏删误 → 标签核验 → 精调框 → 抽检。**
    3. **类别排序原则：**
       - 先做好认的类；
       - 好认的里面，量大的优先；
       - 容易混的类成对做，比如听写和举手；
       - 模糊类、兜底类最后做，比如"未标定动作"。
    4. **四项分类可以这样用：**
       - 好认量多：优先，可合并一轮；
       - 好认量少：跟着做；
       - 难认量多：中间做，等标准清晰再啃；
       - 难认量少/兜底：最后集中做。
    5. **多类同显适合"看"，不适合"精调框"。**
       - 核标签、补漏删误：可以多类同时显示。
       - 精调框：最好单类显示，甚至单框显示，隐藏其他框。
    6. **精调框不要按全图轮播，要走"待调框队列"。** 粗筛时只标记"需调框"，不马上精调。之后从待调框列表里逐个跳转、逐个精调。
    7. **缩略图要分场景：**
       - 紧贴框裁剪的缩略图：只能看标签，不能精调框。
       - 带留边、可放大、可拖边、坐标同步、同图分组的缩略图：才能辅助调框。
       - 缩略图最好支持跳回原图、高亮上下文，减少重复框和漏检。
    8. **置信度区分度低时不用。** 如果大部分置信度都是0.4几，排序没意义。如果区分度高，可以高置信度快过，低置信度重点改。
  - **三、标签修改的方式**
    1. **数字键改标签：** 选中对象后，用数字键1~9直接改标签，比鼠标点列表快。
    2. **标签前置：** 先按数字键拿标签，再画框，框画完自动填入标签。减少鼠标在画布和标签列表之间来回跑。
    3. **锁定当前类别：** 单类轮标时，直接把当前类锁死，画框自动填标签，连数字键都不用按。流程变成：**看目标 → 画框 → 微调 → 下一个。**
    4. **缩略图批量核标签：** 按类分组，同图相邻显示，一次检查上百个目标。遇到不确定的，点缩略图跳回原图，高亮该对象，追回上下文。
    5. **易混类成对做：** 比如 listening 和 raise hand 放同一轮，成对判断，比分开两轮更准。
    6. **兜底类最后做：** "未标定动作"这类规则模糊的，放最后集中处理，避免拖慢整体节奏。
    7. **脚本辅助：** 用 IoU 去重删重复框，统计类别分布，检查图与 JSON 是否对应。
  - **四、标注框和检测框的修改方式**
    1. **手动拖拽：** 选中框 → 选边或角 → 拖到边界。优点直观，缺点是手会抖，边界是离散的，很难一次拖准，常要两三轮精调。速度可能只有每分钟4~8个对象。
    2. **数值 + 边指令：** 选中框 → 输入数值 → 告诉程序改哪条边。虽然多一步"边指令"，但比手抖拖拽更准，实际可能更快。
    3. **理想交互：** 先选边或角 → 再输入数值或按方向键微调，程序自动知道改哪条边。配合方向键微调、Shift加速、边缘吸附、放大镜看边界。
    4. **减少要调的框数量：** 删误检；IoU 去重；置信度过滤；规则提前定死，减少犹豫；预标注准的直接过。
    5. **精调策略：**
       - 按待调框队列走，不按全图轮播；
       - 按类分组，同图相邻；
       - 单框放大，隐藏其他框；
       - 先调无遮挡，再调遮挡；
       - 先选边，再方向键或数值微调。
    6. **补漏删误：** 最好前置，快速过一遍，只判断"多了还是少了"，不精修。漏检误检会污染后面的标签和框判断。
  - **五、针对5000图、6类、20万对象的推荐流程**
    1. **脚本预检：** 图与 JSON 对应、类别统计、IoU 去重、越界空框、规则确认。
    2. **粗筛1轮：** 多类同显，补漏删误，标签明显错的顺手改，框不准的标记"需调框"。
    3. **标签核验1~3轮：** 好认类一轮，易混对一轮，兜底类一轮。用缩略图批量核，数字键改标签。
    4. **精调框：** 走待调框队列，单框放大，先选边再微调，不走全图6轮。
    5. **抽检1轮：** 脚本+人工抽查漏检、误检、重复框、标签错、框偏移。
  - **六、一句话结论**
    - **标签靠多类同显 + 缩略图批量核；调框靠待调队列 + 单框放大 + 先选边再微调。**
    - 结构上：**先清大错，好认优先，易混成对，兜底最后，精调走队列。**
