# X-AnyLabeling 个人扩展版（beta.4 fork）— 个人扩展功能提炼

> 本表基于 `CVHub520/X-AnyLabeling`（4.0.0-beta.4，基线提交 `b99efef6`）提炼本 fork（`ljh725/X-AnyLabeling`，当前 154 次提交、`feature/selection-optimization` 分支）相对上游的**个人扩展**，全部剔除上游原版功能。
>
> - **来源**：`anylabeling/views/labeling/label_widget.py`、`widgets/canvas.py`、`widgets/inspector/`、`widgets/pose_label/` 等。
> - **验证方式**：每条均经 `git log --diff-filter=A` / `git log -S` 比对，确认是基线之后 fork 新增（上游基线 `b99efef6` 已有的 Overview / Navigator / Brightness / Compare View 等不收录）。
> - **状态**：`✅新增` = fork 从无到有全新实现；`🔧增强` = 在上游基础上改写/扩展行为；`⛔退役` = 曾实现但已删除（仅保留文档）。
> - **模块#**：对应 `docs/portfolio/` 编号；`—` 表示未单列文档。
> - **菜单**：仅 UI 行为有菜单/快捷键；纯内部算法 / 配置驱动项无菜单入口，列在"无菜单"区。
> - 快捷键列若为空，表示未绑定或读取配置键（`shortcuts[...]`）。
>
> 为保持可对照，全文主表统一六列：**标识符 · 显示文本 · 快捷键 · 模块# · 状态 · 说明**。对"无菜单"的功能，标识符填方法/规则/配置键名，显示文本填功能名，快捷键无则填 `—`。

---

## 一、视图菜单（&View）— 个人扩展最密集

上游 View 菜单扩展为本 fork 改动密度最高的菜单。共 16 个个人扩展菜单项（13 新增 + 3 增强）。

### 个人质检 / 数据检查入口

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `toggle_inspector` | Data Inspector | — | #01/#02 | ✅新增 | 数据检查面板（5-Tab 工作台），checkable，图标 eye |
| `show_rectangle_size_violations` | Show Rectangle Size Violations | — | — | ✅新增 | 显示矩形尺寸规则告警，checkable |
| `configure_rectangle_size_rules` | Configure Rectangle Size Rules | — | — | ✅新增 | 编辑类别级矩形宽高阈值规则 |

### 标签解耦 / 聚焦渲染

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `pose_view` | Pose View | — | #05 | ✅新增 | 切换姿态关键点渲染（彩色框/骨架/防遮挡布局），checkable，配置持久化 |
| `toggle_rect_refine_mode` | 三框聚焦模式 | — | — | ✅新增 | 选中 person/head/face，仅显示按相对位置筛选的关联矩形框，checkable |
| `toggle_rect_edge_align` | 矩形单边调整 | `shortcuts["toggle_rect_edge_align"]` | #04 | ✅新增 | 启用已选中矩形单条边独立拖动，checkable，启动默认关闭 |

### 筛选系统 / 数据集索引

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `toggle_global_filter_keep` | Enable Global Filter | — | #11 | ✅新增 | 过滤值跨图片保持，checkable，默认勾选 |
| `toggle_filter_navigation` | Filter Result Navigation | — | #11 | ✅新增 | 启用过滤结果跨文件导航，checkable，图标 eye |
| `refresh_filter_navigation` | Refresh Filter Result Navigation | — | #11 | ✅新增 | 刷新当前过滤匹配的文件列表 |
| `refresh_dataset_index` | Refresh Dataset Index | — | #11/#16 | ✅新增 | 增量刷新数据集过滤索引（SQLite 派生缓存） |
| `rebuild_dataset_index` | Rebuild Dataset Index | — | #11/#16 | ✅新增 | 从零重建数据集索引（staging-DB 原子重建） |
| `cancel_dataset_index` | Cancel Dataset Index Build | — | #11/#16 | ✅新增 | 取消正在运行的索引构建任务 |

> 筛选子菜单（动态 QMenu，非 QAction）：`Filter by Label` / `Filter by Group ID` / `Filter by Shape Type`，挂在标签列表菜单与画布右键菜单。

### 精修 / 视口保持

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `toggle_precision_mode_lock` | 精修模式锁定 | `shortcuts["toggle_precision_mode_lock"]` | #09 | ✅新增 | 锁定鼠标精修降速，无需持续按 Ctrl，checkable |
| `keep_prev_viewport` | Keep Previous Viewport | — | #15 | ✅新增 | 切图保持上一张的缩放与视图中心，checkable |

### 重置视图子菜单（Reset Image Views）

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `reset_current_image_view` | Reset Current Image View | — | #15 | ✅新增 | 重置当前图片视口 |
| `reset_views_from_current_to_end` | Reset Views from Current to End | — | #15 | ✅新增 | 重置当前及之后所有图片视口 |
| `reset_all_image_views` | Reset All Image Views | — | #15 | ✅新增 | 重置整个数据集所有视口 |

---

## 二、工具菜单（&Tool）— 关键点工具与数字快捷键

共 6 个个人扩展菜单项（均为 ✅新增）。

### 关键点填充工作流

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `enter_keypoint_fill_mode` | Enter Keypoint Fill Mode | `K` | — | ✅新增 | 为选中 person 填充缺失关键点 |
| `toggle_keypoint_tool_window` | Toggle Keypoint Tool Window | `Ctrl+K` | — | ✅新增 | 打开/关闭关键点填充工具窗口 |
| `switch_to_prev_person` | Switch to Previous Person | `Ctrl+Shift+[` | — | ✅新增 | 关键点工具中切换到上一个 person |
| `switch_to_next_person` | Switch to Next Person | `Ctrl+Shift+]` | — | ✅新增 | 关键点工具中切换到下一个 person |

### 数字快捷键管理器

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `digit_shortcut_manager` | Digit Shortcut Manager | `shortcuts["edit_digit_shortcut"]` | #13/#14 | ✅新增 | 数字键绘制模式/标签绑定管理（分页） |
| `digit_relabel_manager` | Digit Relabel Manager | `Alt+R`（默认） | #14 | ✅新增 | 编辑模式下数字键批量改名管理器 |

> 数字快捷键分页切换 `switch_digit_page`（`Switch Digit Shortcut Page`，默认 `F1`）以窗口级 `addAction` 注册，不挂在菜单。

---

## 三、编辑菜单（&Edit）— 数字快捷绘制

共 10 个个人扩展菜单项（`digit_shortcut_0`~`9`，均为 🔧增强）。

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `digit_shortcut_0`~`digit_shortcut_9` | Digit Shortcut 0~9 | `0`~`9` | #13 | 🔧增强 | 在上游 10 键基础上加分页层（10 键 × N 页），运行时联动 `digit_page_manager`，图标 digit0~9 |

> 数字键三模式互斥派发：`rename`（编辑模式选中有 shape）/ `draw`（绘制模式）/ `bind_draw`（绑定源 box 后画同实例）。优先级 `bind_draw > auto_person_instance > auto_use_last_gid`。

---

## 四、画布交互（canvas.py 内部，无菜单项）

canvas.py 在上游基础上经 44 次 fork 提交，承载核心交互增强。以下为方法/算法级标识，无菜单入口，快捷键列填 `—`，实现位置写入"说明"列。

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `_shape_hit_candidates` | 优先级排序拾取模式 | — | #03 | ✅新增 | 核心算法 `canvas.py:769-859`，4 维优先级元组升序排序 |
| `mouseMoveEvent`（悬停分支） | 悬停高亮取候选 | — | #03 | ✅新增 | `canvas.py:1557` 取排序后首个候选 |
| `mousePressEvent`（点击分支） | 点击选择取候选 | — | #03 | ✅新增 | `canvas.py:2321` 同上 |
| `mouseDoubleClickEvent` | 双击编辑取候选 | — | #03 | ✅新增 | `canvas.py:2210` 同上 |
| `RectEdgePhase` / `RectEdgeInteractionController` | 矩形边编辑状态机 | — | #04 | ✅新增 | hover→press→实时→release→Esc，`rect_edge_interaction.py:13-122` |
| `geometry_with_edge_coord` | 反翻转 clamp | — | #04 | ✅新增 | `min < max` 永不坍塌，`rect_edge_alignment.py:320-370`，`min_size=1.0` |
| `RectEdgeRef` | 矩形边编辑句柄 | — | #04 | ✅新增 | 仅内存编辑句柄，绝不写回 JSON / `other_data`，`rect_edge_alignment.py:99-115` |
| `toggle_draw_mode` ↔ `toggle_rect_edge_align` | 绘制模式双向互斥 | — | #04 | ✅新增 | 进 create 模式边编辑休眠，反之切回 edit，`label_widget.py:4186-4277,7379-7398` |
| `_clamp_scroll_value` / `_zoom_around_canvas_pos` | 缩放中心点修复 | — | #12 | ✅新增 | 修上游竖图 Ctrl+滚轮缩放漂移，坐标锚点算法，`label_widget.py:7074-7143` |
| 鼠标 zoom/fixed 双钳位 | 精修模式（鼠标降速） | — | #09 | 🔧增强（部分退役） | `canvas.py:3626-3694,4959-5026`；Tab 边选择/箭头微调/撤销合并已退役 |
| 虚拟光标隔离（D4 规则） | 精修拖拽零污染 | — | #09 | ✅新增 | 独立累加器缩放拖拽增量，零污染 `prev_point`，`canvas.py` |

### 优先级元组核心算法

```python
# canvas.py:777-783 — priority = (级别, 距离, 面积, -stack_index)，升序(越小越优先)
#   级别 0: 附近顶点(可抓取编辑点)——最高优先      → canvas.py:817
#   级别 1: 附近可编辑边(可双击加点)              → canvas.py:834
#   级别 2: 整体命中(contains_point)——兜底         → canvas.py:855
#   同级别下: 距离更近者优先；仍相同时面积更小者优先(嵌套场景小对象优先)；
#     最后后创建者(栈顶)优先。
# 排序: candidates.sort(key=lambda item: item[0])  → canvas.py:858
```

---

## 五、Inspector 数据检查面板（QDockWidget，5 个 Tab）

上游完全无对应功能。窗口标题 `数据检查 (Inspector)`，通过 `issue_navigate_requested` 信号点击问题即跳转 Canvas shape。

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `IssueListWidget` | 数据检查 | — | #02 | ✅新增 | 扫描/扫描当前/导入外部结果；问题树 `[问题, 文件, 详情]` |
| `QualityReviewWidget` | 质检复核 | — | #01 | ✅新增 | 8 状态工作流（待复核/已查看/已修复/确认错误/误报/可接受/需讨论/复扫后消失），上下条导航、修复类型选择、备注 |
| `EditableTableWidget` | 数据表格 | — | #02 | ✅新增 | 列 `[#, Label, Group ID, Type, Description]`，双击可编辑 shape 属性 |
| `RuleConfigWidget` | 规则配置 | — | #02 | ✅新增 | 项目标签集 + 11 条结构规则的勾选/参数/严重级别 |
| `_build_export_widget` | 导出 | — | #02 | ✅新增 | 按规则拆分 JSON+图片到子目录（复制） |

---

## 六、L1/L2 质检引擎（纯 Python，零 PyQt 依赖）

上游无对应能力。`widgets/inspector/quality/` 12 文件 0 个 PyQt import，CLI 与 UI 共享同一份逻辑。

### L1 结构合法性规则（5 条，全 error）

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `L1-01` / `label_required` | 空标签 | — | #01 | ✅新增 | shape 标签为空 |
| `L1-02` / `shape_type_binding` | 类型不匹配 | — | #01 | ✅新增 | label 应为 X，实际为 Y |
| `L1-03` / `points_required` | 坐标点不足 | — | #01 | ✅新增 | shape_type 需要 ≥ N 个点，实际 M 个 |
| `L1-04` / `group_id_required` | 缺少 group_id | — | #01 | ✅新增 | person/head/face 矩形框未设合法 group_id |
| `L1-05` / `bbox_geom_valid` | bbox 退化 | — | #01 | ✅新增 | 矩形框面积/尺寸非法 |

### L2 几何/视觉关系规则（12 条）

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `L2-01` / `face_matched_head_candidate` | face/head 匹配弱 | — | #01 | ✅新增 | 默认 warning，最高候选分 / 无匹配 head |
| `L2-02` / `face_inside_matched_head` | face 超出 head | — | #01 | ✅新增 | warning，overflow_ratio 越界 |
| `L2-03` / `face_head_area_ratio` | face/head 面积比异常 | — | #01 | ✅新增 | warning，合理区间 0.08~0.70 |
| `L2-04` / `face_head_center_alignment` | face/head 中心偏移 | — | #01 | ✅新增 | warning，中心偏移 norm |
| `L2-05` / `head_matched_person_candidate` | head/person 匹配弱 | — | #01 | ✅新增 | info，候选分 / 无 person |
| `L2-06` / `head_person_spatial` | head/person 空间异常 | — | #01 | ✅新增 | warning，violation / y_rel / x_ov |
| `L2-07` / `head_person_area_ratio` | head/person 面积比异常 | — | #01 | ✅新增 | warning，合理区间 0.015~0.25 |
| `L2-08` / `cross_class_size_order` | 大小顺序异常 | — | #01 | ✅新增 | warning，face < head < person |
| `L2-09` / `keypoint_inside_person` | 关键点出 person | — | #01 | ✅新增 | warning，N 个关键点越界 |
| `L2-10` / `body_part_vertical_band` | 人体垂直分区异常 | — | #01 | ✅新增 | warning，关键点越出期望身体区 |
| `L2-11` / `head_keypoints_near_head_face` | 头部点远离 face/head | — | #01 | ✅新增 | warning，N 个远离候选 |
| `L2-12` / `image_level_class_density` | 单图类密度统计 | — | #01 | ✅新增 | info，z-score 异常 |

### Inspector 结构规则（11 条，规则配置 Tab 用）

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `label_in_allowlist` | 标签名检查 | — | #02 | ✅新增 | error |
| `group_label_uniqueness` | 同组标签唯一性 | — | #02 | ✅新增 | error |
| `person_rect_requires_group_id` | Person 矩形 group_id 检查 | — | #02 | ✅新增 | error |
| `group_id_valid` | group_id 合法性检查 | — | #02 | ✅新增 | error |
| `head_face_group_id_required` | Head/Face group_id 检查 | — | #02 | ✅新增 | error |
| `head_face_group_id_uniqueness` | Head/Face 组内标签唯一性 | — | #02 | ✅新增 | error |
| `label_shape_type_binding` | 标签形状类型绑定 | — | #02 | ✅新增 | error |
| `group_id_keypoint_integrity` | Person 主体绑定 | — | #02 | ✅新增 | warning |
| `required_field_not_empty` | 必填字段非空 | — | #02 | ✅新增 | error |
| `group_id_uniqueness` | Group ID 唯一性（自定义） | — | #02 | ✅新增 | error |
| `attribute_consistency` | 属性一致性 | — | #02 | ✅新增 | warning |

### 跨类匹配协议（face→head→person，QA 推断不写回）

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `match_face_to_heads` | face→head（严格） | — | #01 | ✅新增 | 5 条硬过滤（x/y 重叠、面积比上限、head 扩展包含、溢出比）+ containment/center_alignment/area_ratio/overlap 打分 |
| `match_head_to_persons` | head→person（宽松） | — | #01 | ✅新增 | 4 条硬过滤（x 重叠、y_rel 上限、面积比上限、person 扩展）+ upper_body_position/x_overlap/area_ratio/center_distance/keypoint_support 打分 |

---

## 七、Pose View 标签解耦模块（widgets/pose_label/）

上游无对应能力。filter-driven：用配置标志 `pose_click_to_focus` 驱动 gid 过滤聚焦，不污染原生选中流程；Esc 清除聚焦退出。

### 4 种防遮挡布局算法

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `direct` | 方向引线 | — | #05 | ✅新增 | `layout_direct`，沿身体相对方向放置，无消重叠 |
| `anti` | 防遮挡（默认） | — | #05 | ✅新增 | `layout_anti`，direct + 优先级排序消重叠（先肢体后面部） |
| `column` | 侧栏列表 | — | #05 | ✅新增 | `layout_column`，person 框外左右上分栏垂直堆叠 |
| `category` | 类别分组 | — | #05 | ✅新增 | `layout_category`，按身体部位分组（head 上沿 / 左侧 la,ll / 右侧 ra,rl） |

### 配置键（20+ 持久化项，`pose_view:` 块，模块 #05，✅新增）

`enabled` / `layout_mode` / `color_mode`(按部位/按目标) / `font_size` / `opacity` / `leader_length` / `border_width` / `column_gap` / `pose_click_to_focus` / `show_skeleton` / `show_midline` / `show_bbox` / `occlusion_highlight` / `show_leader` / `font_shadow` / `font_color_mode` / `custom_font_color` / `body_colors` / `person_colors` / `color_preset`（default/pastel/neon/earth）。

### COCO 17 关键点常量与骨架（`pose_constants.py`，#05，✅新增）

`COCO_KEYPOINT_ORDER`、`BODY_PARTS`(head/la/ra/ll/rl)、`SKELETON_EDGES`(16 边)、`COLOR_PRESETS`。

---

## 八、纯内部 / 配置驱动（无菜单入口）

这些功能无 QAction，由配置标志或自动钩子触发。

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `auto_person_instance` | 自动创建 person 实例 | — | #07 | ✅新增 | 画 person 自动分配 `max(gid)+1`，免手动输 ID，`label_widget.py:6584-6614` |
| `DigitBindDrawManager` | 数字快捷绑定绘制 | — | #08 | ✅新增 | 选源 box 后按数字键同实例新建并回填 gid，两阶段 TOCTOU 去重 |
| 局部边缘吸附 | 局部边缘吸附 | `Ctrl+Alt+E`（曾） | #10 | ⛔退役 | Sobel 梯度像素吸附，2026-07-20 全删，仅留决策文档 |
| `ViewportController` | 视口自动存/取 | — | #15 | ✅新增 | 图片坐标持久化，切图保持缩放+中心 |
| 筛选状态持久化 | 筛选状态跨图保持 | — | #11 | ✅新增 | `load_file` 快照/恢复，label/gid/shape_type 跨图保持 |
| `auto_activate_keypoint_fill` | 自动激活关键点填充 | — | — | ✅新增 | 进入 person 自动启用填充模式 |
| 导航器状态持久化 | 导航器状态自动读写 | — | — | 🔧增强 | 上游窗口 + fork 持久化（settings 自动读写） |
| 缩放中心修复（navigator 侧） | 导航器缩放修复 | — | #12 | 🔧增强 | 主画布已修，navigator 侧仍用旧模型（Phase 2 待办） |

---

## 九、数据集索引生命周期（widgets 同级，纯后台）

上游无对应能力。SQLite 派生索引，JSON 仍是唯一真相源。

| 标识符 | 显示文本 | 快捷键 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| 6 态状态机 | 6 态状态机 | — | #16 | ✅新增 | missing/cached_unverified/syncing/ready/stale/failed，`dataset_meta` 指纹 |
| staging-DB 原子重建 | staging-DB 原子重建 | — | #16 | ✅新增 | 临时 DB 全量重建→`integrity_check`→`os.replace`，可回滚零停机 |
| 缓存优先短路 | 缓存优先短路 | — | #16 | ✅新增 | 移除上游模态进度框，500 条/批直接读 SQLite |
| 存储解耦 | 存储解耦 | — | #16 | ✅新增 | 索引失败仅标 stale 重试，不影响标注保存 |
| 并发读+单线程写 | 并发读+单线程写 | — | #16 | ✅新增 | 4 worker 解析 JSON（64/批），主线程独占 INSERT |

---

## 十、数据处理脚本集（scripts/，模块 #06）

上游无对应能力。40 个统一 `argparse` CLI 脚本，覆盖姿态数据生产全链路。快捷键列填主要 CLI 参数。

### A. YOLO Pose 三步流水线

| 标识符 | 显示文本 | 快捷键 / CLI 参数 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `step1-convert_json_to_yolopose.py` | LabelMe→YOLOv8 Pose | `--src --dst --config-yaml` | #06 | ✅新增 | 604 行，LabelMe/cls3pose JSON → YOLOv8 Pose labels |
| `step2-split_yolov8pose_dataset.py` | 数据集切分 | `--dataset --ratio --mode` | #06 | ✅新增 | 239 行，按 ratio 切分 train/val/test |
| `step3-visualize_yolo_dataset.py` | YOLO 可视化 | `--dataset --splits --task` | #06 | ✅新增 | 253 行，可视化 YOLO 检测/姿态标注 |

### B. ViTPose 预标注对比

| 标识符 | 显示文本 | 快捷键 / CLI 参数 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `vitpose_rename_pred_jsons.py` | 预测 JSON 重命名对齐 | `--images-dir --json-dir` | #06 | ✅新增 | 291 行，按 stem 对齐 `{stem}_pred.json` |
| `vitpose_labels_rename_pred_jsons.py` | 注入 ViTPose 关键点 | `--base-json-dir --pred-json-dir` | #06 | ✅新增 | 396 行，按 group_id 注入预测关键点 |

### C. 质检 CLI（与 Inspector 集成）

| 标识符 | 显示文本 | 快捷键 / CLI 参数 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `run_l1l2_qc.py` | L1/L2 质检 CLI | `--input --output --thresholds` | #06 | ✅新增 | 172 行，输出 `review.tsv` + `report.json`，只读 |
| `gen_threshold_suggestion.py` | 阈值建议生成 | `--report --feedback --output` | #01/#06 | ✅新增 | 114 行，非约束建议（永远 pending） |
| `validate_pose_labels_codex.py` | 姿态增强校验 | `-i -w -o -f` | #06 | ✅新增 | 777 行，group_id 一致性 + head/face 重复检测 |
| `validate_pose_labels.py` | 姿态 JSON 校验 | `-i -w -o` | #06 | ✅新增 | 626 行，姿态标注校验器 |

### D. 数据集统计与对比

| 标识符 | 显示文本 | 快捷键 / CLI 参数 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `collect_unique_labels.py` | 唯一标签提取 | `input_dir output_file` | #06 | ✅新增 | 91 行，多进程提取唯一标签集 |
| `compare_dataset_stems.py` | 两目录 stem 对比 | `--dir-a --dir-b --output` | #06 | ✅新增 | 581 行，A-only/B-only/common |
| `stats_json_shapes.py` | shape 统计 | `--input --output` | #06 | ✅新增 | 462 行，统计点/矩形/group_id |
| `glm_compare_datasets.py` | 本地/服务器目录对比 | （硬编码配置） | #06 | ✅新增 | 274 行，本地 vs 服务器目录对比 |

### E. 标签重排与批量操作

| 标识符 | 显示文本 | 快捷键 / CLI 参数 | 模块# | 状态 | 说明 |
|---|---|---|:---:|:---:|---|
| `sort_pose_labels.py` | 标签重排 | `input_dir output_dir` | #06 | ✅新增 | 284 行，矩形→有 gid 关键点→无 gid 关键点 |
| `cth_cls3_psoe_rename_labels.py` | COCO 全名→缩写 | （硬编码映射） | #06 | ✅新增 | 123 行，批量改名 |
| `generate_empty_jsons_all.py` | 批量生成空 JSON | `--img-dir --out-dir --start-index` | #06 | ✅新增 | 138 行，支持起始位置 |
| `align_image_json_pairs.py` | 对齐图片/JSON | `--images-dir --json-dir` | #06 | ✅新增 | 341 行，对齐单层文件夹 |
| `sync_and_clear_shapes.py` | 同步并清空 shapes | `-a -b -o` | #06 | ✅新增 | 114 行，同步并清空 |

---

## 十一、统计汇总

### 按区域统计（去重前）

| 区域/菜单 | ✅新增 | 🔧增强 | ⛔退役 | 合计 |
|---|:---:|:---:|:---:|:---:|
| 视图菜单 | 15 | 1 | 0 | 16 |
| 工具菜单 | 6 | 0 | 0 | 6 |
| 编辑菜单（数字快捷键） | 0 | 1（10 键） | 0 | 10 |
| 画布交互（无菜单） | 9 | 1 | 0 | 10 |
| Inspector 面板（5 Tab） | 5 | 0 | 0 | 5 |
| L1/L2 质检规则 | 28 | 0 | 0 | 28 |
| Pose View 布局/配置 | 4+ | 0 | 0 | 4+ |
| 纯内部/配置驱动 | 6 | 2 | 1 | 9 |
| 数据集索引生命周期 | 5 | 0 | 0 | 5 |
| 数据脚本（scripts/） | 18 | 0 | 0 | 18 |
| **合计（去重前）** | **96+** | **5** | **1** | **~111** |

> 注：
> - 本表按各区域逐行提炼统计，去重前合计约 **111 项**（新增 96+ / 增强 5 / 退役 1）。
> - 多数功能同时跨区（如质检规则在引擎/Inspector 两处体现），去重后独立功能约 **16 个核心模块 + 18 个数据脚本**。
> - 与 `docs/PORTFOLIO.md` 的"16 模块 / 300+ 测试 / ~18,500 新增行"口径一致。

### 新增 vs 增强 vs 退役 占比

| 类型 | 数量（去重前） | 占比 | 特征 |
|---|:---:|:---:|---|
| ✅新增 | 96+ | ~94% | 上游完全没有，fork 从无到有 |
| 🔧增强 | 5 | ~5% | 在上游基础上扩展（数字快捷键分页、精修、持久化等） |
| ⛔退役 | 1 | ~1% | 实验性功能验证失败后删除（仅留文档） |

### 个人扩展改动密度

| 区域 | 改动密度 | 说明 |
|---|---:|---|
| L1/L2 质检规则 | 28/28 = 100% | 完全 fork 新建，零 PyQt |
| 视图菜单（个人扩展项） | 16/项 = 全新 | 上游 View 菜单大幅扩展 |
| Pose View 模块 | 100% | 4 布局 + 20 配置键全新建 |
| 画布交互 | 44 次提交 | canvas.py 经 44 次 fork 提交强化 |
| 数据脚本 | 18 项 | 覆盖姿态数据生产全链路 |

---

## 附：模块作品集对照

| 模块# | 文档 | 标题 | 状态 |
|:---:|---|---|:---:|
| #01 | `docs/portfolio/01-quality-engine.md` | L1/L2 数据质检引擎 | ✅新增 |
| #02 | `docs/portfolio/02-inspector-panel.md` | Inspector 数据检查面板 | ✅新增 |
| #03 | `docs/portfolio/03-selection-optimization.md` | 优先级排序拾取模式 | ✅新增 |
| #04 | `docs/portfolio/04-rect-edge-edit.md` | 矩形边编辑 | ✅新增 |
| #05 | `docs/portfolio/05-pose-view.md` | Pose View 标签解耦 | ✅新增 |
| #06 | `docs/portfolio/06-data-toolkit.md` | 数据处理脚本集 | ✅新增 |
| #07 | `docs/portfolio/07-auto-person-instance.md` | 自动创建 person 实例 | ✅新增 |
| #08 | `docs/portfolio/08-digit-bind-draw.md` | 数字快捷绑定绘制 | ✅新增 |
| #09 | `docs/portfolio/09-precision-mode.md` | 精修控制模式 | 🔧部分退役 |
| #10 | `docs/portfolio/10-local-edge-snap.md` | 局部边缘吸附 | ⛔退役 |
| #11 | `docs/portfolio/11-filter-system.md` | 筛选系统（保持+引擎+索引+导航） | ✅新增 |
| #12 | `docs/portfolio/12-zoom-center-fix.md` | 缩放中心点修复 | ✅新增 |
| #13 | `docs/portfolio/13-digit-shortcut-pagination.md` | 数字快捷键分页扩展 | 🔧增强 |
| #14 | `docs/portfolio/14-digit-rename.md` | 数字快捷键改名 | ✅新增 |
| #15 | `docs/portfolio/15-viewport-persistence.md` | 视口状态保持 | ✅新增 |
| #16 | `docs/portfolio/16-dataset-index-lifecycle.md` | 数据集索引生命周期工程化 | ✅新增 |

---

## 附：如何使用本表

1. **找功能**：知道功能名 → 在各表"显示文本"列搜索 → 看"标识符"和"模块#"。
2. **查文档**：知道模块编号 → 进入 `docs/portfolio/NN-*.md` 读痛点/架构图/代码定位/测试覆盖。
3. **定位代码**：知道标识符 → 在 `label_widget.py` 搜索 `标识符 = action(`；画布算法在 `canvas.py` 按行号定位。
4. **改快捷键**：表中 `shortcuts["..."]` 项读取 `xanylabeling_config.yaml` 的 `shortcuts:` 块。
5. **验证来源**：`git log --diff-filter=A --oneline -- <文件>` 或 `git log -S "<标识符>" --oneline -- <文件>` 末尾即引入提交（基线为 `b99efef6`，之后的均为 fork 新增）。
