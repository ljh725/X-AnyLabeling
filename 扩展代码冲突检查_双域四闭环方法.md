# 扩展代码冲突检查——双域四闭环方法

> 文档定位：检查 X-AnyLabeling fork 中两类代码冲突：
>
> 1. 扩展功能彼此之间的内部冲突；
> 2. 扩展功能与项目原生代码之间的集成冲突。
>
> 本文所说的“冲突”不限于 Git merge conflict，而是任何在特定触发条件下导致契约被破坏、状态不一致、行为被覆盖、数据丢失、资源泄漏、功能退化或上游升级困难的代码关系。

---

## 一、先把问题定义正确

### 1.1 两个检查域

设：

- `N`：可信上游基准中的原生代码和原生行为；
- `E`：fork 新增文件，以及 fork 对原生文件所做的扩展 hunk；
- `B`：扩展与原生发生接触的边界，包括导入、继承、调用、信号、共享状态、配置、文件格式、注册表和 UI 入口。

需要检查两个关系集合：

```text
E × E：扩展内部交互
E × N：扩展与原生交互
```

不能用“fork_only 文件就是扩展、modified 文件就是原生”来划界。一个大型原生文件中的若干 hunk 可能属于扩展，而一个 fork_only 适配器可能只是在完整复用原生契约。边界必须落实到符号、hunk 和行为责任。

### 1.2 冲突不等于差异

以下情况只是差异，不应直接报冲突：

- 有意覆盖原生默认行为，且契约、测试和用户预期一致；
- 两个功能修改同一文件，但读写状态互不影响；
- 代码重复但短期没有产生不一致行为；此时可报维护风险，而非运行冲突；
- 上游 API 被包装而非绕过，并保留兼容语义。

一个有效冲突结论必须能回答：

```text
在什么前置条件和操作顺序下，
组件 A 做了什么，组件 B 又假设了什么，
哪条不变量或契约因此被破坏，
最终造成什么可观察影响。
```

### 1.3 三个时间维度

检查不能只看当前静态代码：

| 维度 | 关注点 |
|---|---|
| 当前静态 | 重名、覆盖、循环依赖、契约和注册冲突 |
| 当前运行 | 时序、重入、信号、线程、状态和生命周期冲突 |
| 后续演进 | 上游升级热点、私有 API 耦合、重复修改和迁移风险 |

---

## 二、四个闭环

| 闭环 | 要证明什么 | 收口条件 |
|---|---|---|
| 边界闭环 | 原生、扩展及所有接触边界均已识别 | 未分类文件、hunk、符号和边界为 0 |
| 候选闭环 | 所有共享资源和可能冲突的交互均被机器点名 | 高风险共享点均生成候选并有责任人/组件 |
| 判定闭环 | 每个候选都有证据支持的结论 | 未审候选为 0；未知项有明确证据缺口 |
| 场景闭环 | 高风险结论经过真实操作顺序验证 | P0/P1 均有复现或反证测试，关键组合场景已覆盖 |

四个闭环全部通过，才能说“在记录的基准和场景范围内未发现未决高风险冲突”。静态搜索无命中不能等价为无冲突。

---

## 三、冲突分类体系

### C1：符号、注册与命名冲突

- 相同 action、shortcut、objectName、配置键、registry key 或命令名称重复注册；
- fork 模块遮蔽原生模块、导入顺序改变实际加载对象；
- 同名类/函数语义不同，调用方误用；
- 资源路径、翻译 context、模型名称或缓存 key 碰撞。

### C2：API、继承与覆盖契约冲突

- override 参数、返回值、异常或副作用与基类不一致；
- 应调用 `super()` 的生命周期函数遗漏或重复调用；
- 扩展直接依赖原生私有属性/方法，绕过公开入口；
- monkey patch、条件导入或替换实现使其他原生调用方看到不同契约；
- 扩展改变共享对象的可变性、所有权或身份语义。

### C3：共享状态与所有权冲突

- 两个功能同时写 selection、mode、dirty、current file、zoom、scroll、shape list 等状态；
- 多份缓存/索引各自成为“真相源”，刷新顺序导致数据不一致；
- 临时状态未在文件切换、取消、关闭、异常路径中复位；
- A 认为对象仍有效，B 已替换、删除或重建；
- 状态更新不是原子操作，中途信号观察到半完成状态。

### C4：事件、UI 与交互优先级冲突

- 快捷键、菜单、工具栏、右键菜单或鼠标手势重复；
- Qt shortcut context 不同导致全局/局部入口互相抢占；
- `eventFilter`、mouse/key handler、accept/ignore 顺序覆盖原生行为；
- 选择、绘制、编辑、平移、框选、复核等模式同时为真；
- 同一 signal 重复连接、错误断开、递归触发或连接顺序影响结果；
- `blockSignals`、防重入标志或编辑保护在异常路径未恢复；
- 动态 Tab、Dock、菜单索引假设被另一扩展打破。

### C5：配置、持久化与数据契约冲突

- YAML 默认值、schema、运行时应用器和 UI 控件含义不同步；
- 配置键改名但缺少迁移，旧配置覆盖新默认；
- 多个模块读写同一键但类型、范围或单位不同；
- JSON 读写丢弃未知字段、改变 `group_id`、flags、attributes 或 points；
- 保存、自动保存、切图、撤销/重做和外部脚本对数据真相源理解不同；
- 质检只读约束被 UI 或修复脚本意外突破；
- round-trip 后原生文件无法再次正常读取。

### C6：并发、异步与生命周期冲突

- `QThread`/worker 在错误线程访问 QWidget 或 QObject；
- worker 结束、窗口关闭、文件切换后仍回调旧对象；
- 多次启动复扫、索引、推理或加载任务造成竞态；
- timer、future、signal connection 没有取消或释放；
- QObject parent、`deleteLater()`、thread quit/wait 顺序错误；
- 后台结果覆盖用户刚完成的新状态。

### C7：依赖、资源、构建和环境冲突

- `onnxruntime` 与 `onnxruntime-gpu` 等互斥依赖同时出现；
- 扩展改变版本、环境变量或导入顺序，破坏原生启动；
- 手工修改自动生成的 `resources.py`，下次编译被覆盖；
- 新资源、翻译、模型 YAML、入口点没有加入构建或发布清单；
- CPU/GPU、Windows/macOS/Linux 或 PyQt 版本分支行为不一致。

### C8：性能与资源争用冲突

- 多个扩展各自全量扫描数据集、重复建立索引或缓存；
- UI 线程执行大文件 I/O、模型推理或全量校验；
- 高频 paint/mouse 事件中引入 O(n²) 操作；
- 缓存缺少失效策略，内存长期增长；
- 一个功能的节流、批处理或延迟刷新让另一个功能读取陈旧状态。

### C9：上游演进与合并热点冲突

- 扩展长期集中修改原生超大文件，未来升级难以自动合并；
- 复制原生实现后独立演进，安全修复和 bug fix 无法继承；
- 依赖未承诺稳定的私有 API、布局索引或隐式调用顺序；
- 删除原生文件/入口，但其他原生模块仍保留调用；
- fork 与新版上游分别修改同一契约，文本可合并但语义不兼容。

---

## 四、第一阶段：建立原生—扩展边界

### 4.1 固定审计基准

记录：

- `UPSTREAM_SOURCE`：可信上游仓库/tag/目录快照；
- `UPSTREAM_BASE`：用于定义原生代码的版本；
- `TARGET`：当前 fork HEAD；
- `WORKTREE_SNAPSHOT`：当前 staged、unstaged、untracked 状态；
- 扫描时间、脚本摘要、排除规则和所有读取错误。

审计过程中工作区发生变化时，必须重建快照或把结果标记为过期。

### 4.2 形成扩展清单

扩展清单至少包含：

1. fork-only 的应用源码、脚本、配置、测试和资源；
2. modified 原生文件中属于 fork 的 hunk 和符号；
3. 删除或替换的原生入口；
4. 工作区中尚未提交的扩展；
5. 生成物和纯文档项，单独分类而不是混入运行代码。

每个扩展组件分配稳定 `component_id`，每项能力分配稳定 `feature_id`。

### 4.3 形成边界图

为每个扩展组件枚举它与原生代码的所有边：

```text
imports / inherits / overrides / calls / called_by
connects_signal / emits_signal / installs_event_filter
reads_state / writes_state / owns_object
reads_config / writes_config / migrates_config
reads_data / writes_data / registers_key
starts_worker / receives_async_result
```

输出 `boundary_map.json`。任何扩展组件如果没有边界却声称集成到应用，应检查它是否为死代码或缺少注册。

---

## 五、第二阶段：共享资源索引与候选生成

### 5.1 不做盲目的全量两两组合

若有大量扩展，直接检查所有 `E × E` 组合会产生噪声。先建立共享资源倒排索引：

| 资源类型 | 示例 |
|---|---|
| 状态 | current file、selected shapes、canvas mode、dirty、zoom、review item |
| 事件 | key/mouse/wheel、shape changed、file loaded、close、scan finished |
| UI 标识 | action、shortcut、objectName、menu、dock、tab index |
| 配置 | YAML/schema/runtime key、用户配置迁移 |
| 数据 | LabelMe JSON 字段、shape identity、group_id、缓存、索引 |
| 生命周期 | QWidget/QObject/QThread/timer/worker 所有权 |
| 注册项 | model、rule、converter、exporter、CLI、translation context |
| 文件/资源 | 同一输出文件、临时目录、模型缓存、报告文件 |

只有两个组件共享同一资源、调用边界或时序前提时，才生成高价值候选。

### 5.2 读写关系决定优先级

候选优先级从高到低：

1. `write ↔ write`：两个组件写同一状态或输出；
2. `write → assumed/read`：A 改变 B 依赖的不变量；
3. `delete/replace → use`：A 删除或替换 B 仍使用的对象；
4. `event ↔ event`：争抢同一输入或依赖处理顺序；
5. `lifecycle ↔ callback`：异步回调晚于对象/上下文寿命；
6. `read ↔ read`：通常不冲突，只检查性能和陈旧缓存。

输出 `shared_resource_index.json` 和 `conflict_candidates.jsonl`。每个候选必须说明生成原因，避免成为无法解释的搜索结果堆积。

---

## 六、第三阶段：静态契约审计

### 6.1 原生契约先于扩展实现

对每个 `E × N` 候选，先从上游代码、测试和调用方提取原生契约：

- 输入类型、允许的空值和边界值；
- 返回值、异常、信号和副作用；
- 状态修改顺序和必须保持的不变量；
- 对象所有权和生命周期；
- 是否允许重入、异步或跨线程；
- 数据格式及向后兼容要求。

然后检查扩展是：

- `preserve`：保持契约；
- `extend`：兼容扩展；
- `intentional_override`：有意改变且调用方已同步；
- `violate`：破坏契约；
- `unknown`：证据不足。

### 6.2 PyQt 专项静态检查

- override 是否保持签名并正确调用 `super()`；
- QObject 是否在正确线程创建和使用；
- signal 是否重复连接，disconnect 是否误伤其他订阅者；
- lambda/closure 是否捕获过期索引或对象；
- event handler 是否正确 accept/ignore 和调用父类；
- QAction shortcut context、enabled、checked 状态是否冲突；
- model reset/layout change 是否会杀掉正在编辑的 editor；
- QTimer/QThread/worker 是否在切图和关闭时停止；
- Dock/Tab 插入位置是否依赖脆弱的固定索引。

### 6.3 数据与配置专项静态检查

- config 默认值、schema、runtime applier、UI、迁移是否形成一条完整链；
- shape/label 文件读写是否保留未知字段并保持 round-trip；
- 多个模块是否复制、缓存或重建同一 shape identity；
- 只读质检模块是否越界写回正式 JSON；
- 文件切换、撤销/重做、自动保存是否共享同一 dirty 语义；
- 默认行为变化是否会使旧用户配置失效。

---

## 七、第四阶段：运行时场景与交互矩阵

### 7.1 从真实操作序列验证

语义冲突通常只在顺序组合中出现。场景至少覆盖：

- 启动 → 加载文件/目录 → 切图 → 关闭；
- 创建/编辑/删除 shape → 撤销/重做 → 保存 → 重新加载；
- 单选/多选/框选 → 绘制/平移/缩放 → 复核或跨图操作；
- 筛选/Inspector/质检复核 → 导航 → 编辑 → 复扫；
- 设置修改 → 即时应用 → 重置 → 重启后读取；
- worker 运行中切图、重复启动、取消或关闭窗口；
- 旧格式 JSON、包含未知字段 JSON 和异常数据的读写往返。

不能穷举所有功能排列时，按共享资源索引选择组合：

1. 所有 P0/P1 候选；
2. 所有 `write↔write` 和 `lifecycle↔callback`；
3. 每个核心状态至少一个双功能组合；
4. 同时影响三个以上组件的共享状态增加三功能场景。

### 7.2 验证方式

优先顺序：

1. 已有窄范围单测/集成测试；
2. 临时只读探针或 offscreen Qt 场景；
3. 最小人工复现步骤；
4. 静态推演，仅在无法运行时使用并降低置信度。

测试通过不能自动反证所有冲突；必须确认测试确实触发候选中的操作顺序和状态交叉。

### 7.3 必须监测的不变量

- 当前文件、UI 展示和数据模型指向同一对象；
- selection 中不存在已删除或属于旧文件的 shape；
- canvas mode 在互斥状态中至多一个有效；
- dirty 只在真实数据变化时变化，保存后正确清除；
- undo/redo 后 UI、数据和持久化一致；
- 配置 UI、内存值和落盘值一致；
- worker 结果只能应用到它启动时对应且仍有效的上下文；
- JSON round-trip 不丢失原生和扩展均应保留的字段；
- 关闭、切图或异常后没有悬挂线程、timer、连接和临时状态。

---

## 八、第五阶段：上游升级冲突检查

当前运行正常不代表能够安全继承上游更新。需要额外生成升级风险视图：

1. 统计扩展修改最密集的原生文件和符号；
2. 找出复制粘贴后独立演进的上游实现；
3. 找出对 `_private` API、固定行序、固定 tab index、隐式信号顺序的依赖；
4. 使用只读的 diff/merge-tree 或目录三方比较评估新版上游候选；
5. 区分文本冲突与“可自动合并但语义冲突”；
6. 为高耦合点提出隔离边界，例如 adapter、service、registry 或明确公开接口。

不得在脏工作树上实际执行 merge/rebase 来做检查。

本项目优先检查的升级热点包括：

- `label_widget.py` 与 `canvas.py` 的扩展 hunk；
- 设置 schema/default/runtime applier；
- label/shape 序列化与 identity；
- Inspector、复核队列、筛选和数据索引之间的导航与刷新；
- 自动标注模型 registry、设备选择和依赖；
- 翻译资源生成链与打包配置。

---

## 九、冲突判定与严重度

### 9.1 结论状态

每个候选必须落入：

| 状态 | 含义 |
|---|---|
| `confirmed_conflict` | 已有明确违反契约或可复现错误 |
| `latent_conflict` | 当前默认路径未触发，但合理操作/配置可触发 |
| `upgrade_conflict` | 当前可用，但上游演进或重新生成会破坏 |
| `maintenance_risk` | 暂无行为错误，但重复真相源或高耦合易产生分叉 |
| `intentional_override` | 有意改变原生行为，边界、调用方和测试一致 |
| `compatible` | 已检查且有证据表明兼容 |
| `unknown` | 证据不足，不能下结论 |

### 9.2 严重度

- **P0**：数据损坏/丢失、安全问题、稳定崩溃、无法启动；
- **P1**：核心标注流程错误、跨文件污染、线程/生命周期高风险；
- **P2**：局部功能失效、状态偶发不一致、明显升级阻塞；
- **P3**：维护风险、轻微 UI 冲突、低概率兼容问题。

严重度看影响，置信度看证据，两者必须分开记录。

### 9.3 单条发现的证据格式

```text
conflict_id
domain: extension_internal | extension_native
type: C1..C9
severity / confidence / status
components / features / files / exact lines
preconditions
trigger_sequence
conflict_path: A action → shared resource → B assumption → violated invariant
observable_impact
static_evidence
runtime_evidence
native_contract_evidence
false_positive_checks
recommended_direction
required_regression_test
```

没有触发条件、违反的不变量和可观察影响时，只能记录为调查线索，不能作为确定缺陷。

---

## 十、执行 SOP

### 步骤 0：保护现场

- 读取 `AGENTS.md` 和已有方法/清单；
- 固定 Git、上游和工作区快照；
- 不 reset、checkout、clean、merge、rebase、删除或修改应用代码；
- 大文件先 `rg -n` 定位，再读取最小窗口。

### 步骤 1：建立边界

- 生成原生、扩展、modified hunk 和工作区扩展清单；
- 给 component/feature 分配稳定 ID；
- 生成 `boundary_map.json`；
- 确认未分类项为 0。

### 步骤 2：建立共享资源索引

- 索引状态、事件、UI 标识、配置、数据、生命周期、注册项和文件资源；
- 生成 `E×E` 和 `E×N` 候选；
- 按读写关系与影响排序。

### 步骤 3：静态契约检查

- 提取原生契约；
- 检查 override、super、私有 API、信号、事件、配置和序列化；
- 对每个候选形成初步 verdict；
- 记录无法读取或无法解释的证据缺口。

### 步骤 4：运行时验证

- 先跑最窄相关测试；
- 为 P0/P1 和共享写状态建立最小场景；
- 验证状态不变量、对象寿命、数据 round-trip 和异常路径；
- 不为“让结果通过”而修改生产代码。

### 步骤 5：上游演进检查

- 统计原生热点与私有 API 耦合；
- 检查复制实现、生成物、构建与依赖；
- 用只读三方方法识别文本和语义升级冲突。

### 步骤 6：判定与报告

- 每个候选落入七种结论之一；
- P0/P1 必须有复现或高强度静态证据；
- compatible 项也保留反证依据，防止以后重复调查；
- 报告先列 confirmed/latent，再列 upgrade/maintenance，最后列 unknown；
- 修复建议只描述方向，不在代码检查任务中擅自实施。

---

## 十一、审计固定产物

建议写入 `.tmp-conflict-audit/audit-<时间>/`：

```text
audit_meta.json
native_inventory.json
extension_inventory.json
extension_hunk_ledger.jsonl
boundary_map.json
native_contracts.json
shared_resource_index.json
conflict_candidates.jsonl
conflict_findings.jsonl
scenario_matrix.json
test_evidence.json
upgrade_hotspots.json
unknown_items.json
conflict_audit_report.md
```

长期需要人工阅读的正式结果可另写根目录
`扩展代码冲突检查报告.md`，临时脚本和机器台账保留在审计目录。

---

## 十二、最终收口清单

- [ ] 原生、扩展、modified hunk 和工作区边界已固定
- [ ] 每个扩展组件均有 component_id、feature_id 和边界关系
- [ ] 未分类文件、hunk、符号和无法读取项均为 0
- [ ] 状态、事件、UI、配置、数据、生命周期、注册和文件资源已建索引
- [ ] 所有共享写、替换后使用、事件争抢和异步寿命候选已生成
- [ ] 每个候选都有明确生成原因和结论
- [ ] 原生契约来自上游实现、测试或真实调用方，而非主观猜测
- [ ] override/super、信号槽、event handler、shortcut context 已检查
- [ ] 配置全链与 JSON round-trip 已检查
- [ ] P0/P1 均有复现步骤、测试证据或高强度静态证明
- [ ] 核心状态至少覆盖一个双功能交互场景
- [ ] 涉及三个以上组件的共享状态已覆盖组合场景
- [ ] 关闭、切图、取消、异常和后台任务晚到路径已检查
- [ ] 上游热点、私有 API、复制实现和生成物升级风险已检查
- [ ] 每项 finding 都包含触发链、违反的不变量和可观察影响
- [ ] compatible/intentional_override 也保留反证依据
- [ ] unknown 项有证据缺口、影响范围和下一步
- [ ] 审计结束时工作区快照未发生未记录变化

任一硬性项目未通过时，报告必须写明检查边界，不得使用“代码不存在冲突”的绝对结论。

