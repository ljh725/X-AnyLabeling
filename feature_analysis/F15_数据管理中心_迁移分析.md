# 功能15：数据管理中心 — 迁移分析报告

- **难度**: ⭐⭐ | **影响级别**: 🟢 直接迁移
- **源文件**: 6个文件（`widgets/data_manager/` 目录，共~64KB）
- **嵌入代码**: label_widget.py (~20行菜单+快捷键)
- **快捷键**: Ctrl+Shift+D

---

## 1. 依赖与导入调整

### 独立模块文件
| 文件 | 大小 | 操作 |
|------|------|------|
| `widgets/data_manager/__init__.py` | 320B | 直接复制 |
| `widgets/data_manager/data_manager_dialog.py` | 16,563B | 直接复制 |
| `widgets/data_manager/data_scanner.py` | 7,393B | 直接复制 |
| `widgets/data_manager/file_view.py` | 7,398B | 直接复制 |
| `widgets/data_manager/label_view.py` | 17,027B | 直接复制 |
| `widgets/data_manager/object_view.py` | 15,695B | 直接复制 |

### 导入变更
- 新增导入（`widgets/__init__.py`）:
  ```python
  from .data_manager import DataManagerDialog
  ```

### 外部依赖
- **无Canvas依赖**: 完全独立的数据扫描+展示模块
- **内部依赖**: 通过 `label_widget` 的属性获取数据（`image_list`, `label_list`, `output_dir`等）

---

## 2. 集成点分析

### label_widget.py (~20行)
- **菜单**: 在 Tools 或 Edit 菜单中新增 "Data Manager" 选项
- **Action定义**:
  ```python
  self.data_manager_action = action(
      self.tr("Data Manager"),
      self.open_data_manager,
      shortcut="Ctrl+Shift+D",
  )
  ```
- **新增方法**: `open_data_manager()` (~10行)
  ```python
  def open_data_manager(self):
      dialog = DataManagerDialog(self)
      dialog.exec_()
  ```

---

## 3. Canvas API 兼容性

| 旧API调用 | 3.3.8适配方式 | 状态 |
|-----------|-------------|------|
| 无直接Canvas调用 | — | ✅ |

**无Canvas依赖。** DataManagerDialog通过parent引用访问label_widget的数据属性（文件列表、标签列表等），这些属性在3.3.8中未变化。

---

## 4. 独立文件拆解建议

| 文件 | 状态 | 操作 |
|------|------|------|
| `widgets/data_manager/` 整个目录 | ✅ 已独立 | 直接复制整个目录 |

**无需重构**。最干净的迁移目标——完整的独立子系统，零Canvas依赖。

### 迁移工作量估算
- 复制文件：6个（整个目录）
- 修改文件：1个（label_widget.py）
- 新增代码：~20行
- 预计耗时：0.5小时
