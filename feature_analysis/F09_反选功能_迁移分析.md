# 功能9：反选功能 (Invert Selection) — 迁移分析报告

- **难度**: ⭐ | **影响级别**: 🟢 直接迁移
- **源文件**: 无独立文件，嵌入 `label_widget.py` (~30行)
- **附带**: `toggle_select_all()` 过滤器感知改造 (~6行修改)

---

## 1. 依赖与导入调整

### 独立模块文件
无。代码量少（~30行），直接嵌入 label_widget.py。

### 导入变更
- 无新增导入（QPushButton已在label_widget.py现有导入中）

### 外部依赖
- 无

---

## 2. 集成点分析

### label_widget.py — 反选功能
- **`__init__`**: 创建按钮
  ```python
  self.invert_select_button = QPushButton("Invert")
  self.invert_select_button.clicked.connect(self.invert_selection)
  ```
- **布局**: `filter_layout.addWidget(self.invert_select_button, 0)`
- **新增方法** `invert_selection()` (~26行):
  - 遍历所有shapes
  - 判断过滤可见性（配合FilterController）
  - 翻转选中状态
  - 调用 `self.canvas.select_shapes(inverted_list)`

### label_widget.py — toggle_select_all() 过滤器感知改造
- **修改方法** `toggle_select_all()` (~6行修改):
  - 原始：`self.canvas.select_shapes(self.canvas.shapes)` — 选择所有shapes
  - 修改后：先收集可见shapes（检查 `item.checkState() == Qt.Checked`），只选择过滤后可见的shapes
- **建议**: 与反选功能一起迁移

---

## 3. Canvas API 兼容性

| 旧API调用 | 3.3.8适配方式 | 状态 |
|-----------|-------------|------|
| `self.canvas.shapes` | adapter Property proxy → `ShapeManager._shapes` | ✅ 兼容 |
| `self.canvas.selected_shapes` | adapter Property proxy → `ShapeManager._selected_shapes` | ✅ 兼容 |
| `self.canvas.select_shapes(list)` | adapter方法 → `ShapeManager.select_shapes()` | ✅ 兼容 |

**纯label_widget层操作，完全兼容。**

---

## 4. 独立文件拆解建议

| 文件 | 状态 | 操作 |
|------|------|------|
| 无需独立文件 | — | 直接嵌入label_widget.py |

**不建议独立**: 仅~30行，逻辑简单。若功能7(FilterController)已迁移，可考虑将反选逻辑纳入FilterController。

### 迁移工作量估算
- 复制文件：0个
- 修改文件：1个（label_widget.py）
- 新增代码：~30行（反选）+ ~6行（全选改造）
- 预计耗时：0.5小时
