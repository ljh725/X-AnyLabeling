# 功能17：分组标签列表视图 — 迁移分析报告

- **难度**: ⭐⭐ | **影响级别**: 🟢 直接迁移
- **源文件**: `widgets/grouped_label_list_widget.py` (31,507B)
- **嵌入代码**: label_widget.py (~30行 `toggle_grouped_view`)

---

## 1. 依赖与导入调整

### 独立模块文件
| 文件 | 大小 | 操作 |
|------|------|------|
| `widgets/grouped_label_list_widget.py` | 31,507B | 直接复制到3.3.8 `widgets/` |

### 导入变更
- 新增导入（`widgets/__init__.py`）:
  ```python
  from .grouped_label_list_widget import GroupedLabelListWidget
  ```

### 外部依赖
- **纯UI组件**: 继承自 QWidget
- **内部依赖**: `label_list_widget.py` 中的数据模型（LabelListWidgetItem）

---

## 2. 集成点分析

### label_widget.py
- **`__init__`**: 创建 `GroupedLabelListWidget` 并添加到布局
  ```python
  self._grouped_label_list = GroupedLabelListWidget(self)
  # 添加到 dock 或 stacked widget 中
  ```
- **新增方法**: `toggle_grouped_view()` (~30行)
  - 切换标准列表视图与分组视图
  - 同步选中状态
- **菜单**: View菜单新增 "Grouped Label View" 切换选项
- **数据同步**: shapes变化时需同步更新分组视图

---

## 3. Canvas API 兼容性

| 旧API调用 | 3.3.8适配方式 | 状态 |
|-----------|-------------|------|
| 无直接Canvas调用 | — | ✅ |

**纯UI widget，无Canvas依赖。** 通过label_widget的label_list数据进行展示和交互。

---

## 4. 独立文件拆解建议

| 文件 | 状态 | 操作 |
|------|------|------|
| `widgets/grouped_label_list_widget.py` | ✅ 已独立 | 直接复制 |

**无需重构**。完全独立的UI组件。

### 已有测试
源项目中已有测试文件:
- `tests/test_grouped_label_list_widget.py`
- `tests/test_grouped_model_logic.py`

建议一并复制测试文件到3.3.8的 `tests/` 目录。

### 迁移工作量估算
- 复制文件：1个（+ 2个测试文件）
- 修改文件：1个（label_widget.py）
- 新增代码：~30行
- 预计耗时：1小时
