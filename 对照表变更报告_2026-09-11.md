# 对照表变更报告（2026-09-11）

> 方法：《对照表维护方法.md》（纯净版 vs 魔改版代码对比法，函数为最小对比单元）
> 判官：`D:\X-AnyLabeling-4.0.0-beta.4` 目录快照（纯净版）
> 取数：本仓库工作区现状（HEAD `8955add5` + 46 条未提交），AST 函数级全对比
> 台账：`.tmp-doc-update/plan/`（func_ledger.json / attribution.json / classify_result*.json / attribution_summary.md）
> 更新对象：《菜单功能对照表.md》《个人扩展功能提炼.md》

## 一、变更总览

| 类别 | 数量 | 说明 |
|---|---:|---|
| 新增条目 | 12 | 12 个全新 UI 入口进入对照表（提炼表另补录 5 个视图入口 + 8 个工具入口 + 23 行链路增强） |
| 状态变化 | 31 | 📦上游 → 🔧增强（槽函数/专属辅助被改写，Tier A 28 + Tier B 4，其中 `set_cross_line` 原表已 🔧 不重复计） |
| 消失条目 | 0 | 无 UI 入口被删除；上游 140 个 action 全部健在 |
| 冲突条目 | 1 + 统计修正 | `toggle_annotation_checked` 旧表误标 ✅新增，实为上游已有（✅→📦）；旧统计节算术误差一并重算 |

## 二、新增条目（12，对照表实际插入位置）

| 标识符 | 显示文本 | 菜单 | 快捷键 |
|---|---|---|---|
| `batch_mark_mode` | Cross-Image Object Marking | 工具 | —（checkable） |
| `clear_batch_marks` | Clear Cross-Image Marks | 工具 | — |
| `relabel_marked_objects` | Relabel Marked Objects... | 工具 | — |
| `edit_marked_object_fields` | Batch Edit Marked Objects... | 工具 | — |
| `restore_object_relabel` | Restore Object Relabel from Manifest... | 工具 | — |
| `dataset_label_thumbnails` | Dataset Label Thumbnails... | 工具 | — |
| `locate_in_thumbnails` | Locate in label thumbnails | 工具 | Ctrl+Alt+T（默认） |
| `assign_shape_ids` | Assign Shape IDs to Project | 工具 | —（默认置灰） |
| `behavior_analytics` | Local Behavior Analytics | 视图 | — |
| `isolate_selection` | Isolate Selection/Group | 视图 | 配置键（checkable） |
| `virtual_review_next` | Next Virtual Review Task | 窗口级 | F2（默认） |
| `virtual_review_prev` | Previous Virtual Review Task | 窗口级 | Shift+F2（默认） |

README# 编号：全部留 `—` 待补（本轮口径，见未决问题 3）。

## 三、状态变化（31，📦→🔧）

- **Tier A（槽函数实质修改，28）**：`open_` `open_next_image` `open_prev_image` `change_output_dir` `close` `delete_file` `delete_image_file` `edit` `duplicate` `delete` `copy` `paste` `undo` `remove_point` `visibility_shapes_mode` `hide_selected_polygons` `show_hidden_polygons` `label_manager` `digit_shortcut_manager` `digit_shortcut_0`~`9`
  典型证据（`func_ledger.py show` 可复核）：
  - `LabelingWidget.undo_shape_edit`：新增 `rectangle_workflow.draft` 草稿优先撤销分支；
  - `LabelingWidget.edit_label`：新增 `canvas.notify_shape_changed(shape)` 同步；
  - `LabelingWidget.paste_selected_shape`：新增 `created_count` 与 id 归一化；
  - `LabelingWidget.digit_shortcut_manager`：新增 `digit_page_manager.update_shortcuts(...)` 分页联动。
- **Tier B（专属辅助改写，4）**：`opendir` `loop_thru_labels` `select_toggle_shapes` `set_cross_line`
  - `set_cross_line` 已核清：方法自 LabelingWidget **迁移至** `Canvas.set_cross_line`（`canvas.py:6970`，签名扩为 show/width/color/opacity），改由设置面板 `canvas.crosshair` 驱动——与"十字线设置并入设置面板"的既有记录闭环。

## 四、冲突条目（1 + 统计修正）

1. **`toggle_annotation_checked`**：旧表（08-12）在窗口级备注中标 ✅新增；本轮对比纯净版 `label_widget.py:565` 即有 `toggle_annotation_checked = action(`（fork 在 `:966`）。**以代码为准改 📦上游**，已在对照表第七节备注中留痕。
2. **统计算术修正**：旧统计节"视图菜单 50 项（16✅）"等与逐行实际不符（实际视图 48 行、✅22）；本轮以脚本逐行重算（去重前 215 行 / 去重后 178 独立 action = 38✅ + 32🔧 + 108📦）。

## 五、git 查无改动清单（"部分修改 git 里没有"的兜底证据）

本轮所有数字按**工作区现状**取数，以下 46 条未提交内容已包含在台账与两表中（取数 2026-09-11）：

- 已跟踪修改 28：`xanylabeling_config.yaml`、`resources.py`、4 个翻译文件、`label_widget.py`、settings 三件套、dataset_thumbnail 3 文件、`digit_bind_draw_manager.py`、`label_batch.py`、`label_dialog.py`、`object_relabel*.py`、`rect_edge_click_controller.py`、`rectangle_workflow.py`、openspec 5 文件、tests 2 文件、两张本表；
- 未跟踪新增 18：`review_refinement/keyboard_fitting.py`、`keyboard_shortcuts.py`、`dataset_thumbnail/history_*.py`×5、`incremental.py`、`page_diff.py`、`rectangle_fit_hud.py`、`rectangle_keyboard.py`、tests 3 个、openspec 2 目录、`artifacts/`、`pelican-ride.html`、`对照表维护方法.md`。

> 若稍后提交工作区，建议下轮以新 HEAD 重跑 `func_ledger.py scan` 刷新台账（分钟级）。

## 六、范围外差异（不进功能表，仅备案）

- 非 anylabeling 的差异 .py 4 个：`scripts/compile_languages.py`、`scripts/generate_languages.py`、`tests/test_settings/test_dialog_layout.py`、`tests/test_schema.py`（工程/测试域）。
- 配置/翻译层：`xanylabeling_config.yaml` +169 行（新功能默认项）；4 个 .ts 各 +1100~1300 行翻译串；`pyproject.toml` ±12；`.gitignore` +48；`.pre-commit-config.yaml` +1/-7；README ×2。
- fork 独有模块 173 个 .py（约 2453 函数）：分组清册见 `classify_result2.json`。

## 七、未决问题（待用户拍板）

1. **i18n 不一致**：`toggle_precision_mode_lock`（精修模式锁定）、`toggle_rect_edge_align`（矩形单边调整）、`toggle_rect_refine_mode`（三框聚焦模式）3 个入口显示文本为中文硬编码，其余 35 个新入口均为英文 `tr()`。是否本轮统一改 `tr()` 并补 4 语翻译？
2. **两表口径已写死**（建议维持）：纯净版判官 = 目录快照；魔改侧 = 工作区现状。若工作区后续提交，请同步刷新两表头部的取数时间戳。
3. **README#/模块# 编号**：本轮新条目一律留 `—`；`docs/portfolio/` 编号仅旧条目持有。是否需要一轮集中补编？
4. **仓库卫生**：未跟踪的 `artifacts/`（2956 文件）、`pelican-ride.html` 与本任务无关，建议加入 `.gitignore` 或清理。
