# X-AnyLabeling 个人扩展版（beta.4 fork）— 补充维度对照表

> ## ⚠ 状态声明（2026-08-12 核对）
>
> **基准 commit**：`77cf2832`。维度一/二/三/四/五/七的结论基于 git pickaxe/MD5 验证，**可靠**。
>
> **已知误差（维度六测试统计）**：维度六把 5 个 viewport 测试文件（`test_viewport_state_machine.py` / `test_viewport_reset.py` / `test_viewport_context_menu.py` / `test_viewport_controller_results.py` / `test_viewport_label_widget_integration.py`）算进了"已存在测试"——核对后发现这 5 个是**未跟踪文件**（`git status` 显示 `??`），属于工作区正在进行的 viewport 状态机重构，尚未提交。实际已提交测试文件数应为 **~89 而非 ~94**，总行数相应核减 ~887 行。下方维度六已加 `⚠` 标注。
>
> **正在进行的工作**：工作区还有 `viewport_state_machine.py`（未跟踪新文件）、`viewport_controller.py`（402 行未提交改动）、4 个 openspec 变更目录（未跟踪）——这是一次模块 #15 的状态机拆分重构，未提交前不计入 fork 已发布功能。
>
> ---

> 本表是前三份文档（[`菜单功能对照表.md`](菜单功能对照表.md) / [`设置面板字段对照表.md`](设置面板字段对照表.md) / [`个人扩展功能提炼.md`](个人扩展功能提炼.md)）的补充，覆盖**前三份文档抓不到的 7 个维度**。
>
> 这 7 个维度源于一次自我评估：前三份文档共享同一个方法论局限——**只从 UI 向下提取**（菜单 → 配置 → 模块文档），而 fork 的很多实质改动在 UI 之下。本表用 `git diff --stat` 热度榜 + `git log --diff-filter=A` 新增文件分布建立全局改动面后，逐维度补全。
>
> - **数据来源**：git 仓库（基线 `b99efef6`，HEAD `77cf2832`，153 commits）+ 文件系统扫描。
> - **状态**：`✅新增` = fork 新建；`🔧增强` = 上游已有、fork 改写；`📦上游` = 未改；`⛔废弃` = 归档/搁置。
> - 每条均带 file:line 与 commit hash，可复核。

---

## 维度一：数据持久化层（隐藏的数据完整性增强）

前三份文档只覆盖 UI 和配置，但 fork 对**标注文件读写本体**有实质改动。这是最易遗漏的一层——用户看不到，但崩溃/断电场景下数据完整性显著不同。

### 1.1 `label_file.py` — 原子保存（🔧增强，核心）

| 改动 | 位置 | 状态 | 说明 |
|---|---|:---:|---|
| 原子保存（tempfile + fsync + os.replace） | `label_file.py:233-257` | 🔧增强 | 临时文件与目标同目录（保证同卷原子）→ `os.fsync` 强制落盘 → `os.replace` 原子替换；崩溃时目标保持旧版本完整。基线是直接覆写，写入中崩溃会留半截损坏 JSON |
| 性能埋点 | `label_file.py:21-27, 75-152` | ✅新增 | `XANYLABELING_PERF_LOG=1` 环境变量开启 load/save 分段计时（>50ms/100ms 触发），默认零噪声 |
| load 鲁棒性 | `label_file.py:113, 129, 143-148` | 🔧增强 | `data["imageData"]` → `data.get("imageData")`；`_check_image_height_and_width` 包进 `if image_data is not None`，缺字段不崩 |

> **重要更正**：`个人扩展功能提炼.md` 第九节"存储解耦"说"索引失败不影响标注保存"——这是对的；但若解读为"save 自动同步 index"则是错的。**`LabelFile.save` 本身不触发索引刷新**，索引失效/刷新由 `label_widget` 层负责。

### 1.2 `shape.py` — 渲染缓存 + deepcopy 修复（🔧增强，非数据模型扩展）

**关键判断**：153 行改动是**渲染性能 + 拷贝安全**，不是数据模型字段扩展。磁盘 JSON schema 100% 向后兼容基线。

| 改动 | 位置 | 状态 | 说明 |
|---|---|:---:|---|
| 几何缓存（bbox/path） | `shape.py:113-116, 768-833` | ✅新增 | 4 个缓存槽 `_cached_bbox/_cached_bbox_key/_cached_path/_cached_path_key`；`make_path()`/`bounding_rect()` 改为带缓存版本 |
| 缓存失效点 | `shape.py:161,179,230,278,389,392,395,841,846,886` | ✅新增 | 所有几何变更入口调用 `_invalidate_cache()` |
| deepcopy/pickle 修复 | `shape.py:869` | ✅新增 | `__getstate__` 把 Qt 缓存槽置 None，避免 QPainterPath 不可拷贝对象在 `copy_shape()` 崩溃 |
| paint 无副作用化 | `shape.py:420-431` | 🔧增强 | 新增 `force_unselected` 参数；修复原来 paint 期间可能改变 `selected` 状态的副作用 |
| `locked` 占位字段 | `shape.py:96` | ✅新增 | 注释明示是"迁移自 beta.11 功能C"的**有意 no-op**，不进 KEYS、不序列化、`not shape.locked` 永远 True |

### 1.3 `label_shape_contract.py` — 数据契约层（✅新增，50 行）

fork 新建（commit `57a74d93`），定义**标签 ↔ shape_type 的中央契约**，被 inspector 校验器共用，避免硬编码。

| 内容 | 位置 | 说明 |
|---|---|---|
| `RECTANGLE_SHAPE_TYPE` / `POINT_SHAPE_TYPE` | `:5-6` | shape_type 常量 |
| `DEFAULT_RECTANGLE_LABELS` | `:8` | `("person", "head", "face")` 三类检测框 |
| `DEFAULT_POINT_LABELS` | `:9-27` | 17 个 COCO 关键点名 |
| `expected_shape_type_for_label(label)` | `:39` | **契约核心 API**：给定标签返回应绑定的 shape_type |
| `labels_to_csv(labels)` | `:48` | 标签序列渲染成 CSV，供 UI 配置默认值 |

**使用方**：`inspector/rule_config_widget.py:20-24,46-48`、`inspector/validation_engine.py:13`、`inspector/quality/l1_rules.py:16-19,98`。测试：`tests/test_label_shape_contract.py`（50 行）。

### 1.4 `label_converter.py` — ⚠️ 零改动（重要更正）

| 文件 | 改动 | MD5 一致性 |
|---|:---:|---|
| `anylabeling/views/labeling/label_converter.py` | **0 行** | 基线 = HEAD = `1729f34c5ddff6c39515ef6410a1344e` ✓ |
| `anylabeling/views/labeling/utils/upload.py`（21 文件目录） | **0 行** | 整个 `utils/` 目录零改动 |
| `anylabeling/views/labeling/utils/export.py` | **0 行** | 同上 |
| `tools/label_converter.py` / `tools/label_drawer.py` | **0 行** | 同上 |

> **重要更正**：菜单对照表把导入/导出 38 项标 `📦上游` 是**完全正确**的——不只是菜单标识符没变，**底层转换函数也是字节级一致**。YOLO Pose 关键点可见性、COCO 字段、所有格式的转换逻辑全部保持上游原样。之前自我评估怀疑"底层可能被改写"，经 MD5 双重验证确认**未改**。

---

## 维度二：`pose_qa/` 独立子项目（✅新增，约 5242 行）

前三份文档只列了 `scripts/`（模块 #06），**完全漏掉了 `pose_qa/` 这个并行子项目**。它是 fork 新增的独立姿态标注 QA 工具链，有自己的 README、docs、四阶段流水线。

### 2.1 目录结构

| 路径 | 行数 | 职责 |
|---|---:|---|
| `pose_qa/README.md` | 195 | 自述：对人工 pose 标注批量 QA，捕获语义错误（"nose 标到脚踝"/左右关键点互换），把 3.5 万张图精简到前 5-10% 高风险尾部 |
| `pose_qa/docs/` | 1989 | 5 篇设计文档（pipeline_design/quality_strategy/grouping 策略/troubleshooting） |
| `pose_qa/export/export_vitpose_onnx.py` | 396 | **阶段 1**：ViTPose → ONNX（一次性，隔离 conda 环境 `vitpose_export`） |
| `pose_qa/inference/vitpose_inference_demo.py` | 589 | **阶段 2a**：单图 ViTPose 推理 + 可视化（健全性检查） |
| `pose_qa/inference/batch_infer.py` | 464 | **阶段 2b**：目录级批量推理 |
| `pose_qa/grouping/group_merger.py` | 878 | group_id 回写清洗器（匈牙利一对一匹配） |
| `pose_qa/qa/pose_qa_compare.py` | 846 | **阶段 3**：OKS + 归一化距离的模型差异对比 + 排序 |
| `pose_qa/qa/baseline_stats.py` | 806 | 基于可信基准语料库分布的离群点检测 |
| `pose_qa/qa/spatial_check.py` | 882 | 分组后几何 QA（6 条固定阈值规则） |
| `pose_qa/qa/export_for_inspector.py` | 381 | **阶段 4**：QA 报告 → Inspector TSV 转换器 |

### 2.2 与 Inspector L1/L2 引擎的关系（关键）

**结论：功能上 L2 有重叠，但 pose_qa 是独立的研究/原型工具集，不是重复代码。**

| 层 | pose_qa | inspector/quality/ | 关系 |
|---|---|---|---|
| L1 硬规则 | — | `l1_rules.py`（5 条） | 仅 inspector 有 |
| L2 人体结构 | `spatial_check.py`（6 条固定阈值）+ `baseline_stats.py`（分布式） | `l2_rules.py`（12 条，profile 驱动） | **功能重叠**：inspector 是 profile 驱动的生产版，pose_qa 是固定阈值/分布的前身 |
| L3 模型差异 | `pose_qa_compare.py`（OKS + ViTPose） | — | **仅 pose_qa 有**，inspector 无对应 |
| group_id 回写 | `group_merger.py`（永久回写） | `matching.py`（临时推理，不回写） | 架构不同 |
| L4 复查队列 | `export_for_inspector.py`（导出给 inspector） | `quality_review_queue.py` | pose_qa 最终汇入 inspector |

**L3 模型差异（OKS + ViTPose）是 pose_qa 独有的，整个 `anylabeling/` 主树没有任何对应实现**（`grep "OKS|ViTPose|L3"` 在 inspector/ 返回空）。

### 2.3 入口点

8 个脚本**全部是独立 CLI**（各有 `parse_args` + `__main__`），运行在 `xanylabeling-cu12` conda 环境（阶段 1 用独立 `vitpose_export` 环境）。**`scripts/` 下的脚本不调用 pose_qa**，pose_qa 也不导入 `scripts/`，两者独立。

### 2.4 `group_merger.py` 的物理约束匹配（亮点）

不同于 inspector 的通用空间评分，group_merger 用**标注员物理习惯**做匹配：
- head ↔ person：依据 `|head.top − person.top|`（标注员都从头顶下拉这两个框）
- face ↔ head：依据 `|face.bottom − head.bottom|`（face 到下巴，head 到颈部）
- 用 `scipy.optimize.linear_sum_assignment` 做全局最优匈牙利一对一匹配，避免贪婪匹配的搁置问题

### 2.5 README 不一致（值得标记）

`pose_qa/qa/export_for_inspector.py` 实际输出 **TSV**（`write_tsv` @ `:282-307`），但 `pose_qa/README.md:144-156` 描述为 `pose_qa_inspector.json`。README 示例命令的 `--output` 后缀也写错。这是 fork 文档自身的 bug。

---

## 维度三：`historical_code_3.3.8/` 归档（⛔归档，约 4631 行）

fork 在仓库内建了归档目录，前三份文档完全没提。对理解功能演进有参考价值。

### 3.1 目录结构与性质

| 文件 | 行数 | 性质 |
|---|---:|---|
| `historical_code_3.3.8/label_dialog.py` | 1952 | PyQt5 时代旧版（当前是 PyQt6，2144 行） |
| `historical_code_3.3.8/digit_rename_manager.py` | 565 | 旧版（当前重构后 400 行） |
| `historical_code_3.3.8/digit_shortcut_page_manager.py` | 295 | 旧版（当前 311 行） |
| `historical_code_3.3.8/data_manager/`（5 文件） | 1846 | **从未集成进主树的原型** |
| `historical_code_3.3.8/*.json` × 2 | — | 标注样例（person + 17 关键点） |
| `historical_code_3.3.8/*.png` × 1 | — | UI 参考截图 |

### 3.2 建立时间与动机

- commit `eaa6f402`（2026-04-28，"数字键多标签分页"）：重写数字键功能时，把三个文件的**重写前快照**丢进归档作为回退参考
- commit `5332364d`（2026-04-30，"新增数据"）：建立整个 `data_manager/` 子包

### 3.3 `data_manager/` —— 被放弃的"数据管理中心"原型

`historical_code_3.3.8/data_manager/` 是一个**完整但从未集成**的原型（文件视图/标签视图/对象视图三标签页），`git log --all -- anylabeling/views/labeling/widgets/data_manager/` 返回空——**整个 git 历史中从未进入主代码树**。其功能后来被 Inspector 面板（模块 #02）取代。

---

## 维度四：上游格式转换的底层实现（📦上游，零改动，重要更正）

自我评估时怀疑"菜单 action 标识符没变，但底层转换函数可能被改写"。经 MD5 + git log 双重验证，**这个怀疑不成立**。

| 验证项 | 结果 |
|---|---|
| `anylabeling/.../label_converter.py` | 基线 = HEAD（MD5 一致），0 commit 触碰 |
| `anylabeling/.../utils/upload.py`（21 文件） | 整个 `utils/` 目录 0 改动 |
| `anylabeling/.../utils/export.py` | 同上 |
| `tools/label_converter.py` / `tools/label_drawer.py` | MD5 一致，0 改动 |
| import/export slot（`upload_yolo_annotation` 等） | 通过 lambda 接到 `utils/upload.py`，实现层零改动 |

**结论**：菜单对照表里导入/导出 38 项全标 `📦上游` 是**正确且完整**的，无需修订。fork 的实际精力集中在 inspector/pose_qa/筛选/矩形边精修/数字键，**不在格式转换**。

### 容易误判的"伪导入/导出"文件（已澄清）

以下文件名带 convert/export/import，但**不是上游菜单的格式转换**，属于 fork 新增的其他功能：

| 文件 | 实际职责 | 模块归属 |
|---|---|---|
| `widgets/inspector/export_manager.py` | Inspector 报告线程化导出（commit `57a74d93`） | #02 |
| `widgets/inspector/external_result_importer.py` | 外部校验结果导入 inspector | #02 |
| `scripts/convert_*.py` / `scripts/step1-convert_*.py` | 独立 QA/pose 脚本 | #06 |
| `pose_qa/export/export_vitpose_onnx.py` | ViTPose ONNX 导出 | 维度二 |

---

## 维度五：依赖与打包层（📦上游，零改动）

### 5.1 `pyproject.toml`（26 行改动，但与依赖/打包无关）

| 改动节 | 内容 | 性质 |
|---|---|---|
| `[tool.pytest.ini_options]` | 新增 `testpaths = ["tests"]`、`python_functions = "test_*"` | 测试工具配置 |
| `[tool.black]` | `exclude` → `extend-exclude`，简化正则 | 代码格式化工具配置 |

**逐项确认零改动**：
- `dependencies` 数组：逐字节相同（28 项依赖）
- `[project.optional-dependencies]`（dev/cpu/gpu/gpu-cu11）：相同
- `[project.scripts]`：仍只有 `xanylabeling = "anylabeling.app:main"`
- 元数据（name/version/description）：相同
- `requires-python = ">=3.11"`：相同

### 5.2 其它打包文件

| 文件 | 改动 |
|---|:---:|
| `packaging/`（PyInstaller 配置） | 📦上游，0 改动 |
| `requirements.txt` / `setup.py` / `setup.cfg` | 不存在（纯 pyproject.toml 管理） |
| `GITHUB_PINNED_SETUP.md`（176 行） | ✅新增，但**与代码无关**——教 fork 作者如何把 GitHub 主页 Pinned 卡片换成自己的 fork |

---

## 维度六：测试覆盖映射表 ⚠

> **⚠ 误差声明**：本节原记"~94 个测试文件、~18,700 行"，其中 5 个 viewport 测试文件（共 ~887 行）经 `git status` 核对为**未跟踪**（工作区正在进行的状态机重构）。**已提交测试应为 ~89 个文件、~17,813 行**。下表"F. 视口持久化"行的 5 个文件标注了 `未跟踪`。

前三份文档只在每节标了测试数，没有"测试 → 被测功能"映射。fork 共 **~89 个已提交测试文件、~17,813 行测试代码**（另有 5 个 viewport 测试 ~887 行未跟踪）。

### 6.1 按功能模块的测试分布

| 功能模块 | 测试文件数 | 总行数 | 代表性文件 |
|---|:---:|---:|---|
| 矩形尺寸/覆盖层（person 小目标） | 14 | ~3,952 | `test_size_overlay.py`（744）、`test_rectangle_size_e2e.py`（439） |
| 矩形边/精修 | 7 | ~2,565 | `test_rect_edge_canvas_semantics.py`（1180）、`test_rect_refine_grouping.py`（232） |
| 质检引擎 + 复查队列 | 7 | ~2,435 | `test_quality_review_queue.py`（586）、`test_quality_suggestion.py`（408） |
| 数据集索引/筛选 | 10 | ~1,851 | `test_dataset_index_controller.py`（528）、`test_filter_persistence.py`（267） |
| Inspector 面板 | 5 | ~1,618 | `test_inspector_validation.py`（485） |
| 数字绑定绘制 + auto person | 3 | 1,021 | `test_digit_bind_draw_manager.py`（487）、`test_auto_person_instance.py`（486） |
| 视口持久化 | 5（⚠ 均未跟踪） | 887 | `test_viewport_state_machine.py`（254）——见误差声明 |
| Pose View | 4 | 721 | `test_pose_category_layout.py`（267）、`test_pose_layout.py`（213） |
| 设置子系统 | 3 | 715 | `test_settings/test_controller.py`（348） |
| 十字线 + 精修模式 | 3 | 391 | `test_crosshair.py`（179）、`test_precision_mode.py`（115） |
| 形状契约/标签文件/标签布局 | 4 | 323 | `test_label_file_atomic_save.py`（55）、`test_label_shape_contract.py`（50） |
| 优先级拾取（选择优化） | 1 | 321 | `test_shape_hit_candidates.py`（321） |
| Canvas 通用/配置/特性交互 | 4 | 449 | `test_canvas_change_notifications.py`（180） |

### 6.2 测试覆盖的缺口（Portfolio 矩阵与实际的差异）

| Portfolio 模块 | 矩阵声明 | 实际测试文件 | 差异 |
|---|---|---|---|
| #13 数字键分页 | — | 无专属测试文件 | **缺口** |
| #14 数字键改名 | — | 无专属测试文件 | **缺口** |
| #10 局部边缘吸附 | — | 已退役，测试删除 | 一致（退役） |
| #12 缩放中心修复 | — | 无专属测试文件 | **缺口**（修复型，可能依赖手动验证） |
| #06 数据脚本集 | — | 无单元测试 | 一致（CLI 脚本） |

---

## 维度七：`docs/` 文档库索引（166 篇）

前三份文档只引用了 `docs/portfolio/`（16 篇），但 `docs/` 下还有约 150 篇设计文档未被索引。

### 7.1 文档分布

| 范围 | .md 文件数 |
|---|---:|
| `docs/` 根目录（顶级） | 99（含 1 个 .txt） |
| `docs/portfolio/`（已覆盖） | 16 |
| `docs/Document-Index/` + `feature-records/` | 22 |
| `docs/en/` / `docs/zh_cn/` | 10 / 11 |
| `docs/incremental_review_outputs/` | 10 |
| `docs/superpowers/` | 4 |
| **仓库 .md 总数** | **166** |

### 7.2 顶级 `docs/` 按前缀分类（99 篇）

| 前缀 | 数量 | 主题 |
|---|:---:|---|
| `qc-` | 13 | L1/L2 质检（三阶段 bbox 质量治理、group_id 解耦、ViTPose 分歧排序） |
| `pose-` | 12 | Pose View（标签解耦/布局/拖曳/自动聚焦/加载优化） |
| `filter-` | 12 | 筛选系统（状态引擎/JSON 规则/SQLite 索引/优先级拾取/导航/EXIF） |
| `cls3-` | 11 | 矩形边对齐/编辑 + 三框精修模式 |
| `canvas-` | 8 | Canvas 分析报告/重构方案/视口状态/缩放中心飘移 |
| `meth-` | 7 | 方法论 prompt 指南（问题建模/标注质量方法论/模式卡片） |
| `inspect-` | 3 | Inspector 面板设计/参考手册/外部结果格式 |
| `arch-` | 2 | 早期开发记录 + 文件名差异报告 |
| `doc-` | 2 | 文档分类清单 + 自扩张文件名流程 |
| 无前缀（中文/英文） | ~17 | 稳定精修预览、Person 尺寸浮层、增量代码评测、LLM playbook 等 |
| 索引/总览 | 4 | `PORTFOLIO.md`、`PORTFOLIO_EN.md`、`superpowers-readme.md`、`openspec-README.md` |

### 7.3 关键方法论文档（模式卡片）

| 文档 | 主题 | 关联模块 |
|---|---|---|
| `meth-050_note_模式卡片001_决策转排序.md` | "决策转排序"模式：把多重 if-elif 决策转成可比较的元组排序 | #03 优先级拾取 |
| `filter-030_des_筛选状态引擎模式.md` | State + Engine 架构模板（Qt 信号/跨图持久化/UI 刷新） | #11 筛选系统 |
| `canvas-020_des_canvas重构方案.md` | Canvas.py 拆分重构方案（3684 行 God object → 5 个正交关注点） | 驱动 #02/#04/#15 重用 |

### 7.4 额外的索引文件（除 PORTFOLIO.md 外）

| 索引文件 | 范围 |
|---|---|
| `docs/PORTFOLIO.md` / `PORTFOLIO_EN.md` | 16 功能模块主作品集（中/英） |
| `docs/Document-Index/README.md` | 文档总目录（AI prompt 描述/Canvas 扩展同步/项目维护） |
| `docs/Document-Index/feature-records/`（11 篇） | 按功能划分的记录（dataset_filter_index/digit_rename/inspector/viewport 等） |
| `docs/doc-010_spec_文件分类清单.md` | 文件分类规范（前缀命名约定） |
| `docs/feature_summary.md` | 非正式功能汇总 |

### 7.5 Portfolio 未正式计入的功能（散落在无前缀文档）

- **稳定精修预览**（6 篇文档：功能总结/任务文档/Review 清单/阶段二/阶段三）
- **小目标 Person 矩形尺寸浮层**（3 篇：需求/设计/Review 清单）—— 对应 `rectangle_size_controller` 系列，已部分覆盖在设置面板对照表，但 portfolio 16 模块里没有独立编号

---

## 维度八（汇总）：fork 改动全景纠正与补充

### 8.1 对前三份文档的纠正

| 文档 | 原表述 | 纠正 |
|---|---|---|
| 个人扩展功能提炼 #9 | "存储解耦：索引失败不影响标注保存" | 表述正确，但需补充：`LabelFile.save` **不触发** index 同步，索引刷新在 label_widget 层 |
| 菜单对照表 五/六节 | 导入/导出 38 项 `📦上游` | **正确**，底层转换函数 MD5 一致，无需修订 |
| 设置面板字段对照表 | schema.py 是 fork 新增？ | **纠正**：schema.py 和整个 `settings/` 目录是**上游 beta.4 原版**，fork 只在其上新增字段 |
| 个人扩展功能提炼 #6 | label_converter 列为可增强项 | **纠正**：零改动，不应归因 fork |

### 8.2 fork 改动的真实分布（按改动量排序）

| 子系统 | 改动规模 | 已有文档覆盖？ |
|---|---:|:---:|
| `label_widget.py`（菜单/工具栏/主逻辑） | 3684 行 | ✅ 菜单对照表 |
| `canvas.py`（画布交互） | 2566 行 | ✅ 菜单对照表第四节 + 提炼表第四节 |
| `pose_qa/`（独立子项目） | 5242 行 | ❌ **本表维度二补全** |
| `inspector/`（质检面板+引擎） | ~9756 行 | ✅ 提炼表五/六节 |
| `dataset_index/`（索引系统） | ~2297 行 | ✅ 提炼表第九节 |
| `historical_code_3.3.8/`（归档） | 4631 行 | ❌ **本表维度三补全** |
| `shape.py` + `label_file.py` + 契约 | 287 行 | ❌ **本表维度一补全** |
| `tests/`（测试） | ~17,813 行已提交 + ~887 行未跟踪 | ❌ **本表维度六补全** |
| `docs/`（设计文档） | 166 篇 | 部分（portfolio 16 篇）；**本表维度七补全索引** |
| 导入/导出/格式转换 | 0 行 | ✅ 菜单对照表（确认零改动） |
| 依赖/打包 | 0 行（仅工具配置） | ❌ **本表维度五补全** |

### 8.3 前三份文档 + 本表 = 完整覆盖矩阵

| 维度 | 菜单对照表 | 设置面板对照表 | 个人扩展提炼 | **本补充表** |
|---|:---:|:---:|:---:|:---:|
| UI action（菜单/工具栏/右键） | ✅ | — | 部分 | — |
| 配置字段（SettingField + yaml） | — | ✅ | 部分 | — |
| 功能模块（portfolio 16） | — | — | ✅ | — |
| 数据持久化层 | — | — | — | ✅ 维度一 |
| pose_qa 子项目 | — | — | — | ✅ 维度二 |
| 归档代码 | — | — | — | ✅ 维度三 |
| 格式转换底层 | — | — | — | ✅ 维度四（确认零改动） |
| 依赖/打包 | — | — | — | ✅ 维度五 |
| 测试映射 | — | — | — | ✅ 维度六 |
| 文档索引 | — | — | — | ✅ 维度七 |

---

## 附：本表的由来（自我评价记录）

这份补充表源于对前三份文档的一次自我评估。前三份文档共享同一个方法论局限：**只从 UI 向下提取**（菜单 → 配置 → 模块文档），导致反复出现同一类遗漏——

| 遗漏事件 | 根因 | 本表对应维度 |
|---|---|---|
| 十字线（`set_cross_line`）被漏 | 只筛"fork 从无到有"，漏"上游已有、fork 改写" | 菜单对照表已补 🔧增强 |
| `digit_shortcut_mode` 被漏 | 只扫菜单 action，漏"配置层无 UI 入口"的字段 | 设置面板对照表已补 |
| 数据层原子保存被漏 | 只看 UI/配置，漏"持久化层无入口"的改动 | **本表维度一补全** |
| pose_qa 3400 行被漏 | 只列 scripts/，漏并行子项目 | **本表维度二补全** |
| 上游格式转换"疑似增强"误判 | 只验 action 定义行，没验底层函数体 | **本表维度四纠正**（MD5 验证零改动） |

**根本教训**：建立文档前应先用 `git diff --stat` 热度榜 + `git log --diff-filter=A` 新增文件分布建立**改动面全貌**，再分维度提炼。本表即按此方法补全。

1. **找遗漏维度**：对照第八节"完整覆盖矩阵"，本表维度即前三份文档的盲区。
2. **定位代码**：每条带 file:line 和 commit hash，可直接复核。
3. **验证来源**：`git show b99efef6:<path> | md5sum` vs `git show HEAD:<path> | md5sum` 验证零改动文件；`git diff --stat b99efef6 HEAD -- <path>` 查改动量。
4. **交叉查阅**：维度一 → 个人扩展提炼 #16；维度二 → 提炼表 #01/#06；维度六测试 → 提炼表各节测试数；维度七文档 → portfolio 16 篇。
