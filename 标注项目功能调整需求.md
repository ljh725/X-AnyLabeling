需求分层
1 怎么快速准确得调整标签类别 2 怎么更快速高效得调整矩形框 这两项功能是基础操作层面 

3 怎么从流程上更高效得数据检查方法完成大量标签的调整      这是流程设计层面

4 大模型是否有能力构建功能之间的逻辑关系 

第一条要求 
任何讨论的功能扩展 不要出现在一套工作流程 完成所有标签操作的东西 类似矩形框工具这类的功能不要再出现

大模型只分析功能之间逻辑联系 或者询问使用者 功能操作之间的流程关系 如果需要进行功能逻辑分析提出需求 需要任何扩展功能的插件 提出需求

目前需求新增的能力 

1 缩略图功能

增加功能 增加一个缩略图按照标签相邻坐标排序 出现重复检测框的时候 这个两个框因为相对坐标位置 会顺序排序 减少查看的负担

2 矩形边调整功能 


2-1 调整矩形框

"""
第一，减少“找旧边”的动作。
左边和右边只需要一个新的横坐标；上边和下边只需要一个新的纵坐标。
例如已经指定“左边”后，鼠标不必点在矩形左边线段上，只要点到目标正确左边界对应的横坐标即可。画面显示贯穿工作区域的参考线，可以帮助对齐。
这样鼠标直接去新边界，不必先寻找旧边的小命中区域。

"""
我理解的操作方式 

操作方式 鼠标标记目标边界点 + 键盘选边 + 输入点逻辑位置判断

左边和右边只需要一个新的横坐标；上边和下边只需要一个新的纵坐标 

键盘上标记两个快捷键 一个快捷键表示只需要修改Y坐标 另一个快捷键表示只需要修改x坐标 
选中对象之后 鼠标输入坐标点  项目自动判定 坐标点属于矩形框中心坐标点的划分 四象限区域中的某一块 

结合键位输入的调整范围就能进行矩形框对象的某条边的调整

2-2 角调整
两条相邻边不准 指定角＋点击新角点，同时更新两条边 


2-3
调整时候的预览情况
UI 画面显示贯穿工作区域的参考线，可以帮助对齐。


3 矩形工具调整 
首先 矩形工具调整功能 退役
这个工具被设计为流程化工具 四极值画框 应该作为 标注矩形框的功能 单独存在 
修正框功能单独拆解出来 作为编辑模式下 调整矩形框的一种方式 和 当前项目的矩形边编辑 和 默认角调整矩形框 并行的功能 也可以有个开关 开启使用
 

4 数据快捷重命名管理器 和 数字快捷键管理器 
两个功能保存的数据 这两个修改功能 后 都出现 保存的内容消失的情况 

5 项目重启后 页面跳转为英文

