# X-AnyLabeling 个人扩展版 — 功能总表(上游 + 个人扩展)

> **表格定位**:本表是 [`菜单功能对照表.md`](菜单功能对照表.md) / [`设置面板字段对照表.md`](设置面板字段对照表.md) / [`个人扩展功能提炼.md`](个人扩展功能提炼.md) / [`补充维度对照表.md`](补充维度对照表.md) 四份文档的**合并表一**:一个表格同时包含**上游原版功能**与**个人扩展功能**,覆盖 UI(菜单/工具栏/右键)、配置(设置面板/yaml)、核心引擎、数据层、脚本与子项目全部维度。
>
> 只看"自己改了什么"请读姊妹篇 [`个人修改与扩展清单.md`](个人修改与扩展清单.md)。

## 基准与验证方法

| 项 | 值 |
|---|---|
| 上游基准目录 | `D:\X-AnyLabeling-4.0.0-beta.4`(官方 CVHub520/X-AnyLabeling 4.0.0-beta.4 发布版) |
| 与 git 基线关系 | 抽查 `label_widget.py` / `canvas.py` / `shape.py` / `xanylabeling_config.yaml` / `pyproject.toml` 五文件 MD5,与 fork 仓库上游基线提交 `b99efef6` **逐字节一致** |
| fork 版本 | `feature/selection-optimization` 分支,HEAD `77cf2832`,基线后 **153 次提交** + 工作区未提交的视口状态机重构 |
| 验证方式 | 目录级 `diff -qr --strip-trailing-cr` 全量对比 + `git log -S` / `--diff-filter=A` pickaxe 交叉验证 |
| 状态图例 | 📦上游 = 上游原版未改;🔧增强 = 上游已有、fork 改写;✅新增 = fork 从无到有;⛔退役 = 曾实现后删除 |

**目录级全量对比结果**(排除 `.git`/`__pycache__`/`.specstory` 等噪声):

| 类别 | 数量 | 说明 |
|---|---:|---|
| 内容有差异的文件 | 34 | 核心代码 12 + i18n 资源 8 + 文档/配置 14(详见姊妹篇) |
| fork 独有文件 | 3830 | 核心新增代码:anylabeling/ 72 文件 22,968 行、scripts/ 41 文件 13,763 行、pose_qa/ 14 文件 7,425 行、tests/ 68 文件 16,602 行;其余为 docs(151)/openspec(59)/工具会话数据等非应用代码 |
| 仅上游有的代码文件 | 1 | `widgets/crosshair_settings_dialog.py`(fork 删除,功能并入设置面板) |

---

## 一、文件与图像管理(上游为主)

| 功能 | 状态 | 入口/快捷键 | 说明 |
|---|:---:|---|---|
| 打开单图 / 图片夹 / 视频 / 最近打开 | 📦上游 | Ctrl+I / Ctrl+U / — / 子菜单 | File 菜单 |
| 上一张 / 下一张 / 下一张未标注 / 上一张未标注 | 📦上游 | A / D / Ctrl+Shift+A / Ctrl+Shift+D | 导航 |
| 保存 / 另存为 / 自动保存 / 更改输出目录 / 保存含图像数据 / 关闭 | 📦上游 | Ctrl+S / Ctrl+Shift+S / — | File 菜单 |
| 删除标签文件 / 删除图片文件 | 📦上游 | Ctrl+Delete / Ctrl+Shift+Delete | |
| 对比视图(Compare View) | 📦上游 | `shortcuts["toggle_compare_view"]` | 滑条对比标注前后 |
| 批量自动推理(Auto Run) | 📦上游 | 工具栏,默认 `Ctrl+Shift+B`(上游 `Ctrl+B`) | 快捷键默认值被 fork 改绑 |
| 标注保存**原子写**(tempfile+fsync+os.replace) | 🔧增强 | 内部(`label_file.py`) | 崩溃/断电不再产生半截损坏 JSON |
| 保存/加载性能埋点 | ✅新增 | 环境变量 `XANYLABELING_PERF_LOG=1` | 默认零噪声 |
| 标注 JSON 读取鲁棒性(缺字段不崩) | 🔧增强 | 内部(`label_file.py`) | `data.get("imageData")` 等防御式读取 |

## 二、标注创建与编辑(上游)

| 功能 | 状态 | 入口/快捷键 | 说明 |
|---|:---:|---|---|
| 10 种创建模式:多边形/画笔多边形/矩形/立方体/旋转矩形/四边形/圆/线段/点/折线 | 📦上游 | P/—/R/—/O/—/—/—/—/— | 工具栏 + Edit 菜单 + 右键 |
| 编辑对象模式 / 编辑标签 | 📦上游 | E / Ctrl+E | |
| 复制对象/粘贴/复制形状/删除/撤销/撤销上一点/删除选中点 | 📦上游 | Ctrl+C/V/D、Delete、Ctrl+Z、Backspace | |
| 合并多选矩形(Union) / 复制坐标 | 📦上游 | — | |
| 保持上次标注/上次标签/上次 group id | 📦上游 | Ctrl+P / Ctrl+K / — | |
| 系统剪贴板 / 可见性模式 / 循环标签 / 循环选中标签 | 📦上游 | Ctrl+H 等 | |
| 隐藏/显示多边形、分组/取消分组 | 📦上游 | S / W / G / U | |
| **优先级排序拾取**(顶点>边>整体,距离/面积/栈序四维排序) | ✅新增 | 画布内部算法 | 悬停/单击/双击取同一候选序列,嵌套小对象不再被盖住 |
| **矩形单边独立拖动**(状态机 hover→press→实时→release) | ✅新增 | View 菜单,默认 `Ctrl+Shift+E` | 与绘制模式双向互斥 |
| **鼠标精修降速**(zoom/fixed 双模式 + 锁定开关) | ✅新增 | View 菜单锁定,默认 `Ctrl+Alt+F` | 3 个设置字段支撑;Tab 边选择/箭头微调等子功能已退役 |
| 局部边缘吸附(Sobel 梯度吸附) | ⛔退役 | (曾 Ctrl+Alt+E) | 2026-07-20 删除,仅留决策文档 |

## 三、AI 智能标注(上游)

| 功能 | 状态 | 入口/快捷键 | 说明 |
|---|:---:|---|---|
| Auto Labeling 面板 + 模型库(GitHub/ModelScope 双源) | 📦上游 | 工具栏 | ONNX 推理,几十种模型 |
| ChatBot / VQA / Classifier / PaddleOCR 对话框 | 📦上游 | 工具栏 | |
| Ultralytics 训练入口 | 📦上游 | Train 菜单 | |

## 四、视图与显示

| 功能 | 状态 | 入口/快捷键 | 说明 |
|---|:---:|---|---|
| 放大/缩小/原始尺寸/适应窗口/适应宽度 | 📦上游 | Ctrl++/Ctrl+-/Ctrl+0/Ctrl+F/Ctrl+Shift+F | |
| 亮度对比度调节 | 📦上游 | View 菜单 | |
| 显示开关:掩膜/文本/标签/置信度/角度/属性/KIE链接/分组 | 📦上游 | Ctrl+L、Ctrl+Alt+D、Ctrl+Shift+L 等 | |
| 保持上次缩放/亮度/对比度 | 📦上游 | View 菜单 | |
| 导航器(Navigator) | 📦上游 + 🔧 | `shortcuts["show_navigator"]` | 上游窗口;fork 加了状态持久化 |
| 十字线辅助线 | 🔧增强 | View 菜单 `set_cross_line` | fork 删独立十字线设置对话框,统一并入设置面板并深链跳转 |
| **矩形像素尺寸浮层**(小目标 person 精修) | ✅新增 | View 菜单 `show_rectangle_pixels`,默认开 | 4 文件子系统(`rectangle_size/` + 控制器/监视器/浮层/规则对话框) |
| **矩形尺寸规则告警**(类别级宽高阈值) | ✅新增 | View 菜单,默认关 | 规则编辑对话框 + 违规实时提示 |
| **选中对象显示标签**(稀疏标签模式) | ✅新增 | View 菜单 `label_on_selection`,默认开 | 悬停/选中/缩放三级显隐,配 `label_zoom_threshold` |
| **Pose View 关键点标签渲染**(4 种防遮挡布局) | ✅新增 | View 菜单 `pose_view` | direct/anti/column/category + 骨架/中线/引线/分色,20 个设置字段 |
| **三框聚焦模式**(选中 person 仅显示关联 head/face) | ✅新增 | View 菜单 `toggle_rect_refine_mode` | 配 `rect_refine.label_roles` 角色映射 |
| 主题(System/Light/Dark)与语言(EN/中/日/韩) | 📦上游 + 🔧 | 菜单 | fork 新增全部个人功能的 4 语言翻译 |

## 五、视口与缩放行为(个人扩展密集域)

| 功能 | 状态 | 入口/快捷键 | 说明 |
|---|:---:|---|---|
| 保持上次缩放 | 📦上游 | View 菜单 `keep_prev_scale` | |
| **切图保持视口**(缩放+视图中心) | ✅新增 | View 菜单 `keep_prev_viewport`,默认关 | 图片坐标持久化 |
| **重置当前图像视图 / 从当前到末尾 / 全部图像** | ✅新增 | View + 右键菜单三项 | 批量清理持久化视口 |
| **缩放中心点修复**(竖图 Ctrl+滚轮漂移) | ✅新增 | 内部算法 | 坐标锚点法;navigator 侧待修(Phase 2) |
| **视口状态机**(idle/pan/zoom 等状态集中管理) | ✅新增 | 内部 | 工作区未提交进行中(`viewport_state_machine.py`) |

## 六、标注效率工作流(数字键 / 关键点 / person)

| 功能 | 状态 | 入口/快捷键 | 说明 |
|---|:---:|---|---|
| 数字快捷键 0~9 绘制绑定 | 📦上游 + 🔧 | 0~9 | 上游 10 键;fork 加**分页层**(10 键 × N 页,`F1` 切页)与三模式派发(rename/draw/bind_draw) |
| 数字快捷键管理器 | 📦上游 + 🔧 | `shortcuts["edit_digit_shortcut"]` | 对话框内加页管理(增页/删页/翻页) |
| **数字键批量改名管理器** | ✅新增 | Alt+R | 编辑模式按数字键批量改标签,`LabelInfoScanThread` 后台扫描 |
| **数字键模式总开关**(rename / bind_draw) | ✅新增 | 设置面板 `digit_shortcut_mode` | |
| **自动创建 person 实例**(画框自动分配 max(gid)+1) | ✅新增 | 设置面板 `auto_person_instance`,默认关 | 免手动输 group id |
| **数字绑定绘制**(选源框按数字键同实例新建) | ✅新增 | 内部 `DigitBindDrawManager` | 两阶段 TOCTOU 去重回填 gid |
| **关键点填充模式**(为选中 person 补缺失关键点) | ✅新增 | K | 17 类 COCO 关键点 |
| **关键点填充工具窗口** | ✅新增 | Ctrl+Shift+K | 含切换上一个/下一个 person(Ctrl+Shift+[ / ]) |
| 进入 person 自动激活关键点填充 | ✅新增 | yaml `auto_activate_keypoint_fill`,默认关 | 隐藏配置 |
| 标签对话框(分组/搜索/补全/校验) | 📦上游 | 双击 shape | fork 改造其数字键区为分页 UI |

## 七、数据检查与质检(上游完全无,fork 全新)

| 功能 | 状态 | 入口/快捷键 | 说明 |
|---|:---:|---|---|
| **Inspector 数据检查面板**(QDockWidget 5 Tab) | ✅新增 | View 菜单 `toggle_inspector` | 数据检查/质检复核/数据表格/规则配置/导出 |
| 8 条基础结构规则 + 11 条扩展结构规则 | ✅新增 | 规则配置 Tab | 标签集/组唯一性/gid 绑定等 |
| **L1 结构合法性规则 5 条**(纯 Python) | ✅新增 | CLI `run_l1l2_qc.py` / 复核 Tab | 零 PyQt 依赖,UI 与 CLI 共享 |
| **L2 几何关系规则 12 条**(face→head→person) | ✅新增 | 同上 | 阈值 profile YAML 驱动,`error_requires` 二次确认 |
| **质检复核队列**(8 状态工作流) | ✅新增 | 质检复核 Tab | 写回 `review_feedback.tsv`,复扫合并 |
| 阈值建议生成(9 级优先级,恒 pending) | ✅新增 | CLI `gen_threshold_suggestion.py` | 不自动改 YAML |
| 外部校验结果导入 inspector | ✅新增 | 数据检查 Tab | |
| 标签↔shape_type 中央契约 | ✅新增 | 内部 `label_shape_contract.py` | person/head/face + 17 关键点契约 |

## 八、筛选与数据集索引(上游完全无,fork 全新)

| 功能 | 状态 | 入口/快捷键 | 说明 |
|---|:---:|---|---|
| **按标签/Group ID/Shape Type 筛选子菜单** | ✅新增 | 标签列表菜单 + 画布右键 | 动态 QMenu |
| **筛选值跨图保持**(Global Filter) | ✅新增 | View 菜单,默认开 | `load_file` 快照/恢复 |
| **过滤结果跨文件导航** | ✅新增 | 工具栏/View 菜单 + 刷新项 | 只在匹配文件间跳转 |
| **数据集 SQLite 索引**(6 态生命周期) | ✅新增 | 增量刷新/重建/取消 三 action | staging-DB 原子重建、并发读单线程写、JSON 仍是唯一真相源 |
| EXIF 方向批量扫描 action | ✅新增 | 工具栏/View 菜单 | (字段 `exif_scan_enabled` 为上游) |
| ShapeType 筛选下拉组件 | ✅新增 | 文件列表区 | `filter_label_widget.py` |

## 九、导入 / 导出(38 项全上游,字节级零改动)

| 功能 | 状态 | 入口 | 说明 |
|---|:---:|---|---|
| 导入 21 种格式(Image Flags/Label Flags/Attributes/Label Classes/YOLO HBB·OBB·Seg·Pose/VOC Det·Seg/COCO Det·Seg·Pose/DOTA/MASK/MOT/ODVG/MMGD/PPOCR Rec·KIE/VLM-R1 OVD) | 📦上游 | Upload 菜单 | 底层 `utils/upload.py` 21 文件与上游 MD5 一致 |
| 导出 17 种格式(YOLO HBB·OBB·Seg·Pose/VOC Det·Seg/COCO Det·Seg·Pose/DOTA/MASK/ODVG/MOT/MOTS/PPOCR Rec·KIE/VLM-R1 OVD) | 📦上游 | Export 菜单 | 底层 `utils/export.py` 与上游 MD5 一致 |

## 十、管理器与统计工具(上游)

| 功能 | 状态 | 入口 | 说明 |
|---|:---:|---|---|
| 标注统计总览(Overview) | 📦上游 | Tool 菜单 | |
| 子图导出 / 可视化图导出 / 可视化视频导出 | 📦上游 | Tool 菜单 | |
| Label Manager / Group ID Manager / Shape Manager / Shape Converter | 📦上游 | Tool 菜单 | |

## 十一、设置系统(上游框架 + fork 扩展)

| 功能 | 状态 | 入口 | 说明 |
|---|:---:|---|---|
| 设置对话框框架(General/Shape/Canvas/Shortcuts 四区 + 动态快捷键) | 📦上游 | `shortcuts["open_settings"]` | `settings/` 目录为上游原版 |
| 上游 46 个静态字段(行为/标签对话框/图形样式/画布交互/滚轮编辑/十字线/属性叠加/旋转/画笔/立方体/掩膜) | 📦上游 | 设置面板 | |
| 上游 75 个快捷键字段 | 📦上游 | Shortcuts 区 | 5 分类动态生成 |
| **设置项深链跳转**(菜单直达具体字段并滚动定位) | ✅新增 | 内部 `navigate_to_setting()` | 替代被删的十字线独立对话框 |
| fork 新增 6 静态字段 + pose_view 20 字段 + 8 快捷键字段 | ✅新增 | 设置面板 | 详见姊妹篇 |
| fork 新增 12 个仅 yaml 隐藏配置键 | ✅新增 | `xanylabeling_config.yaml` | 详见姊妹篇 |

## 十二、核心引擎与工程层增强(无 UI 入口)

| 功能 | 状态 | 入口 | 说明 |
|---|:---:|---|---|
| Shape 几何渲染缓存(bbox/path 4 缓存槽) | ✅新增 | 内部 `shape.py` | 全部几何变更入口统一失效 |
| Shape deepcopy/pickle 修复 | ✅新增 | 内部 | Qt 缓存槽不参与序列化 |
| Shape.paint 无副作用化(`force_unselected`) | 🔧增强 | 内部 | 修复绘制期改选中态的副作用 |
| widgets 包懒加载导出 | 🔧增强 | 内部 `widgets/__init__.py` | 由急切导入改 `__getattr__` 懒导出;移除已删对话框 |
| 顶层包 CLI 助手懒加载 | 🔧增强 | 内部 `anylabeling/__init__.py` | checks/convert 延迟导入 |
| 日志着色修复 / 标签列表空指针防御 | 🔧增强 | 内部 `logger.py` / `label_list_widget.py` | 小型 bugfix |
| i18n:全部个人功能的 4 语言翻译 | 🔧增强 | `.ts`/`.qm`/`resources.py` | 4 语言 ×350 行级变更 |
| 构建脚本:PySide2→PyQt6 导入重写 + 中文文档 | 🔧增强 | `scripts/compile_languages.py` 等 | |
| 测试工具配置(pytest testpaths / black extend-exclude / pre-commit 正则) | 🔧增强 | `pyproject.toml` 等 | 与依赖无关,依赖数组逐字节同上游 |

## 十三、数据脚本与 QA 子项目(上游无,fork 全新)

| 功能 | 状态 | 入口 | 说明 |
|---|:---:|---|---|
| 数据处理脚本集 41 个 CLI | ✅新增 | `scripts/` | YOLO Pose 三步流水线/ViTPose 对比/质检 CLI/统计对比/重排批操/工程辅助 |
| pose_qa 独立 QA 子项目(4 阶段) | ✅新增 | `pose_qa/` | ViTPose ONNX 导出→推理→OKS 对比/基线统计/空间检查→导出 Inspector |
| group_id 匈牙利回写清洗器 | ✅新增 | `pose_qa/grouping/group_merger.py` | 按标注员物理习惯匹配 |
| migration 原型 / historical_code_3.3.8 归档 | ✅新增(归档) | `migration/` / `historical_code_3.3.8/` | 旧版快照与未集成原型,不参与运行 |

## 十四、测试体系

| 功能 | 状态 | 入口 | 说明 |
|---|:---:|---|---|
| 上游测试(含 test_settings 2 文件被 fork 扩展) | 📦上游 + 🔧 | `tests/` | |
| fork 新增 68 个测试文件 ~16,600 行 | ✅新增 | `tests/` | 覆盖矩形尺寸/边编辑/质检/索引/筛选/数字键/视口/Pose 等 13 域 |

---

## 统计汇总

| 维度 | 📦上游 | 🔧增强 | ✅新增 | ⛔退役 |
|---|---:|---:|---:|---:|
| 去重后独立 UI action(约 150 个) | ~126 | 2(set_cross_line、数字键组) | ~24 | 0 |
| 设置面板字段(146+) | 121 | — | 34(含 pose_view 20) | — |
| 核心功能模块(portfolio 16 模块口径) | — | 3(#09/#13 + 数字管理器) | 12 | 1(#10) |
| 数据层/引擎内部增强 | — | 7 | 9 | — |
| 脚本与子项目 | 0 | — | 41 脚本 + pose_qa 8 CLI | — |

> 总体格局:**UI 层约 82% 保留上游、18% 个人扩展;导入/导出与依赖打包层 0 改动;个人精力集中在质检、筛选索引、视口、矩形精修、数字键工作流五大方向。**
